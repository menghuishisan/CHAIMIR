# ExternalSecret 统一模板。
# 职责:定义 chaimir-secret 所需密钥键集合;环境 overlay 只覆盖 secretStoreRef、刷新周期和 remoteRef 前缀。
# 依据 CLAUDE.md §6/§7:密钥走 K8s Secret/KMS,不入代码、不入镜像、不入仓库。
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: chaimir-secret
  namespace: chaimir-system
  labels:
    app.kubernetes.io/part-of: chaimir
spec:
  refreshInterval: 15m
  secretStoreRef:
    kind: SecretStore
    name: chaimir-external-secret-store
  target:
    name: chaimir-secret
    creationPolicy: Owner
  data:
    - secretKey: JWT_SIGNING_KEY
      remoteRef: {key: chaimir, property: JWT_SIGNING_KEY}
    - secretKey: APP_ENCRYPTION_KEY
      remoteRef: {key: chaimir, property: APP_ENCRYPTION_KEY}
    - secretKey: APP_HMAC_KEY
      remoteRef: {key: chaimir, property: APP_HMAC_KEY}
    - secretKey: PG_PASSWORD
      remoteRef: {key: chaimir, property: PG_PASSWORD}
    - secretKey: PG_PRIV_USER
      remoteRef: {key: chaimir, property: PG_PRIV_USER}
    - secretKey: PG_PRIV_PASSWORD
      remoteRef: {key: chaimir, property: PG_PRIV_PASSWORD}
    - secretKey: REDIS_PASSWORD
      remoteRef: {key: chaimir, property: REDIS_PASSWORD}
    - secretKey: OBJECT_STORAGE_ACCESS_KEY
      remoteRef: {key: chaimir, property: OBJECT_STORAGE_ACCESS_KEY}
    - secretKey: OBJECT_STORAGE_SECRET_KEY
      remoteRef: {key: chaimir, property: OBJECT_STORAGE_SECRET_KEY}
    - secretKey: NATS_TOKEN
      remoteRef: {key: chaimir, property: NATS_TOKEN}
    - secretKey: GRADE_TRANSCRIPT_SIGNING_KEY
      remoteRef: {key: chaimir, property: GRADE_TRANSCRIPT_SIGNING_KEY}
    - secretKey: SMS_HTTP_TOKEN
      remoteRef: {key: chaimir, property: SMS_HTTP_TOKEN}
    - secretKey: CHAIMIR_WALLET_SIGNER_PRIVATE_KEY
      remoteRef: {key: chaimir, property: CHAIMIR_WALLET_SIGNER_PRIVATE_KEY}
    - secretKey: CHAIMIR_WALLET_SESSION_SECRET
      remoteRef: {key: chaimir, property: CHAIMIR_WALLET_SESSION_SECRET}
    - secretKey: CHAIMIR_PAYMASTER_PRIVATE_KEY
      remoteRef: {key: chaimir, property: CHAIMIR_PAYMASTER_PRIVATE_KEY}
    - secretKey: CHAIMIR_PAYMASTER_SERVICE_AUTH_SECRET
      remoteRef: {key: chaimir, property: CHAIMIR_PAYMASTER_SERVICE_AUTH_SECRET}
    - secretKey: CHAIMIR_EVENT_LISTENER_SERVICE_AUTH_SECRET
      remoteRef: {key: chaimir, property: CHAIMIR_EVENT_LISTENER_SERVICE_AUTH_SECRET}
    - secretKey: CHAIMIR_EVM_DEPLOYER_PRIVATE_KEY
      remoteRef: {key: chaimir, property: CHAIMIR_EVM_DEPLOYER_PRIVATE_KEY}
    - secretKey: BOOTSTRAP_ADMIN_PHONE
      remoteRef: {key: chaimir, property: BOOTSTRAP_ADMIN_PHONE}
    - secretKey: BOOTSTRAP_ADMIN_NAME
      remoteRef: {key: chaimir, property: BOOTSTRAP_ADMIN_NAME}
    - secretKey: BOOTSTRAP_ADMIN_PASSWORD
      remoteRef: {key: chaimir, property: BOOTSTRAP_ADMIN_PASSWORD}
    - secretKey: BOOTSTRAP_PLATFORM_ADMIN_USERNAME
      remoteRef: {key: chaimir, property: BOOTSTRAP_PLATFORM_ADMIN_USERNAME}
    - secretKey: BOOTSTRAP_PLATFORM_ADMIN_PASSWORD
      remoteRef: {key: chaimir, property: BOOTSTRAP_PLATFORM_ADMIN_PASSWORD}
