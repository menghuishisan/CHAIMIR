# M5 题库与模板中心 — 数据模型

> PostgreSQL。内容外壳与内容体为租户级(tenant_id + RLS);共享库内容跨租户只读可克隆。
> 建模遵循:消灭数据冗余、表合并有度。
> 最后更新:2026-05-29

---

## 1. 表清单总览

| 表名 | 用途 | 范围 |
| --- | --- | --- |
| `content_item` | 内容外壳(统一元信息 + 版本) | 租户 |
| `content_body` | 类型化内容体(实验/竞赛/理论,JSONB) | 租户 |
| `content_category` | 分类树 | 租户 |
| `content_usage_ref` | 内容版本复用来源登记 | 租户 |
| `paper` | 组卷(题目引用集合) | 租户 |
| `paper_item` | 卷与题目关联 + 分值 | 租户 |

> 消灭冗余/合并说明:
> - **版本不单独建表**:`content_item` 以 `(code, version)` 联合唯一表达多版本,同 code 多行即多版本。
> - **内容体与外壳一对一拆表**:外壳查列表频繁、字段小;内容体是大 JSONB(含答案/判题配置),拆开避免列表查询拉大字段,也便于权限层只在取 full 时读内容体。
> - **标签/知识点不单建关联表**:用 `content_item.tags TEXT[]` / `knowledge_points TEXT[]`(PG 数组 + GIN 索引),避免 N×M 关联表冗余;分类是树形强结构故独立成表。

---

## 2. 租户级表

### 2.1 content_item — 内容外壳

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| code | VARCHAR(96) | 内容标识(克隆生成新 code) |
| version | VARCHAR(32) | semver |
| type | SMALLINT | 1实验模板 / 2竞赛题 / 3理论题 |
| title | VARCHAR(255) | |
| category_id | BIGINT NULL | 分类 |
| difficulty | SMALLINT | 1入门/2进阶/3高级/4研究 |
| tags | TEXT[] | 标签(GIN 索引) |
| knowledge_points | TEXT[] | 知识点(GIN 索引) |
| author_id | BIGINT | 作者 |
| author_type | SMALLINT | 1教师 / 2系统 / 3外部源 |
| visibility | SMALLINT | 1私有 / 2本租户 / 3共享库 |
| status | SMALLINT | 1草稿 / 2已发布 / 3已弃用 |
| usage_count | INT | 复用统计 |
| version_hash | VARCHAR(64) | 版本内容 SHA-256 摘要,用于发布后完整性校验 |
| created_at / updated_at | TIMESTAMPTZ | |

唯一约束:`(tenant_id, code, version)`。

`usage_count` 只由 `content_usage_ref` 聚合刷新,业务模块不得直接递增或自建复用统计。

### 2.2 content_body — 类型化内容体

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| item_id | BIGINT PK | 与 content_item 一对一 |
| tenant_id | BIGINT | |
| body | JSONB | 类型化结构(见架构 §2.2) |
| sensitive_fields | TEXT[] | 敏感字段路径(answer/judge_config.testcases/flag…),权限层据此过滤 |

> 大文本/附件存 MinIO,body 中存引用 key,不入库大二进制。

### 2.3 content_category — 分类树

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| parent_id | BIGINT NULL | 父分类(树形) |
| name | VARCHAR(128) | |
| sort | INT | 排序 |

### 2.4 content_usage_ref — 内容引用来源

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| item_id | BIGINT | 被引用的 `content_item.id` |
| item_code | VARCHAR(96) | 被引用内容 code 快照 |
| item_version | VARCHAR(32) | 被引用内容版本快照 |
| source_scope | VARCHAR(32) | 来源域,如 `teaching.assignment`、`experiment.definition`、`contest.contest` |
| source_ref | VARCHAR(128) | 来源内稳定引用,同一业务对象全生命周期不变 |
| created_at | TIMESTAMPTZ | |

唯一约束:`(tenant_id, source_scope, source_ref, item_code, item_version)`。

`content_usage_ref` 是 M5 删除保护和 `usage_count` 的唯一事实来源。下游业务模块必须通过 `internal/contracts.ContentReadService.ReplaceUsageRefs(ctx, tenantID, sourceScope, sourceRef, refs)` 幂等替换当前业务对象持有的内容版本集合;不得调用旧式加一接口,不得在业务模块自建引用计数。

### 2.5 paper — 组卷

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| name | VARCHAR(255) | 卷名 |
| author_id | BIGINT | |
| gen_mode | SMALLINT | 1手动选题 / 2按条件随机 |
| gen_criteria | JSONB | 随机抽题条件(知识点/难度/数量) |
| created_at / updated_at | TIMESTAMPTZ | |

### 2.6 paper_item — 卷题关联

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| paper_id | BIGINT | |
| item_code | VARCHAR(96) | 引用题目 code |
| item_version | VARCHAR(32) | 锁定版本 |
| score | INT | 该题分值 |
| seq | INT | 题序 |

---

## 3. 多租户隔离

- 全部表带 `tenant_id` + RLS(同 M1)。
- `content_item.visibility=3(共享库)`:仅在 `status=2(已发布)` 时跨租户只读可克隆;数据库 RLS 对已发布共享内容只放开 SELECT,写入/更新/删除仍只能命中当前租户;克隆时复制到目标租户生成新 code,与源解耦。草稿不得直接写成 shared,必须先发布再走 share 状态流转。
- 私有化:`tenant_id` 恒定;共享库内容可随离线包分发。

## 4. 关键索引

- `content_item(tenant_id, code, version)` UNIQUE
- `content_item(tenant_id, type, status)`、`content_item(visibility)`(共享库检索)
- `content_item USING GIN(tags)`、`GIN(knowledge_points)`
- `content_usage_ref(tenant_id, item_id)`、`content_usage_ref(tenant_id, source_scope, source_ref)`
- `paper_item(tenant_id, paper_id, seq)`
