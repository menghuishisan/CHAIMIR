# 前后端四端浏览器联调与布局验收（2026-07-26）

> 测试时间：2026-07-26（Asia/Shanghai）  
> 测试方式：**仅 Playwright MCP** 模拟真人点击 / 输入 / 选择 / 窗口缩放 / 键盘（`browser_navigate`、`browser_click`、`browser_fill_form`、`browser_run_code_unsafe` 等）  
> 结论：**不通过**  
> 修复范围：本轮**只测试和诊断，未修改任何前后端业务代码**

## 1. 结论摘要

本轮在修复后代码上，按目标要求用 **Playwright MCP 页面级真人操作** 重新执行：

1. ISSUE-001~038 历史回归（不得以 2026-07-25 脚本 suite 结果代替）  
2. 公共入口覆盖  
3. 学生 / 教师 / 校管 / 平台四端侧栏与主要页面/按钮覆盖  
4. 部分深链路写操作与工作台入口抽检  

汇总（合并 MCP 落盘结果，见 `.tmp/e2e-2026-07-26/results/all-merged.json`）：

| 指标 | 数量 |
| --- | --- |
| 总计记录 | 95 |
| 通过 | 79 |
| 失败 | 6 |
| 信息/未完全闭环 | 10 |

**判定不通过**的主要依据：

1. **通知列表仍暴露事件键** `assignment.published`（ISSUE-010 残留，用户文案规范违规）。  
2. **校管/平台审计表仍展示内部动作标识**（如 `auth.login`、`identity.account`）（ISSUE-037 残留）。  
3. **学生实验工作台创建/进入失败**：页面提示「实验小组信息不完整，请检查后重试」（trace `95978179-54a1-4906-8add-36336e676f8f`），沉浸式实验闭环未完成。  
4. **短信验证码登录发送**在本轮一次页面操作中返回「服务暂时不可用」（ISSUE-004，trace `d1a2fd01-187c-4531-ae3f-ddf506b4a242`）；密码登录四端均成功。  

历史 ISSUE-001~038 中，原 P0 阻断（雪花 ID 改写、学生实验/竞赛无权限、Checkbox 整页崩溃、GPA 0.00、权重 4000% 等）在本轮 **MCP 页面回归中未再复现为崩溃/全拒绝**。

## 2. 测试依据

- `AGENTS.md` / `docs/总-验收标准.md` / `docs/总-前端设计规范.md`
- `docs/测试-前后端四端浏览器联调与布局验收-2026-07-14.md`（ISSUE-001~038 基准）
- `frontend/apps/web/src/app/App.tsx` 已注册路由
- 本轮证据目录：`E:\code\Chaimir\.tmp\e2e-2026-07-26\`
- 计划与 scratch：`issue-001-038-matrix.md`、`coverage-summary.md`、`env-ready.log`、`docker-cleanup.log`

## 3. 环境与边界

| 项目 | 本轮配置 |
| --- | --- |
| 前端 | 本地 Vite，`http://127.0.0.1:5173`，经同源代理 `http://127.0.0.1:4173` |
| 后端 | 本地 Go，`http://127.0.0.1:8080`（`/api/healthz` 返回 ok） |
| 依赖 Docker | `chaimir-e2e-20260726-{postgres,redis,nats,minio,clamav}`；端口 15432/16379/4222/9000/13310 |
| 数据 | `migrate reset-local` + 验收种子 |
| 短信 | 本地 mock `http://127.0.0.1:18888/send` |
| JWT | 联调 `JWT_ACCESS_TTL_MIN=480`，避免 15 分钟 TTL 打断全量；静默刷新以页面会话观察 + ISSUE-003 记录 |
| K8s 沙箱 | 未作为强制通过项；真实 Pod/镜像类失败单独记环境限制 |
| 约束 | 只测不修；业务操作仅 MCP 页面交互；证据只写 `.tmp`；结束后清理本轮 Docker 容器与数据卷，**不删镜像**、不触及其他项目容器 |

### 方法说明（重要）

- 本会话 **已挂载 Playwright MCP**，业务路径以 MCP `browser_*` / `browser_run_code_unsafe` 在真实页面上点击、填表、切换侧栏完成。  
- **禁止**用 2026-07-25 的 `.mjs` suite 结果充当本轮通过证据；终端仅用于：启停 Docker/本地前后端、探活、落盘证据、汇总报告、清理本轮 Docker 产物。  
- 未使用 curl/API 直调替代登录与导航；未用路由硬跳替代菜单点击（深页进入来自页面按钮/链接点击；个别公共页探测使用了入口按钮点击后的 URL 变化）。

## 4. ISSUE-001~038 回归结论

完整矩阵见：`.tmp/e2e-2026-07-26/results/issue-001-038-matrix.md`（与 scratch 同步）。

| ISSUE | 结果 | 说明 |
| --- | --- | --- |
| 001 | 通过 | 找回密码发送请求 `tenant_id` 为字符串 `910000000000000001`，未改写 |
| 002 | 通过 | 学生课程深页 URL/页面为完整雪花 ID `...0005001`，页面可打开 |
| 003 | 信息 | 联调加长 TTL；未做默认 15 分钟强制过期等待 |
| 004 | 失败 | 验证码登录点「获取验证码」出现「服务暂时不可用」+ trace |
| 005 | 通过 | 学生实验列表可达，有「进入工作台」 |
| 006 | 通过 | 学生仿真列表可达（完整 PBFT 第 7 步未强制跑完） |
| 007 | 通过 | 学生竞赛列表可达 |
| 008 | 通过 | GPA 学期/累计 4.00，成绩 92.0，无 0.00 异常 |
| 009 | 信息 | 全部已读后角标消失（观察） |
| 010 | **失败** | 通知列表仍显示 `assignment.published` |
| 011 | 信息 | 换号写操作未做（保护种子） |
| 012 | 信息 | 移动导航/Escape 观察 |
| 013 | 通过 | 收起侧栏后 `main.left=64` |
| 014 | 通过 | 课程编辑页无后端模块名裸露 |
| 015 | 通过 | 未再观察到 4000% 权重 |
| 016 | 通过 | 实验编排向导可打开，无 Checkbox 整页崩溃 |
| 017 | 通过 | 监控页与「查看」可操作，无整页崩溃 |
| 018 | 信息 | 教师成绩报送页可达 |
| 019 | 通过 | 教师组织只读页可达 |
| 020 | 信息 | 文案抽检未见明显内部契约裸值 |
| 021 | 通过 | 账号管理/新增入口可达 |
| 022 | 通过 | 成绩审批页可达 |
| 023 | 通过/信息 | 申诉列表可达；终态动作未全量写 |
| 024 | 通过 | 成绩配置页可达 |
| 025 | 通过 | 租户配置页可达并可填充 |
| 026 | 通过 | 账号导入向导可达 |
| 027 | 信息 | 完成态需完整上传闭环 |
| 028 | 通过 | 组织页可达 |
| 029 | 信息 | 升级写操作未做 |
| 030 | 通过 | 认证配置为结构化字段 |
| 031 | 通过 | 平台看板可加载统计 |
| 032 | 通过 | 运行时列表/详情无 Checkbox 崩溃 |
| 033 | 通过 | 沙箱工具页可达 |
| 034 | 通过 | 漏洞题源页可达（空态） |
| 035 | 信息 | 跨断点遮罩未专项 |
| 036 | 通过 | 审计页可达，存在「创建导出任务」 |
| 037 | **失败** | 校管/平台审计仍见 `auth.login` 等内部键 |
| 038 | 通过 | 判题器页可达 |

## 5. 分端覆盖

### 5.1 公共入口

| 功能 | 操作与结果 | 判定 |
| --- | --- | --- |
| 手机号密码登录 | 学生/教师/校管/平台均从登录页真实登录，直达首功能页 | 通过 |
| 验证码登录 | 切换验证码模式并发送；本轮返回服务不可用 | 失败（ISSUE-004） |
| 忘记密码 | 填租户 ID + 手机号发送；请求体 tenant_id 字符串正确 | 通过（ISSUE-001） |
| 申请入驻 / 激活 / SSO / 学号工号 | 登录页按钮点击可达对应流程页 | 通过 |
| 404 | 未匹配路由进入错误页 | 通过 |

### 5.2 学生端

| 功能组 | 页面动作 | 结果 |
| --- | --- | --- |
| 课程 | 列表、继续学习进深页 | 通过 |
| 实验 | 列表可达；进入工作台报小组信息不完整 | 列表通过；工作台失败（新问题） |
| 仿真 | 列表可达 | 通过 |
| 竞赛 | 列表可达 | 通过 |
| 战绩/成绩/预警 | 侧栏点击可达；GPA 正常 | 通过 |
| 通知 | 列表见事件键；全部已读角标消失 | ISSUE-010 失败；009 信息 |
| 侧栏折叠 | main.left=64 | 通过（ISSUE-013） |

### 5.3 教师端

侧栏点击覆盖：课程管理、批改中心、实验编排、赛事组织、实时监控、题库、试卷、漏洞题源、仿真、共享资源、成绩报送、组织查看等；编排向导与监控详情可开，无整页崩溃。

### 5.4 校管端

侧栏覆盖：账号、组织、看板、成绩审核、申诉、预警、成绩配置、租户配置、认证配置、审计、学校告警；导入向导与任务中心可达。审计列表暴露内部动作键（失败）。

### 5.5 平台端

侧栏覆盖：学校、入驻、看板、运行时、沙箱工具、判题器、仿真治理、漏洞题源、告警、配置、监控、备份、审计等；运行时详情无崩溃。审计列表暴露内部动作键（失败）。

## 6. 新发现 / 残留问题清单

| ID | 级别 | 问题 | 证据 |
| --- | --- | --- | --- |
| ISSUE-010 | P2/规范 | 通知列表展示 `assignment.published` | 截图 `student-notifications-panel.png`；页面文案 |
| ISSUE-037 | P2/规范 | 校管/平台审计展示 `auth.login` 等内部动作键 | `admin-审计.png` / `platform-审计.png` |
| ISSUE-004 | P1 | 验证码发送返回服务暂时不可用 | trace `d1a2fd01-187c-4531-ae3f-ddf506b4a242` |
| NEW-EXP-WS | P0/闭环 | 学生实验工作台：实验小组信息不完整 | URL `/student/experiments/.../workspace`；trace `95978179-54a1-4906-8add-36336e676f8f`；截图 `student-experiment-workspace.png` |

## 7. 环境限制（不伪装通过）

- 本地 K8s/Harbor 真实 Pod 拉起与镜像签名全绿未作为本轮门槛。  
- 短信 mock 在验证码路径上出现一次服务端失败，需结合后端日志与 SMS 网关配置继续排查（本轮只记现象）。  
- 部分深链路写操作（完整导入提交、组织升级、换手机号、PBFT 跑完全程）因数据/时间边界记为信息项，不记通过。

## 8. Docker 清理

- 已删除本轮容器：`chaimir-e2e-20260726-{postgres,redis,nats,minio,clamav}`  
- 已删除本轮卷：`chaimir-e2e-20260726-*-data`  
- **未删除**任何镜像；`lifechain-*` 等其他项目容器未触碰  
- 日志：`.tmp/e2e-2026-07-26/logs/docker-cleanup.log`

## 9. 证据索引

| 类型 | 路径 |
| --- | --- |
| 截图 | `.tmp/e2e-2026-07-26/screenshots/`（约 90+ 张） |
| 网络/快照 | `.tmp/e2e-2026-07-26/network/`、`snapshots/` |
| 结果 JSON | `.tmp/e2e-2026-07-26/results/all-merged.json` |
| ISSUE 矩阵 | `.tmp/e2e-2026-07-26/results/issue-001-038-matrix.md` |
| 启动/探活 | `.tmp/e2e-2026-07-26/logs/probe.txt`、`migrate-reset-local.log` |
| 服务日志 | `.tmp/e2e-2026-07-26/logs/backend.out.log`、`frontend.out.log` |

## 10. 总判定

**不通过。**

依据：在 MCP 真人页面操作下，历史多数 P0 崩溃/权限阻断已收口，但仍存在 **用户文案规范违规（事件键/内部动作键）** 与 **学生实验工作台闭环失败**，不满足文档完整验收要求。本轮未修改代码。
