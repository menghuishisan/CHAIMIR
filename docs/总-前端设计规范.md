# 总-前端设计规范

> Chaimir 四端前端的**唯一设计真相源**:从设计令牌、组件库、信息架构,到沉浸式工作台、数据可视化、响应式自适应、无障碍基线与工程落地的完整规范。
> 本文与 `CLAUDE.md`/`AGENTS.md` §10、`docs/总-工程目录设计.md`(`frontend/` 结构)互为一体:**§10 是铁律摘要,本文是展开;工程目录定义代码落点。**
> 改动设计先改本文,再写代码 —— 保持一致(CLAUDE.md 总则)。
>
> 注意:**高保真原型已清理,后续重建。** 重建时必须以本文、已实现功能清单、后端角色/接口语义和真实业务流程为输入,不能照抄旧原型或当前不合理前端外观。**一切以本规范 + 实际功能语义为准**;本文未尽处以工程实践与无障碍/响应式标准补足。

---

## 0. 文档定位与铁律

### 0.1 这份文档解决什么问题

此前前端设计只散落在 `CLAUDE.md §10` 几行和早期示意中,没有正式规范。真做 React 四端时,四套工作台、上百个页面若各做一套,必然风格漂移、无障碍失守、组件重复。本文把设计沉淀为**可检索、可校验、可落地**的单一标准,让 `frontend/packages/ui` 直接照此实现。后续高保真原型必须基于已实现功能与页面语义重新设计,不继承旧示意的简化和错误。

### 0.2 前端铁律(违反即不可合并)

| # | 铁律 | 依据 |
| --- | --- | --- |
| FE-1 | **全令牌化**:组件零裸 hex,一律引用语义令牌(`var(--color-*)`)。原始色阶→语义令牌单一真相源。 | CLAUDE.md §10 |
| FE-2 | **无障碍达标**:关键文字/控件对比度 ≥ WCAG AA(正文 4.5:1、大字/图形 3:1);焦点可见、键盘可达、尊重 `prefers-reduced-motion`、信息不靠颜色单一传达。 | §3 |
| FE-3 | **禁用 emoji**:界面/文案/代码一律不用 emoji,图标统一 **Lucide**(单一图标族、描边一致)。 | CLAUDE.md §10 |
| FE-4 | **文案面向用户**:可见文字用用户能懂的自然语言,不暴露开发术语(`null`/`timeout`/字段名/`error_code`)。每条文案斟酌。 | CLAUDE.md §8/§10 |
| FE-5 | **无独立工作台落地页**:四端登录后直达该角色侧栏的第一个功能页。 | CLAUDE.md §10、蓝图 §8 |
| FE-6 | **双模态**:日常用侧栏导航(深色框架+白底内容);做实验/仿真/答题进沉浸式全屏。 | §6/§7 |
| FE-7 | **多步骤状态服务端持久化**:向导/长表单中间态以服务端为权威,前端 localStorage 仅即时兜底。 | CLAUDE.md §9、蓝图 §11 |
| FE-8 | **错误分层暴露**:前端只展示友好文案 + `trace_id`,绝不显示堆栈/SQL/内部字段。按 `code` 决定交互,按 `message` 兜底。 | CLAUDE.md §8 |
| FE-9 | **窗口自适应(响应式)必做**:所有页面在常见视口(手机/平板/笔记本/大屏)与窗口任意缩放下均**正常可用、无横向滚动、无遮挡/错位**;布局随窗口流式重排,而非固定像素。沉浸式工作台同样自适应。 | §6.4 |
| FE-10 | **质感开发时打磨,不以旧稿为标准**:组件精度、交互手感、微动效必须在**真实开发中实测、优化、重做**。守住本文**原则**,以真实产品手感为达标判据。 | §4 |
| FE-11 | **结构化数据面向任务呈现**:JSONB/对象只是传输与存储格式,不得把原始 JSON、内部字段名或自由对象编辑器直接交给终端用户。编辑按领域字段拆分,只读结果使用标签、描述列表、表格、时间线或代码输出。 | §6.7 |

### 0.3 设计气质一句话

**开发者工具的精确感 × 教学产品的亲和力。** 不是消费级炫酷,也不是后台管理系统的呆板;是「专业、克制、可信、有学科代入感」。设计智能(ui-ux-pro-max)对区块链开发者工具的判定印证此方向:Inter 字体、暗色骨架、状态色体系(绿/琥珀/红)、最小辉光、可见焦点;**明确避免** AI 紫粉渐变、装饰性持续动画、慢动画(>500ms)。

---

## 1. 设计理念与品牌

### 1.1 青电 × 琥珀(冷暖双色)

品牌色经多轮迭代定为**青电 × 琥珀双色**,取代单一色板,解决「不单一又不杂」:

- **主色 = 青电(teal-cyan)**:链上感、数字感,替代千篇一律的科技蓝。用于**交互/CTA/导航激活/焦点/链接/进度**。
- **副色 = 琥珀(amber)**:能量、对抗、高亮。用于**统计强调/通知角标/告警/竞赛对抗/能量态**。
- **骨架 = 墨黑/石板(slate)**:深色框架(顶栏/侧栏底/沉浸面板)与中性层级。
- **冷暖对撞**:青电(冷)与琥珀(暖)形成张力,这是「突破常规」的落点 —— 突破在**配色**,不在字号/布局(布局保持克制)。

> 换主色 = 改一个令牌。所有品牌色点位都引用语义令牌,主色调整只动 `--color-primary*` 与 `--color-accent`。

### 1.2 双模态:框架深、内容白

| 模态 | 场景 | 配色 | 依据 |
| --- | --- | --- | --- |
| **导航态** | 日常:看课、管理、看板、列表 | 深色顶栏/侧栏白底 + **白底内容区** | 长时间读题/读文档,白底正文更护眼;设计智能虽推纯 OLED 暗色,但教学场景**经论证偏离**——正文不全黑。 |
| **沉浸态** | 做实验/仿真/答题/对抗回放 | **深色全屏工作台**(IDE/终端/仿真画布) | 代码与可视化在深色下对比更高、更聚焦,符合开发者工具直觉。 |

这套「深框架 + 白内容 + 沉浸深色」是**有意的折中**,不是没做暗色模式。纯暗色模式作为后续增量(见 §11)。

### 1.3 Logo 与标识

- Logo mark:青电渐变方块(`linear-gradient(135deg, cyan-400, cyan-600)`)+ Lucide 图标,深色顶栏上对比 ≥ 9:1。
- 不臆造第三方品牌 Logo;引用任何外部标识遵守其使用规范。

---

## 2. 设计令牌(Design Tokens)

> 令牌是**原始色阶(Primitive)→ 语义令牌(Semantic)** 两层。组件只引用语义层。真相源:`frontend/packages/ui/tokens/`。

### 2.1 颜色 — 原始色阶

完整 50–950 色阶定义以 `frontend/packages/ui/tokens/` 为准。关键锚点:

| 色族 | 角色 | 关键档位 | 用途锚点 |
| --- | --- | --- | --- |
| **青电 cyan** | 品牌主色 | `400 #22D3EE`(深底点睛,对 ink ≈9:1)、`500 #06B6D4`(纯品牌:指示条/焦点环)、`700 #0E7490`(浅区文字/CTA 底,对白字 ≈4.8:1) | 交互/导航/焦点 |
| **琥珀 amber** | 品牌副色 | `400/500`(深底点睛/装饰)、`700 #B45309`(浅区文字/底,对白字 4.5:1 达 AA) | 能量/对抗/告警 |
| **石板 slate** | 中性骨架 | `50→950` 全阶 | 背景/文字/边框/深色面板 |
| **状态** | 反馈 | green/red/blue/purple/teal 的 `50/100`(浅底)+ `600/700`(文字) | 成功/危险/信息/特殊 |

**无障碍硬约束(写进令牌注释)**:琥珀做文字/按钮底一律用 `amber-700`(对白字 4.5:1);亮琥珀 `400/500` 只用于深色面板上的点睛与**非文字装饰**(指示条/焦点环/图标),禁止用亮琥珀承载浅底上的正文。

### 2.2 颜色 — 语义令牌

分三组:**浅色内容区 / 深色面板 / 语义反馈**。命名规约 `--color-<role>[-<variant>]`。

```
/* 浅色内容区 */
--color-bg / surface / surface-sunken / surface-hover
--color-text / text-strong / text-sub / text-faint
--color-border / border-strong / border-subtle

/* 主色(青电) */
--color-primary / primary-hover / primary-fg / primary-soft / primary-text / accent / focus-ring
/* 副色(琥珀) */
--color-secondary / secondary-hover / secondary-soft / secondary-text / secondary-accent

/* 语义反馈(文字用 *-700,底用 *-50/100) */
--color-success/-bg/-border  --color-warning/-bg/-border
--color-danger/-bg/-border   --color-info/-bg/-border  --color-purple/-bg/-border
--color-danger-solid/-hover  /* 实心危险按钮 */

/* 深色面板(顶栏/Hero/沉浸工作台) */
--color-dark-bg / dark-surface / dark-elevated / dark-text / dark-text-sub / dark-border
--color-on-dark-accent  --color-terminal-bg  --color-editor-bg
```

> `text-faint` 仅用于占位/禁用,**不承载信息**(对比度不足,信息态禁用)。深底次要文字用 `slate-300`(≈7:1),不用 `slate-400`(偏低)。

### 2.3 字体

- **拉丁/数字:Inter**(400/500/600/700/800);**中文:系统中文**(PingFang SC / Microsoft YaHei)渐进增强。
- **等宽:JetBrains Mono**(代码、哈希、地址、trace_id、数据列)。
- 生产环境**自托管字体**(`font-display: swap`),本地预览允许离线回退系统字体。
- **字号阶梯**(令牌):`xs 12 / sm 13 / base 14 / md 15 / lg 16 / xl 18 / 2xl 22 / 3xl 28 / 4xl 34`。正文基准 14(数据密集后台),移动端正文 ≥16 防 iOS 自动缩放。
- **行高**:`tight 1.3 / normal 1.5 / relaxed 1.65`;正文 1.5,长文 1.65。
- **数字列必用等宽数字**(`font-variant-numeric: tabular-nums`):统计数值、成绩、计时、表格数字列,防跳动。

### 2.4 间距 · 圆角 · 阴影 · 动效 · 层级

| 维度 | 令牌 | 值 |
| --- | --- | --- |
| **间距**(4/8 节奏) | `--space-1..16` | 4/8/12/16/20/24/32/40/48/64 |
| **圆角** | `--radius-xs/sm/(base)/lg/full` | 6/8/12/16/999 |
| **阴影**(柔和分层) | `--shadow-xs/(base)/md/lg` | 见 §4.1,低透明度多层叠加,杜绝硬黑影 |
| **过渡** | `--t-fast/(t)/-slow` | .12s / .18s / .3s |
| **签名缓动** | `--ease / --ease-out` | `cubic-bezier(.22,.61,.36,1)` / `cubic-bezier(.16,1,.3,1)` |
| **层级 z-index**(统一阶,禁止裸数字) | `--z-below/ground/base/sticky/dropdown/drawer/immersive/modal/toast` | -1/0/1/50/60/80/100/120/200 |
| **布局尺寸** | `--topnav-h/sidebar-w/-collapsed/content-max` | 56/248/64/1200px |

> z-index **只能引用令牌阶**,严禁组件里写 `z-index: 9999`。新层级需求先在令牌表新增一阶。

**断点实现方式(强制)**

CSS 侧禁止在 `@media` 内直接使用 CSS 变量(浏览器不解析),断点通过 **PostCSS `postcss-custom-media`** 在构建期编译到规范值。工程约定:

```css
@custom-media --bp-sm (min-width: 640px);
@custom-media --bp-md (min-width: 768px);
@custom-media --bp-lg (min-width: 1024px);
@custom-media --bp-xl (min-width: 1280px);
@custom-media --bp-2xl (min-width: 1536px);
```

使用:`@media (--bp-lg) { ... }`。TS/JS 侧沿用 `packages/ui/src/tokens/index.ts` 的 `breakpoints` 常量。禁止在业务样式里出现 `640/768/1024/1280/1536` 之外的媒体查询数值;`720/860/1100/1440` 等历史断点必须在 Phase 1 内清零。stylelint 通过 `media-feature-name-value-allowed-list` + 自定义 `@custom-media` 白名单校验。

**动效令牌落点(与 §4.2 强红线一一对应)**

以下令牌与 `packages/ui/src/tokens/` 保持双出(CSS 变量 + TS 常量)。缺失或与本表不一致视为回归,CI 阻断。

```css
--ease-out: cubic-bezier(.23, 1, .32, 1);
--ease-in-out: cubic-bezier(.77, 0, .175, 1);
--ease-drawer: cubic-bezier(.32, .72, 0, 1);

--t-press: 140ms;
--t-tooltip: 160ms;
--t-menu: 200ms;
--t-modal-enter: 240ms;   --t-modal-exit: 160ms;
--t-drawer-enter: 260ms;  --t-drawer-exit: 180ms;
--t-toast-enter: 260ms;   --t-toast-exit: 180ms;

--press-scale: .97;
```

保留原 `--t-fast / --t / --t-slow` 与 `--ease / --ease-out` 令牌作为通用兜底;上述新增令牌为组件契约级。禁止新增 `--ease-in`。

---

## 3. 无障碍基线(WCAG AA,强制)

无障碍是铁律 FE-2,不是可选项。设计智能将其列为**最高优先级(CRITICAL)**。

### 3.1 对比度

| 元素 | 标准 | 本规范落点 |
| --- | --- | --- |
| 正文文字 | ≥ 4.5:1 | `text` 对白底 ≈12:1;`text-sub` ≈4.6:1 |
| 大字(≥18px/粗 14px)、图形/图标 | ≥ 3:1 | 状态点、指示条、图标达标 |
| CTA 按钮文字 | ≥ 4.5:1 | 青电 CTA 用 `cyan-700` 对白字 ≈4.8:1;琥珀底用 `amber-700` |
| 深底文字 | 主 ≥4.5:1 / 次 ≥3:1 | `dark-text` ≈15:1;`dark-text-sub`(slate-300)≈7:1 |
| 焦点环 / 装饰 | ≥ 3:1 | `accent`(cyan-500)在深浅底均达标 |

历史修复方向:①琥珀 CTA 使用 `amber-700`;②激活态、深底次要文字对比度上调。**新增颜色必须查表验证后入令牌。**

### 3.2 焦点 · 键盘 · 动效 · 颜色

- **焦点可见**:全局 `:focus-visible` 焦点环(2px `focus-ring` + 2px offset),**绝不 `outline:none` 不补替代**。鼠标点击不显示焦点环(`:focus:not(:focus-visible)`)。
- **键盘可达**:Tab 顺序 = 视觉顺序;所有交互元素可聚焦可操作;模态/抽屉/向导提供 Esc/取消等逃生路径;拖拽操作提供键盘替代。
- **跳过链接**:`.skip-link` 跳到主内容(读屏/键盘用户);`.sr-only` 供读屏专用文本。
- **reduced-motion**:`@media (prefers-reduced-motion: reduce)` 全局把动画/过渡降到 ~0;流式图表冻结动画、保留当前值大字 KPI。
- **颜色非唯一信息**:状态除颜色外**必带图标或文字**(成功√/危险!/状态点配文字标签);图表系列用线型/纹理区分,不靠颜色单一。
- **图标按钮**:纯图标按钮必带 `aria-label`;触达区 ≥ 44×44(不足则扩 hitArea)。
- **表单无障碍**:`label[for]` 显式关联;错误 `role="alert"`/`aria-live`;提交后自动聚焦首个错误字段;Toast 用 `aria-live="polite"` 不抢焦点。
- **标题层级**:`h1→h6` 顺序,不跳级。

### 3.3 无障碍验收清单(每页过)

- [ ] 所有文字/控件对比度达标(查 §3.1)
- [ ] 仅用键盘可完成核心流程,焦点全程可见
- [ ] 关闭动画(reduced-motion)后无信息丢失、无布局错乱
- [ ] 状态/图表不靠颜色单一传达
- [ ] 图标按钮有 `aria-label`,图片有 alt
- [ ] 表单错误就近显示、读屏可感知、自动聚焦首错

---

## 4. 质感三件套(从「好」到「精致」)

> 这一章是设计上限的所在 —— 同样的功能,质感决定它看起来是「能用的后台」还是「专业的产品」。三件套:**间距·布局·层次 / 交互微动效 / 组件精度**。
>
> 注意:**本章数值是参考基线(起点),不是标准(终点)。** 组件精度、交互手感这类东西**无法靠抄一遍数值达成**,必须在**真实开发中反复实测、打磨、优化**——在真实数据、真实内容长度、真实交互节奏下逐帧调。下文给出的具体数值(时长/位移/字距等)是**经验起点供参考**,开发时应大胆调整,只要守住本章的**原则**(因果动效、统一缓动、海拔一致、可中断、无障碍)与设计气质。**判定质感是否达标,看真实产品手感,不看是否等于某份静态稿。**

### 4.1 间距 · 布局 · 层次

**目标:让眼睛一眼看到主次,而不是平铺一片。**

- **竖向节奏分层**:同级内容块统一间距(卡片与卡片、网格与卡片之间 18px);区块级 16/24/32/48 分层,层级越高间距越大。禁止相邻区块用相近但不等的间距(视觉抖动)。
- **内容区呼吸**:内容区内边距 `30px 36px`,最大宽 1200px 居中(`content-max`);数据密集页可放宽,但保持左右对称 gutter。响应式断点增大 gutter。
- **标题层次(kicker 范式)**:小标签(kicker:11px 大写字距 +.09em 青电)+ 大标题(25px 紧字距 -.02em)+ 副述。三段式建立强层次,替代「光秃秃一行标题」。
- **卡片内边距**:`card-pad` 20px;卡头 `15px 20px`。信息密度高的表格卡可贴边。
- **留白即分组**:用留白把相关项聚成组、把无关项分开(白空间是结构,不是浪费)。
- **阴影分层**:四档阴影对应四种海拔 —— `xs`(卡片静置)/`base`(浮起)/`md`(下拉/悬浮卡)/`lg`(模态/抽屉/Toast)。**禁止随机阴影值**,海拔与阴影一一对应。

```
--shadow-xs: 0 1px 2px rgba(15,23,42,.05);
--shadow:    0 1px 2px rgba(15,23,42,.04), 0 2px 8px rgba(15,23,42,.05);
--shadow-md: 0 4px 14px rgba(15,23,42,.08), 0 2px 6px rgba(15,23,42,.04);
--shadow-lg: 0 12px 32px rgba(15,23,42,.12), 0 4px 12px rgba(15,23,42,.06);
```

### 4.2 交互微动效

**目标:每个动效都表达「因果」,不为动而动。** 设计智能红线:常用控件反馈 100–200ms、弹层 180–260ms、只动 `transform/opacity`;进入和退出都以立即响应的强 ease-out 为主,退出更快;单视图最多 1–2 个关键元素动,装饰性持续动画一律禁止。键盘触发的高频操作即时完成,不得等待动画。下表数值为**参考起点**,实际手感开发时实测微调(不同元素、不同内容长度的体感不同)。

| 场景 | 动效 | 时长/缓动 |
| --- | --- | --- |
| 页面进入 | 默认不动画;仅路由上下文确需说明空间关系时淡入 | 0 / 最长 .18s ease-out |
| 可点击卡片悬浮 | 只改变边框/实色表面;默认不位移、不加辉光 | .12–.16s ease |
| 按钮悬浮/按压 | 悬浮只调表面/边框;按压 `scale(.97–.985)` | .12–.16s ease-out |
| 侧栏项悬浮 | 只改变实色背景;不右移、不做渐变 | .12–.16s ease |
| Tab 切换 | 指示条在相邻项间平移;键盘切换即时 | 最长 .16s ease-out |
| 行悬浮 | 精细指针下只改变极浅实色背景 | .12s ease |
| 弹层/下拉 | 从触发源缩放淡入(transform-origin 对齐) | .18s ease-out |
| 模态入场 | 居中 `scale(.97)` + opacity;不模拟触发源 | .2–.24s ease-out |
| 加载/处理中 | 图标旋转(`loader` 自动 spin),>300ms 操作必给反馈 | 1s linear 无限(仅加载) |
| 进度/步骤 | 真实进度用 `scaleX`/transform 表达,离散状态即时切换 | .16–.22s ease-out |
| 列表入场 | 默认不做逐项错峰;仅低频首次引导允许短错峰 | 每项 30ms,总计不超过 .2s |

**统一签名缓动**:全局共用 `--ease`/`--ease-out`,所有动效同一节奏感。**可中断**:用户操作立即取消进行中的动画,绝不阻塞输入。

**定位浮层的 transform 所有权(强制)**:Select、Tooltip、Popover 等通过 Floating UI 定位的组件，定位外层独占库写入的 `transform`，视觉内层才允许做 opacity/scale 动效；同一 DOM 元素不得同时承担坐标 transform 与动画 transform。浮层在 `isPositioned` 前不可见且不可交互，最终 `placement` 决定 `transform-origin`。指针打开下拉可使用约 180ms 强 ease-out；键盘打开即时显示；reduced-motion 下只保留必要的透明度反馈。

**动效硬红线(Phase 1 起 CI 卡,Skill 参考与规范合并版)**

以下为四包与四端一律遵守的动效硬约束。参考来源:`emilkowalski/skills`(animations.dev) 的 `emil-design-eng`、`review-animations/STANDARDS.md`、`animation-vocabulary` 与本规范 §4.2 / §3.2 / §7 合并后的最终态。当 Skill 与本规范冲突时以本规范为准(见「明确不采纳」清单)。

- **只动 transform / opacity**;禁止过渡 `width / height / margin / padding / top / left / right / bottom`。真实进度用 `transform: scaleX(...)` 表达;离散状态即时切换。
- **禁 `transition: all`**(含 shorthand 中的 `all`);必须显式列出属性。
- **禁 `ease-in`**。进入/离开一律 `--ease-out`;屏内位移一律 `--ease-in-out`;悬浮/颜色允许 `ease`;常态运动 `linear`。
- **弹层起点禁 `scale(0)`**,一律 `scale(.95–.97)` + `opacity: 0` 起手。
- **`transform-origin` 对齐触发源**(Tooltip / Menu / Select / Popover),使用 Floating UI 计算的 `var(--transform-origin)`;**Modal 保持 `center`**。
- **定位外层独占 `transform`**(Floating UI 写入),动效(`opacity` / `scale`)放**内层**;同一 DOM 元素禁止同时承担坐标 transform 与动画 transform。
- **可打断优先**:Toast / Drawer / Menu / Popover / Tabs 指示条一律 CSS **transitions** + `@starting-style`(或 mount 标志位),禁止 `@keyframes` 在快速反复触发的组件里使用;`transitions` 可中断并从当前值继续,`keyframes` 会从零重启。
- **非对称时长**:进入慢于离开;进入 `--t-*-enter`,离开 `--t-*-exit`。
- **UI 时长 < 300ms**:控件按压 100–160ms、Tooltip 125–200ms、下拉/Select 150–250ms、Modal/Drawer 200–260ms。超过 300ms 必须在样式旁写 `/* animation-budget: <理由> */`,否则 CI 阻断。
- **按压反馈**:`Button / IconButton / Tab / MenuItem / SegmentedControl / 可点击 Card` 在 `:active` 应用 `transform: scale(var(--press-scale))`,不改变尺寸不引起重排。
- **Hover 门禁**:业务 `:hover` 一律用 `@media (hover: hover) and (pointer: fine)` 包裹;`packages/ui` 内的 hook 抽象点例外,通过 stylelint 白名单标识。
- **prefers-reduced-motion**:全站尊重;去掉位移/缩放/辉光/粒子,保留 opacity/颜色最小反馈;`SimulationWorkbench` 冻结阶段动画、保留当前值大字 KPI。
- **CSS 变量驱动子元素 transform 禁止**:变量写在父级会引发子树 style recalc 风暴;需要驱动多个子元素时,直接把 `transform` 写在各自子元素上。
- **Framer Motion / 三方动画库**:本轮不引入。若未来引入,只允许完整 `transform` 字符串,不允许 `x / y / scale` 简写(Motion 简写不硬件加速)。
- **装饰性持续动画禁止**(唯一例外:`loader` spinner):Idle / Float / Pulse / Ripple / hold-to-delete overlay / Text morph / Typewriter / Number ticker 一律不做。
- **Stagger 仅低频首屏引导**:每项 30–80ms,总计 ≤ 200ms;高频列表、表格、导航切换禁止 stagger。
- **Blur bridge < 20px**:仅允许在**同一元素跨状态过渡**时用 `filter: blur(2px)` 掩饰不完美 crossfade;`backdrop-filter` 仅在弹层与短生命周期表面使用,强度受 stylelint 白名单约束(严禁全站毛玻璃)。
- **Tooltip perceived performance**:同一 Tooltip Provider 下,首个显示后 ≤ 300ms 内相邻 Tooltip 使用 `data-instant` 跳过延迟与动画。
- **图表动效**:入场动画在 `prefers-reduced-motion` 下必须关闭;实时流式图 ≥ 1Hz 才允许动,且必须提供暂停/继续。

**明确不采纳**(与教学产品定位相斥,写死为反例):

1. Sonner 式"稍慢 + `ease`"元素级审美(除已允许的悬浮/颜色场景)。
2. `spring bounce > 0` 的 UI 弹性;除仿真潜在拖拽外一律不用 spring。
3. Ripple / hold-to-delete overlay。
4. 装饰性 Idle / Float / Pulse。
5. Text morph / Typewriter / Number ticker。
6. Framer Motion `x / y / scale` 简写。

### 4.3 组件精度

**目标:细节经得起放大看。** 以下是每个组件**至少要打磨的维度**(参考起点,非定值):开发时在真实数据下逐项实测,该加的细节(过渡、层次、对齐、状态反馈)做透,做到比下列基线更精致。**数值仅示意方向,以实际观感为准。**

- **按钮**:字重 600、字距 0;主按钮使用稳定实色表面和清晰边界,不使用辉光或悬浮位移;按压微缩且不改变布局尺寸。
- **表格**:表头保持自然大小写、字距 0 和弱层级;数字列等宽数字;精细指针行悬浮只改变实色背景;边框用 `border-subtle` 弱化横线。
- **表单**:输入框 hover 边框加深、focus 主色边框 + 3px 青电浅环;label 下距 7px;错误态红边 + 就近红字 + 图标。
- **向导步骤**:激活态用清晰边框、实色圆点和 `aria-current`;完成态绿;不使用发光环,连接线仅在真实状态变化时更新。
- **统计卡**:数字使用适合页面密度的字号和等宽数字,不采用压缩字距;只在指标确有独立边界时使用卡片。
- **徽章**:`3px 10px` + 字距 0;语义色对(底 100 / 字 700/800)。
- **Tab 下划线**:相邻项间短距离平移(见 4.2),键盘切换即时,非整条 border 闪烁。
- **滚动条**:细化(10px,thumb 圆角 + padding-box 内缩),精致不抢视觉。
- **侧栏激活**:主色浅底 + 左侧 3px 青电指示条;不叠加渐变、辉光或位移。

---

## 5. 组件库规范

> 四端共享组件落在 `frontend/packages/ui`。每个组件 = **样式令牌契约 + React 组件契约(props/状态/无障碍)**。组件的精度、状态完整度、交互手感以本规范原则为准,开发时打磨到位(见 §4)。

### 5.1 组件清单(基础层)

| 组件 | 关键变体 | 必备状态 | 无障碍要点 |
| --- | --- | --- | --- |
| **Button** | primary / secondary / outline / ghost / danger / on-dark;sm/lg/icon/block | hover/active/disabled/loading | loading 时 disabled + spinner;icon-only 带 aria-label |
| **IconButton / Tooltip** | ghost/outline/primary/danger/on-dark;top/right/bottom/left | hover/focus/active/disabled | 纯图标必须 `aria-label`;Tooltip 只解释控件,不承载必读信息 |
| **Card** | 静置 / hover(可点) / 带卡头 | hover(可点卡) | 可点卡整卡可聚焦 |
| **Badge / Dot** | amber/green/red/blue/purple/gray/teal | — | 状态点必配文字(色非唯一) |
| **StatusIndicator** | neutral/primary/success/warning/danger/info;soft/solid/text | pulse/loading | 状态色必须配图标或文字,不能只靠颜色 |
| **Table** | 可排序 / 行操作 / 空态 / 骨架 | hover/sort/loading/empty | `aria-sort`;表头 `th[scope]` |
| **Form**(input/select/textarea/checkbox/radio/switch) | 图标输入 / 错误态 | focus/hover/disabled/invalid/readonly | `label[for]`;`aria-invalid`;错误 `role=alert` |
| **SegmentedControl** | sm/md;图标+文字 / 纯文字 | active/hover/focus/disabled | `radiogroup`;每项是 radio,用于视图/模式切换 |
| **Tabs** | 滑入下划线 | active/hover | `role=tablist`,键盘左右切换 |
| **Steps** | 向导步骤 | active/done | 当前步 `aria-current` |
| **Progress** | 主色 / 绿 | — | 配文字百分比 |
| **Stat** | 统计卡 | 升/降 delta | 等宽数字;delta 配箭头图标 |
| **Modal / Drawer** | sm/lg/xl / 右抽屉 | 入场/关闭 | scrim 40–60%;Esc 关闭;焦点陷阱;关闭按钮 aria-label |
| **Popover / Menu** | 下拉菜单 / 危险项 | hover | 从触发源缩放;键盘可达 |
| **Toast** | success/error/info | 自动消失 3–5s | `aria-live=polite` 不抢焦点;带 trace_id |
| **Callout** | info/warn/danger/success | — | 左 3px 语义边 + 图标 |
| **Empty** | 空状态 | — | 图标 + 标题 + 引导 + 行动按钮 |
| **Skeleton** | line/title/block | shimmer | 占位预留空间防 CLS |
| **Pagination** | 页码 | active/disabled | 按钮 aria-label(上一页/下一页) |
| **Breadcrumb** | 面包屑 | — | 3+ 层级页必带;`nav[aria-label]` |
| **Autosave** | saving/saved | — | 配 §9 服务端草稿 |
| **PanelHeader** | eyebrow/title/description/meta/icon/actions | 响应式换行 | 统一卡片、图表、抽屉、工作台面板标题层级 |
| **DescriptionList** | 1/2/3 列;comfortable/compact | 响应式单列 | 键值详情用 `dl/dt/dd`;数字等宽 |
| **PageScaffold** | PageHeader / PageBody / PageSection / 可选 rail | 响应式重排 | 只提供日常页面结构原语,四端页面自行组合,不得用共享层强套同一业务布局 |
| **WorkbenchShell** | 左说明 / 中舞台 / 右状态 / 控制区 | `<lg` 舞台优先堆叠 | 只提供沉浸式工具壳和槽位,IDE/仿真/答题按自身语义组织内容 |

### 5.2 React 组件契约(强制)

- **零裸 hex**:组件只用令牌类名 / CSS 变量;新颜色先入令牌再用。
- **状态齐全**:每个交互组件必须实现 hover/focus-visible/active/disabled,异步组件加 loading,列表类加 empty/skeleton/error 四态。**缺态即不完整**(对应 CLAUDE.md「禁止占位/假实现」)。
- **受控优先**:表单组件受控,校验在 blur(非每次击键);提交 loading→成功/失败反馈。
- **props 命名**:语义化(`variant`/`size`/`loading`/`disabled`/`invalid`),不用样式名当 prop。
- **图标**:统一 Lucide,经图标组件注入,尺寸走令牌(`icon-sm/md/lg`),描边一致;禁 emoji、禁裸 SVG 散落。
- **可组合**:复合组件(Modal=Head/Body/Foot,Table=列定义驱动)用组合而非巨型 props。
- **文案外置**:可见文案集中管理(i18n 友好),不硬编码在组件深处;面向用户(FE-4)。

### 5.3 组件化原则(该组件化的就组件化)

判定一段 UI 是否该提为组件:

1. **复用 ≥ 2 处** 且 结构稳定 → 提 `packages/ui` 基础组件。
2. **业务语义强、跨端复用**(如「成绩单元格」「沙箱状态灯」「检查点判分卡」)→ 提**业务组件**层(`packages/ui/biz` 或各端 `features/*/components`),引用基础组件。
3. **仅单页用、结构易变** → 留在页面内,不过早抽象(对应工程总则「三行相似优于过早抽象」)。

> 反面:不把「按钮 + 一段文案」这种一次性组合硬抽成组件;也不把出现 5 处的「沙箱状态灯」复制 5 份。**复用即组件,一次即内联。**

---

## 6. 信息架构与导航

### 6.1 四端结构

四个角色端**同一个浏览器入口、同一套外壳、同一套组件、不同角色路径和导航配置**。私有化部署无 platform-admin 端。

| 端 | 登录落点(侧栏首项) | 导航分组(摘要) |
| --- | --- | --- |
| **学生 student** | `student/courses` 我的课程 | 学习(课程/实验/竞赛/仿真实验室)· 成绩(成绩/学业预警)· 账户 |
| **教师 teacher** | `teacher/courses` 课程管理 | 教学(课程/批改)· 实践(实验/竞赛/实时监控)· 资源(题库/组卷/仿真场景/共享库)· 成绩报送 · 组织 · 账户 |
| **学校管理员 school-admin** | `school-admin/accounts` 账号管理 | 用户与组织 · 概览 · 成绩(审核/申诉/预警/配置)· 系统 |
| **平台管理员 platform-admin** | `platform-admin/tenants` 租户 | 租户 · 运营 · 系统(SaaS 专属) |

### 6.2 导航规范

- **登录直达功能页**(FE-5):无独立工作台落地页;登录后跳该角色侧栏第一项。
- **侧栏 = 二级导航**:分组标题(11px 大写弱色)+ 菜单项(图标 + 文字 + 可选角标计数)。**激活态**:主色浅底 + 左 3px 青电指示条 + 字重升。角标计数用于待办(批改 5、审核 4、申诉 2、预警 1)。
- **图标 + 文字并存**:导航项绝不纯图标(可发现性)。当前位置必高亮。
- **面包屑**:3+ 层级深页必带(`nav[aria-label=breadcrumb]`),辅助定位。
- **导航一致性**:四端导航位置/行为一致,不随页面类型变化;危险操作(登出等)与普通导航项视觉/空间分离。
- **顶栏(深色)**:Logo + **任务中心** + **通知铃铛** + 头像菜单。当前阶段不做跨课程、用户、竞赛等资源的全局搜索；搜索与筛选放在对应资源页，并由真实列表接口驱动。帮助入口仅在存在真实帮助内容或支持渠道时展示。

### 6.3 通知:顶栏铃铛(已从侧栏移出)

通知**统一在顶栏铃铛下拉**,不再占侧栏菜单项:

- 铃铛带未读角标(琥珀 `amber-500`,深色顶栏上点睛);
- 下拉含:最近通知列表 + 系统公告 + 「查看全部」入口;
- 四端统一;通知数据经 M10(通知模块),前端不自建通知存储(对应架构铁律「通知统一经 notify」)。

### 6.4 窗口自适应(响应式,FE-9 必做)

> **强制项**:所有页面与组件随窗口流式自适应,而非固定像素布局。侧栏折叠/抽屉、沉浸式工作台窄屏堆叠、表格重排等必须按真实功能补齐,不得以静态示意代替生产体验。

**断点系统(统一,禁止随手写魔法值)**

| 令牌 | 宽度 | 典型设备 | 主要行为 |
| --- | --- | --- | --- |
| `--bp-sm` | 640 | 大手机 | 单列;侧栏抽屉化;表格转卡片/横滚 |
| `--bp-md` | 768 | 平板竖 | 双列;侧栏可收起为图标条 |
| `--bp-lg` | 1024 | 平板横/小笔记本 | 侧栏常驻(可折叠 248↔64);多列恢复 |
| `--bp-xl` | 1280 | 桌面 | 内容区 1200 居中,完整布局 |
| `--bp-2xl` | 1536 | 大屏 | 内容不拉满长文行宽,留白增大 |

**自适应行为约定**

- **流式优先**:布局用 `flex`/`grid`(`minmax`/`auto-fit`/`fr`)、`clamp()` 弹性间距与字号、`min()/max()`,而非写死宽度;容器用 `max-width` + 居中,内部弹性。
- **侧栏**:`≥lg` 常驻且可折叠(248↔64,令牌 `--sidebar-w`/`--sidebar-w-collapsed`);`<lg` 收为**抽屉**(汉堡触发,遮罩 + Esc 关闭,焦点陷阱)。
- **顶栏**:窄屏把次要项收入「更多」溢出菜单；Logo、任务、铃铛和头像始终可达。
- **内容网格**:统计/卡片网格按断点 4→2→1;`auto-fit minmax()` 优先,让卡片按可用宽自动换行。
- **表格(数据密集)**:`<md` 不靠纯横向滚动硬塞——优先**列优先级**(隐藏次要列)或**转卡片式**(每行变一张键值卡);确需横滚时横滚区独立、不带动整页。
- **沉浸式工作台**:三栏在 `<lg` **堆叠或转 Tab/抽屉**(左说明、右状态收为可展开面板,中舞台优先);画布(仿真节点图/拓扑)用 `viewBox` 等比缩放或容器查询自适应,**不溢出、不裁切关键信息**;移动端可降级为「只读回放 + 提示用大屏操作」,但不得报错或空白。
- **弹层**:Modal `width:100%` + `max-width` + `max-height:88vh`;Drawer `max-width:92vw`;窄屏底部弹出(bottom-sheet)更顺手。
- **媒体/图表**:声明宽高或 `aspect-ratio` 防布局抖动(CLS<0.1);图表用容器宽度自适应,小屏简化(减刻度/横向条)。
- **触控**:触屏视口下交互目标 ≥44×44,间距 ≥8px;不靠 hover 传达关键信息(hover 仅增强)。

**红线**

- 禁止:任何视口出现**非预期横向滚动**(表格/代码块的独立横滚区除外)。
- 禁止:写死容器像素宽导致窗口缩放后**遮挡、错位、截断**。
- 禁止:禁用缩放(`user-scalable=no`/`maximum-scale=1`)。
- 禁止:固定定位元素(顶栏/工作台条/底部 CTA)未给内容预留安全内距,压住内容。

**验收**:每页在 **375 / 768 / 1024 / 1440** 及**任意拖拽缩放**下走查;横竖屏均可用;`reduced-motion` 与大字模式下不破版。

### 6.5 任务驱动的页面组合范式

> 四端共享结构原语和状态契约,不共享一张固定业务模板。页面先按用户任务选择布局,再组合 `PageHeader`、`ContextBar`、`PageBody`、`PageSection` 和可选 rail;不得把所有页面统一渲染为「标题 + 统计卡 + 右侧动作卡 + 表格」。

**页面结构原语**

1. **PageHeader**:面包屑、`h1`、必要的用户向说明、状态与主动作。图标仅在能帮助识别业务对象时使用,不作为每页装饰。
2. **ContextBar**:筛选、Tab、批量操作、范围和已选摘要;没有上下文操作时不渲染空工具栏。
3. **PageBody**:按任务选择数据表、主从视图、详情、表单、审批队列或可视化,不预设「列表为主、表单为辅」。
4. **PageSection**:无框分区和稳定垂直节奏;只有重复对象、模态、独立工具或确需边界的内容使用 Card/Panel。
5. **可选 rail**:只承载与当前对象直接相关的摘要、证据或操作;不得把所有按钮拆成带序号的动作卡。
6. **状态**:加载用匹配最终结构的骨架;空态说明下一步;错误态可恢复并保留 trace_id;无权限态与空数据态分开。

**任务布局选择**

- 浏览/筛选资源:工具栏 + 数据表/列表,桌面保持扫描密度,移动端按列优先级重排。
- 编辑/配置:受控宽度表单 + 就近校验 + 稳定动作区;长流程使用服务端草稿。
- 审批/批改:队列 + 选中对象证据 + 决策区,桌面主从、移动分层导航。
- 监控/看板:真实 KPI、异常和待办优先;没有真实序列时不绘制趋势图。
- 组织/层级:树或层级导航 + 当前节点详情;编辑与导入进入 Drawer 或独立流程。

**角色与分组表达**

- 角色端保持同一外壳、组件语法和反馈模型,差异来自任务、信息密度和权限,不是四端各做一套皮肤。
- 青电是主交互色,琥珀只用于真实提醒/能量/对抗语义;页面不靠大面积角色色、渐变或辉光区分身份。
- 高频资源页默认无入场动画;动效只用于因果反馈、弹层空间关系和真实进度,遵守 §4.2。

### 6.6 多步骤流程持久化(FE-7)

适用:实验编排向导、作业草稿、竞赛出题/参战/报名、内容创作草稿、导入预览、入驻申请等。

- **服务端为权威**:每步/定时(参考 M6 作业草稿 60s 自动保存)存服务端;前端 localStorage 仅即时兜底。
- **可恢复**:刷新/关闭重开/换设备不丢;进入向导先拉服务端草稿。
- **自动保存指示**:`autosave` 组件显示 saving(琥珀)→ saved(绿),给用户确定感。

### 6.7 结构化数据呈现(FE-11)

- **存储格式不等于界面**:后端字段为 JSONB、`Record<string, unknown>` 或动态对象时,前端必须先依据业务契约映射成可读模型;不得以“配置灵活”为由展示 JSON 文本框或原始对象转储。
- **编辑按领域建模**:固定契约使用 Input、Select、Switch、规则行、步骤列表和可增删子表单;字段标签、单位、范围、依赖关系与错误提示必须面向用户。可扩展字段由后端返回类型声明或字段描述后再渲染,不得接受任意键值。
- **只读按阅读任务组织**:对象摘要用 DescriptionList,同构集合用 Table/List,过程数据用 Steps/Timeline,状态与数值用 Badge/Stat,源码与命令输出才使用 Code/Pre。缺失值显示“未设置/暂无”,不得显示 `null`、`undefined`、`[object Object]`。
- **协议数据分层**:WebSocket 发送、API payload 组装和开发工具内部可使用序列化,但序列化文本不可直接成为用户界面。沙箱链操作结果必须提取交易哈希、区块、状态、返回值等已知字段;未知字段只进受控诊断日志。
- **严格单一契约**:不得为旧字段名、旧枚举或旧对象形态保留前端兼容分支。存量数据通过版本化迁移统一,种子、导入器、后端校验、API 类型和全部页面同步更新;不符合当前契约的数据由边界明确拒绝并给出用户向提示。
- **敏感与内部字段**:答案、判题配置、flag、密钥、端点凭据和内部错误详情继续按权限裁剪;trace_id、哈希、对象引用等仅在确有排障或操作价值时以次要信息呈现,不可取代姓名、标题和状态等主要语义。

---

## 7. 沉浸式工作台范式

> 做实验/仿真/答题/对抗回放时进**深色全屏**沉浸态(`z-immersive`),退出回导航态。四个工作台是平台的「创新点」与教学引擎门面 —— 必须是**真教学工具,不是空动画**。

### 7.1 通用范式

- **三栏骨架**:左(说明/上下文)· 中(主舞台:编辑器/画布/题面)· 右(状态/结果/操作)。
- **深色面板令牌**:`dark-bg/surface/elevated`、`terminal-bg`、`editor-bg`;点睛用 `on-dark-accent`(青电 400)。
- **顶部工作台条**:退出、标题、进度、主操作(运行/提交/下一步);**单一主 CTA**。
- **WebSocket 实时进度**:长操作(判题/挖矿/对局)经 WS 推进度,前端给骨架/进度而非冻结。
- **共享与业务边界**:`packages/ui` 的 `WorkbenchShell` 只负责深色壳、槽位、响应式和无障碍;`packages/ide` 的 `IdeWorkbench` 只负责文件/编辑器/终端/工具状态布局;`packages/sim-sdk` 的 `SimulationWorkbench` 负责仿真叙事、舞台、控制和渲染器。四端业务页可以使用这些原语,但不得照搬共享层生成的统一页面布局。文件、终端、命令工具和链操作必须由后端实例能力声明驱动,缺失能力时不请求对应接口、不建立 WS、不显示操作。
- **仿真舞台对齐**:节点图、链、树、矩阵、泳道、趋势图都必须由仿真包真实 `TeachingFrame` 数据驱动;复杂拓扑不得硬塞所有边标签导致重叠,趋势图必须提供数据表替代,选择节点/消息/单元格要有键盘可达和可读标签。
- **仿真主次结构**:`TeachingFrame.layout.primary` 决定主舞台,`evidence/timeline/metrics` 放右侧可折叠辅助区,底部只放时间与播放控制。页面不得从 pattern 顺序或旧版区域字段猜布局。
- **过程化可视化**:`TeachingFrame.focus` 与元素生命周期负责当前焦点、历史淡化和离场动画。仿真页面必须把算法过程作为主体,说明和指标只服务于理解过程,不得让说明区挤占主可视化。

### 7.2 四个工作台

#### A. 代码实验 IDE(学生·实验)
- 编辑器 + 终端 + 检查点判分 + 链上操作 + WS 进度。
- **创新点①「区块孵化器」**:把「沙箱在干活」做成 VM 活体状态机,替代转圈 spinner —— 见 §9 整改项(状态机做全 + 中文 + 失败可点开)。

#### B. PBFT 共识仿真(学生·仿真,按 M4 文档)
教学引擎形态,**能逐阶段看清效果**:
- **左栏**:讲「这一步做了什么 + 为什么重要 + 法定人数 2f+1 判定」;
- **中间**:`layout.primary` 指向当前最关键模式,节点图按阶段展示不同消息飞线(如 Prepare 全网状),历史消息按生命周期淡化;
- **右侧**:只放 `evidence/timeline/metrics` 指向的辅助内容,如时序泳道、投票矩阵、法定人数结果,且可折叠;
- **控制**:单步 / 播放;支持注入拜占庭节点 + 设问检查点。
- 可扩展更多仿真(PoW 挖矿 / 默克尔树 / 分叉),套同一范式。

#### C. 对抗赛回放(学生·竞赛复盘)
- 攻防拓扑(守方绿 / 攻方红)+ 链上日志流(随时间轴构建)+ **时间轴回溯条**。
- **创新点②「时空回溯器」**:倒带 VM 执行树 —— 见 §9,回溯须绑真实区块高度快照/执行 trace,不可是假动画。

#### D. 解题赛答题(学生·竞赛)
- 题列表 + 题面 + 起沙箱/作答 + 提交判定 + 防作弊。
- **答案黑盒**(安全铁律):题面与全量(答案/判题配置/flag)接口分离,学生端不可见。

### 7.3 工作台无障碍

- 深色面板文字对比照 §3.1(主 ≥4.5:1 / 次 ≥3:1)。
- 仿真/回放动画**必须**支持 reduced-motion(冻结动画 + 保留当前态文字)。
- 单步/暂停控制 = 无障碍刚需(让动画可被人为掌控,不强制自动播放)。

---

## 8. 数据可视化规范

> 看板、统计、监控、仿真、对抗回放都用图表。图表选型经设计智能(ui-ux-pro-max chart 域)校验,落表如下。**底线:色非唯一、有图例/提示、空/错/载有态、无障碍降级。**

### 8.1 图表选型表

| 场景 | 图表 | 关键约束 |
| --- | --- | --- |
| 成绩/活跃**趋势** | 折线图(Line) | <4 点用统计卡;>6 系列太杂;系列用**线型**(实/虚/点)区分非颜色;Recharts/Chart.js |
| 维度**对比** | 柱状图(Bar) | >5 类别不用饼图,改柱 |
| 占比 | 饼/环(≤5 类) | >5 类改柱状 |
| 实时监控(挖矿/对局**流式**) | 流式面积/Ticker | ≥1Hz 才用;**必带暂停/继续**;当前值大字 KPI;Canvas;reduced-motion 冻结 |
| 异常/告警 | 带高亮折线 | 异常点用**形状标记**(非颜色)+ 文字标注 |
| **投票矩阵 / 活动密度**(PBFT 共识、教务监控) | 热力/网格矩阵(Heatmap/Grid) | 冷→暖色阶 + **数值标签** + 图例刻度;色盲纹理补丁;hover 出精确值 |
| 攻防/节点关系 | 网络拓扑图 | 节点/边状态配图标+文字;守绿攻红但不靠色单一 |

### 8.2 图表无障碍与降级(强制)

- **色盲友好**:不用红/绿单一对;补线型/纹理/形状;divergent 色阶配数值图例。
- **数据表替代**:图表配可切换数据表(读屏不友好的纯图必给表),`aria` 摘要描述关键洞察。
- **交互**:hover/tap 出精确值;图例可点切换系列;键盘可达(非仅 hover)。
- **态齐全**:空数据(「暂无数据」+ 引导,非空白轴)、加载(骨架非空轴框)、错误(报错 + 重试)。
- **数字格式**:locale 感知(数字/日期/货币);轴标签不挤、小屏自动跳刻度;时间序列标明粒度(日/周/月)可切换。
- **reduced-motion**:入场动画可关,数据立即可读。

---

## 9. 前端待优化与完善清单

> 这是已实现功能、真实页面和设计智能复核暴露的**真实缺口**,逐项整改,不留 TODO 占位(CLAUDE.md §4)。每项标注**优先级**(P0 阻断 / P1 重要 / P2 增量)。

### 9.1 四个创新点的整改(P1)

| # | 创意 | 现状缺陷 | 整改要求 |
| --- | --- | --- | --- |
| ① | **区块孵化器**(VM 活体状态) | 只有绿/琥珀两态 + 一句英文 `VM_Status`,信息密度太低,英文术语违反 FE-4 | 状态机**做全**:就绪/编译/挖矿/封块/失败;中文 + Lucide 图标;**失败态可点开看原因**;状态色配文字非颜色单一 |
| ② | **时空回溯器**(倒带 VM 执行树) | 当前只是 4 点进度条切日志文本,「回溯」是假的 | **生产级要害**:回溯绑**真实区块高度快照 / 执行 trace**;与后端**对局录制格式**一起设计;非动画 |
| ③ | **教务雷达 + 一键阻断** | 雷达图学生 x/y 是写死随机散点,不承载语义,属设计智能明确反对的「装饰性持续动画」 | 雷达图**砍掉/重做**为**状态色网格矩阵 + 排序**(卡住/报错的排前面);「一键集中阻断全班」实用,**保留** |
| ④ | **代码双向追溯**(节点作恶↔断言行) | 教学价值最高,但当前单向 + 硬编码字符串 | 做成**真正双向**(点代码行也高亮对应节点/消息);接**真实 VM 断言**而非硬编码;强烈保留强化 |

### 9.2 无障碍与质量修复(P0/P1)

| 优先级 | 项 | 要求 |
| --- | --- | --- |
| P0 | WCAG 对比度 | 琥珀 CTA/激活态/深底次要文字全部达标(§3.1);新增色入令牌前查表 |
| P0 | 焦点态 | 全交互元素 `:focus-visible` 焦点环,绝不裸 `outline:none` |
| P1 | reduced-motion | 全站(尤其仿真/回放)尊重系统「减少动态」 |
| P1 | 色非唯一 | 所有状态/图表补图标或文字 |
| P1 | 装饰性持续动画 | 全面排查并移除(仅加载指示可无限动) |
| P1 | 等宽数字 | 统计/成绩/计时/表格数字列统一 tabular-nums |

### 9.3 窗口自适应整改(P0/P1)

> 以下为实查暴露的真实缺口,实际开发**必须补齐**(规范见 §6.4)。

| 优先级 | 项 | 现状 | 整改要求 |
| --- | --- | --- | --- |
| P0 | 侧栏折叠/抽屉 | 写死 248px,折叠令牌定义了却没接 | `≥lg` 可折叠 248↔64;`<lg` 抽屉化(汉堡 + 遮罩 + Esc) |
| P0 | 沉浸式工作台自适应 | `position:fixed` 多栏写死宽,窄屏不堆叠、画布会溢出 | 三栏 `<lg` 堆叠/转 Tab;画布 `viewBox` 等比缩放;移动端可降级只读 |
| P1 | 表格响应式 | 仅 `overflow-x:auto` 兜底 | `<md` 列优先级隐藏次要列或转卡片式;横滚区独立 |
| P1 | 顶栏窄屏 | 无收起逻辑 | 搜索收图标、次要项进溢出菜单;核心项始终可达 |
| P1 | 断点系统化 | 散落 1100/720/860 魔法值 | 统一为 `--bp-sm/md/lg/xl/2xl`(640/768/1024/1280/1536) |

### 9.4 组件化与体系完善(P1/P2)

| 优先级 | 项 | 要求 |
| --- | --- | --- |
| P1 | 组件库正式化 | `packages/ui` React 组件按 §5.1 清单逐个补齐四态 |
| P1 | 业务组件抽取 | 沙箱状态灯、检查点判分卡、成绩单元格、投票矩阵、攻防拓扑等跨端复用项提为业务组件 |
| P2 | 暗色模式 | 在双模态基础上补完整暗色主题(令牌已分深色组,需补浅区→深区映射并独立验对比度) |
| P2 | 图表库选型落地 | 按 §8.1 统一选 Recharts/Chart.js + 必要 Canvas/D3;封装为带无障碍降级的图表组件 |
| P2 | 骨架/空/错三态覆盖 | 所有列表/看板/图表页补齐加载骨架、空状态、错误重试 |

### 9.5 工程化完善(P1/P2)

| 优先级 | 项 | 要求 |
| --- | --- | --- |
| P1 | 令牌注入 | tokens 作为唯一来源注入 React(CSS 变量 / TS 常量双出);组件零裸 hex 用 lint 卡 |
| P1 | 文案外置 | 可见文案集中、面向用户、可 i18n;错误按 §8 分层(只展示 message+trace_id) |
| P1 | 自托管字体 | 生产自托管 Inter + JetBrains Mono(`font-display: swap`),不依赖 CDN |
| P2 | 按需加载 | 沉浸式工作台 / 图表 / 富文本编辑器路由级懒加载,降首屏 |
| P2 | 长列表虚拟化 | 50+ 行列表(用户/成绩/日志)虚拟滚动 |

---

## 10. React 工程落地

> 落点遵循 `docs/总-工程目录设计.md` 的 `frontend/` monorepo 结构。本节定与设计相关的约定。

### 10.1 分层

```
  frontend/
    apps/
      web/             # 单体前端应用(Single SPA),采用 FSD 架构,懒加载四端视图
    packages/
      ui/              # 设计系统:tokens + 基础组件 + 业务组件(四端共享唯一来源)
        tokens/        # 原始→语义令牌(CSS 变量 + TS 常量双出)
        components/    # Button/Card/Table/... (§5.1)
          PageScaffold/ # 日常页面结构原语:头部、主体、侧栏、区块,不绑定四端业务布局
          WorkbenchShell/ # 沉浸式工具结构原语:深色壳、三栏、控制区
        biz/           # 沙箱状态灯/投票矩阵/攻防拓扑... 业务组件
        charts/        # 数据可视化封装:图形 + 数据表降级 + reduced-motion 契约(§8)
      ide/             # IDE 工作台能力:Monaco/xterm 生命周期 + IdeWorkbench 布局壳;Monaco 核心、语言、Worker 分级按需加载
      sim-sdk/         # M4 仿真 SDK:Worker 运行时 + SimulationWorkbench + 封闭模式渲染器
```

### 10.2 应用层重建约定

- 旧应用入口、旧页面工厂和旧体验层样式已清理;新 UI/UX 方案采用 **单体前端应用 (Single SPA) 配合 FSD 架构**。
- `apps/web/` 内部严格按照 `app/`, `routes/`(路由清单、按需懒加载入口、权限装配), `layouts/`, `features/`(强映射后端 11 大模块,页面实现放各模块 `pages/` 子目录) 划分。
- `apps/web/` 只放应用入口、路由、应用壳、页面和应用级组合逻辑;不得沉淀通用 SDK、设计系统、仿真引擎或 IDE 工作台能力。
- `apps/web/src/utils/labels/` 只放应用层用户文案映射,按后端模块拆分文件;不得恢复跨多个业务域的 `learning`、`platform` 或无边界 `shared` 标签大文件。
- 维持单浏览器入口承载登录前公共页和四角色路径,本地浏览器始终访问一个前端端口。
- 登录页位于同一入口内;手机号登录后不展示角色选择,由后端返回的账号角色决定跳转路径。
- 角色路径固定为 `/student/`、`/teacher/`、`/school-admin/`、`/platform-admin/`,进入后由共享应用壳渲染该角色的第一个功能页。
- 禁止恢复四套独立 Vite app 作为默认启动形态;如需临时调试角色页面,也必须通过新应用的同源路径完成。
- 禁止为单入口应用内部能力恢复 `packages/shared`、`packages/auth`、`packages/ws-client` 这类薄 workspace 包。应用壳、认证公共页、配置、存储、错误、实时连接等如果只服务单个应用,放在该应用内并按职责拆文件。
- `packages/` 只放稳定能力包: `api-client`、`ui`、`sim-sdk`、`ide`。普通业务页面、页面动作、表格列、角色路由定义不得放入 `packages/`。
- `packages/api-client` 只做后端公开 HTTP/WS 契约 SDK:模块请求放 `src/modules/*`,DTO 类型放 `src/types/*` 并按后端公开模块拆分,通用分页等跨模块结构放 `src/types/common.ts`。页面不得绕过 SDK 散落硬编码 API 路径;SDK 不得封装后端 `/internal` 服务间接口。
- 后端状态、角色、任务状态等契约常量统一放 `packages/api-client/src/constants/*`,页面和应用壳只引用常量,不得手写魔法数字/魔法字符串。API SDK 变更必须对照后端公开 `dto.go`、`enum.go` 和 OpenAPI 路径逐项核对。

### 10.3 约定

- **组件零裸 hex**:ESLint 规则禁裸颜色,只许令牌;CI 卡。
- **命名**:组件 PascalCase;目录 kebab-case(`school-admin`);hooks `useXxx`;令牌 `--color-*` / `--space-*`。
- **状态管理**:服务端状态用查询库缓存;多步骤向导中间态走服务端(FE-7),前端只缓存即时态。
- **错误边界**:每端套错误边界,兜底友好页(含 trace_id),不白屏不泄技术细节。
- **图标**:Lucide 经统一图标组件;尺寸令牌化;tree-shake 按需引入。
- **可访问性测试**:关键页跑 axe 类自动检查 + 键盘走查纳入验收。

---

## 11. 明确不做(本阶段)

| 不做 | 原因 |
| --- | --- |
| 完整独立暗色模式 | 当前双模态(深框架+白内容+沉浸深色)已满足;纯暗色列为 P2 增量(§9.3),非本阶段 |
| 多主题/换肤系统 | 仅青电×琥珀一套品牌;令牌化已为未来换肤留口,但不提前建换肤框架 |
| 移动端原生 App | 平台为 React Web 四端;移动以响应式 Web 覆盖,不做 RN/原生 |
| 国际化多语言落地 | 文案外置为 i18n 留口,但本阶段只做中文;不提前接翻译流程 |
| emoji / 第三方花哨动效库 | 违反 FE-3 与动效红线;图标统一 Lucide,动效用令牌化 CSS |

---

## 附录 A:组件速查

按钮 · 卡片 · 徽章/状态点 · 表格 · 表单(输入/选择/多选/开关/图标输入)· Tab · 进度 · 统计卡 · 模态/抽屉 · 下拉/菜单 · Toast · 内联提示条(callout)· 向导步骤 · 自动保存指示 · 分页 · 描述列表 · 骨架屏 · 空状态 · 面包屑 · 跳过链接。
React 实现落 `packages/ui/components`;样式以令牌和组件 CSS 为准。

## 附录 B:前端文案规范(摘 CLAUDE.md §8/§10)

- 面向用户的自然语言,不暴露开发术语(`null`/`timeout`/字段名/`error_code`/技术栈名词)。
- 反例 → 正例:「请求 400 参数校验失败」→「手机号格式不正确,请检查后重试」;「sandbox pod pending」→「实验环境正在准备,请稍候」。
- 错误提示可附「如需帮助,请提供编号 `trace_id`」,定位能力留运维。

## 附录 C:交付前检查清单

**视觉**:□ 无 emoji,Lucide 统一 □ 零裸 hex,全令牌 □ 阴影=海拔对应 □ 字号/间距走令牌
**交互**:□ hover/focus/active/disabled 齐全 □ 动效 150–300ms 且 transform/opacity □ 异步有 loading □ 单视图≤2 动效
**无障碍**:□ 对比度达标 □ 键盘可达+焦点可见 □ reduced-motion 无损 □ 色非唯一 □ 图标按钮 aria-label
**自适应**:□ 375/768/1024/1440 及任意缩放正常 □ 无非预期横向滚动 □ 侧栏窄屏抽屉化 □ 沉浸工作台窄屏堆叠不溢出 □ 未禁缩放
**结构**:□ 登录直达功能页 □ 通知在顶栏铃铛 □ 3+层级带面包屑 □ 多步骤服务端持久化
**反馈**:□ 空/错/载三态全 □ 错误就近+可恢复+trace_id □ Toast 不抢焦点 □ 文案面向用户

---

### 附录 C 补充:Phase 1 起补充门禁(强制)

以下门禁与《前端UIUX全面重构审计-2026-07-22.md》§9.1 和《前端重构执行台账-2026-07-22.md》互为强绑定,Phase 1 起写死为 CI + 交付前必过项。

- □ **逐文件全覆盖**:104 页面位置、96 懒加载入口、`apps/web + packages/ui + packages/ide + packages/sim-sdk` 的**全量 TSX 与 CSS** 均经过五态 / 无障碍 / 响应式 / 动效 / JSONB 呈现五轴复核;不允许"代表页通过 = 全部通过"。
- □ **只认新证据**:`.tmp/e2e-2026-07-14/**` 与 `.tmp/e2e-regression-2026-07-15/**` 不再作为视觉正确性依据;验收证据统一存放 `.tmp/e2e-<日期>/`,Playwright 视口固定 `375 / 768 / 1024 / 1440`。
- □ **后端契约缺失只落文档**:功能所需的后端能力真实缺失时,写入 `docs/前端后端功能对齐清单.md` 的"待补齐"段落(影响页面、影响功能、后端为何不足、是否阻塞),前端**删除该 UI 承诺**,不做占位 / 假态 / 假接口 / 假进度 / 假图表 / 假 AI 推荐。
- □ **动效令牌校验**:`packages/ui/src/tokens/` 与 §2.4「动效令牌落点」一致;`--ease-in` 引用为 0;`transition: all` 出现次数为 0;`transition-duration > 300ms` 且无 `/* animation-budget: <理由> */` 注释为 0。
- □ **断点校验**:`@media` 中出现的非白名单数字(`640/768/1024/1280/1536` 之外)为 0;`@custom-media` 定义与 §2.4 一致;历史断点 `720/860/1100/1440` 引用为 0。
- □ **hover 门禁**:业务 `:hover` 100% 位于 `@media (hover: hover) and (pointer: fine)` 内;`packages/ui` 白名单例外由 stylelint 显式声明。
- □ **JSONB 呈现原语**:`apps/web/src/features/**` 内不出现原始 JSON 编辑器、`<pre>{JSON.stringify(...)}</pre>` 转储、自由键值编辑器;终端用户可见结构化数据一律走 `DescriptionList / KeyValueTable / Timeline / Steps / CodeBlock` 原语,与 FE-11、§6.7 对齐。
- □ **不做旧逻辑/旧数据兼容或兜底(强制,与 CLAUDE.md §4 "统一不兼容 · 不留 TODO 占位" 硬对齐)**:重构以规范和后端**当前公开契约**为唯一真相源,不写入以下任何一类兼容/兜底代码:
  1. 旧字段名 / 旧枚举 / 旧对象形态的兼容分支、别名映射、`toLegacy* / fromLegacy*`、`v1/v2` 双跑逻辑;
  2. 面向已删除路由、已废弃组件、旧原型截图形态的样式复刻或视觉复原;
  3. 面向"接口可能返回 null / undefined / 缺字段"的静默兜底(必须以边界校验 + 用户向错误提示 + `trace_id` 处理);
  4. 面向"后端可能没实现"的假数据、假进度、假图表、假 AI 推荐、假状态机;
  5. `try { ... } catch { /* 忽略 */ }` 式吞错;错误一律显式处理并转用户向文案(见 CLAUDE.md §8)。
  该重构就完整重构,该清理就清理干净;旧代码 / 旧样式 / 旧类型 / 旧标签映射一次性删除,不在仓库中留下"以后再删"的注释。
