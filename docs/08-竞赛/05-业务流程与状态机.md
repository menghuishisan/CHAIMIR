# M8 竞赛 — 业务流程与状态机

> Mermaid 描述竞赛生命周期、解题流程、对抗撮合、回放、漏洞转化。
> 最后更新:2026-05-29

---

## 1. 竞赛状态机

```mermaid
stateDiagram-v2
    [*] --> 草稿: 创建
    草稿 --> 报名中: 发布
    报名中 --> 进行中: 到开始时间
    进行中 --> 封榜: 进入最后N分钟
    封榜 --> 已结束: 到结束时间
    已结束 --> 已归档: 级联回收环境 + 生成成绩快照
    已归档 --> [*]
```

- 封榜期:排行榜冻结显示,提交仍判定但排名不实时公开。
- **已结束 → 已归档**(A5 修复):竞赛结束时执行收尾——① 调 `POST /sandboxes/recycle(source_ref=contest:{id})` 级联回收所有解题赛环境与对抗对局沙箱(与 M7 对齐"三重回收"承诺);② 生成竞赛成绩快照存档(B11 个人战绩档案)。

---

## 2. 报名组队

```mermaid
flowchart TD
    A[学生报名] --> B{个人/团队}
    B -->|个人| C[创建单人队]
    B -->|团队| D[创建队伍+邀请码]
    D --> E[队员加入(可跨校,需平台用户)]
    C --> F[开赛自动锁定]
    E --> F
```

---

## 3. 解题赛流程

```mermaid
sequenceDiagram
    participant S as 选手
    participant M8 as 竞赛
    participant M2 as 沙箱
    participant M5 as 题库
    participant M3 as 评测
    S->>M8: 打开题目
    M8->>M5: 取题面(过滤答案)
    alt 实操题
        S->>M8: 起环境
        M8->>M2: 起沙箱
    end
    S->>M8: 提交(flag/代码/理论答案)
    M8->>M3: 判题(M5配置+沙箱状态)
    M3-->>M8: 结果
    M8->>M8: 更新 ladder_rank
    M8-->>S: 判定结果 + 排行榜推送
```

---

## 4. 对抗赛撮合流程

```mermaid
flowchart TD
    A[选手提交参战物] --> B[进对战池, is_active]
    B --> C[撮合调度器]
    C --> D{撮合模式}
    D -->|循环赛| E[与池中各对手对局]
    D -->|天梯ELO| F[就近积分匹配对手]
    E --> G[起对局沙箱 M2]
    F --> G
    G --> H0[M2 恢复双方参战物到工作区]
    H0 --> H[执行对局: 攻防/博弈]
    H --> I[M3 判定结果]
    I --> J[记 battle_match + replay_ref]
    J --> K[更新 ELO/排名]
    K --> L[WS 推天梯]
    A2[选手改进重新提交] --> B
```

---

## 5. 对抗对局执行(攻防类)

```mermaid
sequenceDiagram
    participant Sched as 撮合器
    participant M2 as 沙箱
    participant M3 as 评测
    Sched->>M2: 起干净对局沙箱
    Sched->>M2: 恢复双方参战物归档到 battle/entry_a 与 battle/entry_b
    Sched->>M2: 部署守方防御合约
    Sched->>M2: 执行攻方攻击脚本
    M2-->>Sched: 链上交易序列(记录回放)
    Sched->>M3: 链上断言判定是否攻破
    M3-->>Sched: 攻破/挡住
    Sched->>Sched: 结算得分 + 存回放 + 销毁沙箱
```

---

## 6. 对局回放

```mermaid
flowchart LR
    A[选手查看对局] --> B[读 battle_match.replay_ref]
    B --> C[取交易序列]
    C --> D[沙箱/前端重放每笔交易]
    D --> E[逐步展示攻击/防御/博弈过程]
```

---

## 7. 真实漏洞转化流程

```mermaid
flowchart TD
    A[配置外部源 + 同步] --> B[拉取漏洞案例]
    B --> C[分级 A/B/C]
    C -->|A| D[自动生成合约+断言+初始化]
    C -->|B| E[待补全草稿 → 人工补全]
    C -->|C| F[转为静态/理论素材]
    D --> G[预验证: 正向PoC通过 + 反向不误判]
    E --> G
    G -->|通过| H[导入即固化: 完整快照存 M5]
    G -->|失败| I[返回修改重验]
    H --> J[题目自包含, 竞赛引用, 运行时零外部依赖]
```

---

## 8. 防作弊判定

```mermaid
flowchart TD
    A[提交/对局数据] --> B[行为监控: 频率/协作模式异常]
    A --> C[M3 提交查重: 哈希/相似度]
    B --> D[汇集可疑名单]
    C --> D
    D --> E[教师/管理员人工核定]
    E --> F{判定}
    F -->|作弊| G[记 cheat_record + 处理: 警告/扣分/取消]
    F -->|正常| H[忽略]
```
