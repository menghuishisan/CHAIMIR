# M8 竞赛 — 接口设计

> RESTful + WebSocket(排行榜/对局状态)。鉴权沿用 M1 JWT。复用 M2/M3/M5。
> 最后更新:2026-08-05

---

## 1. 通用约定

- Base:`/api/v1/contest`
- 统一响应体同 M1。错误码段:`8xxxx`(81xxx 赛事/82xxx 报名/83xxx 解题/84xxx 对抗/85xxx 漏洞源)。

---

## 2. 赛事管理 `[教师/管理员]`

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/contests?status=` | 竞赛列表 |
| POST | `/contests` | 创建竞赛(赛制/赛程/规则) |
| PATCH | `/contests/{id}` | 编辑 |
| POST | `/contests/{id}/problems` | 编排题目(引用 M5/组卷) |
| POST | `/contests/{id}/publish` | 发布(进入报名) |
| POST | `/contests/{id}/start` / `end` | 开始/结束 |
| POST | `/contests/{id}/archive` | 归档:级联回收环境(调 M2 recycle)+ 生成成绩快照。可由结束后系统自动触发或管理员手动 |

---

## 3. 报名与组队 `[学生]`

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/student/contests` | 学生可发现的非草稿赛事分页(`status BETWEEN 2 AND 6`);支持 `status` 筛选(省略或 0 表示不筛,传值仍受可见区间约束),筛选与 `total` 同口径 |
| GET | `/student/contests/{id}` | 单条赛事详情,门槛与列表逐字一致(草稿态对学生不可见) |
| POST | `/contests/{id}/signup` | 报名(个人/创建队伍) |
| POST | `/contests/{id}/join-team` | 用队长分享的邀请码加入本赛事队伍(可跨校);请求体只含 `invite_code`,由后端按码定位队伍 |
| GET | `/teams/{id}` | 队伍信息 |
| POST | `/teams/{id}/lock` | 锁定队伍(或开赛自动锁) |

> 加入队伍只保留「按邀请码」一种语义。邀请码本就是可分享的队伍凭据,而队伍编号对学生是不可知的内部标识 —— 要求前端先拿到 `team_id` 会逼出「让用户手填内部编号」的界面。原先并存的 `POST /teams/{id}/join`(需队伍编号 + 邀请码)与之功能重叠且要求内部编号,统一到本条(见 `docs/前端后端功能对齐清单.md` §6.9)。

---

## 4. 解题赛 `[学生]`

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/contests/{id}/problems` | 题目列表(题面取 M5,过滤答案) |
| POST | `/contests/{id}/problems/{pid}/env` | 起实操题环境(调 M2) |
| POST | `/contests/{id}/problems/{pid}/submit` | 提交(flag/代码/理论答案)→ 调 M3 判 |
| GET | `/submissions/{id}` | 查看判定结果 |

> 提交流程:M8 收提交 → 调 M3(带 M5 题目引用 + 沙箱状态)→ 回写 → 更新排行。
> M8 题目列表必须经 M5 题面接口返回过滤后的内容;M5 题面契约不可用时返回 M8 专用错误码,不能返回无题面或未过滤数据。实操题环境创建必须经 M2 契约,解题提交必须经 M3 契约,缺必需契约时显式失败。

---

## 5. 对抗赛 `[学生]`

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/contests/{id}/battle/entry` | 提交参战物(防御合约/攻击脚本/策略合约) |
| GET | `/contests/{id}/battle/entries` | 我的参战历史(各版本) |
| GET | `/contests/{id}/battle/matches?page=&size=` | 对局分页列表 `[师生]`,响应 data 为 `{list,total,page,size}` |
| GET | `/matches/{id}/replay` | 确认对局回放归档是否可用,只返回 `match_id` 与 `available`,不暴露对象存储引用 |
| POST | `/matches/{id}/replay/download-grant` | 按已落库的回放对象引用,为已授权队伍成员签发统一文件服务短时授权 |
| GET | `/contests/{id}/ladder` | 天梯排名 |

对局列表师生同路由按身份分视角:赛事组织者与学校管理员见本赛事**全部**对局(实时监控要看
「哪一局卡住了」),其余账号退回**本队**对局(时空回溯器要的是本队记录),未报名者读不到任何对局。
同一张表的分页查询只有一份 SQL,按 `team_id` 是否为 0 分叉;不为教师另开一条同义列表路由。
回放取件仍限参赛队伍成员(`authorizedBattleReplay`),组织者可见对局状态但不等于可下载他人轨迹。

对局列表和数据库中的 `score_delta` 使用同一固定结构,不得以无类型对象或数字 ID 双轨保存:`team_a/team_b` 使用公开雪花 ID 字符串,`rating_a_before/rating_b_before/rating_a_after/rating_b_after/delta_a/delta_b/k_factor` 为数字,`result` 使用对局结果枚举;对局尚未结算时数据库保存 `{}`,接口省略该字段。回放归档的 `match_id/task_id/initial_state.contest_id/initial_state.problem_id` 同样全部使用雪花 ID 字符串;动作序列只保存在顶层 `actions`,不得在 `result` 内重复保存;`initial_state.entry_a/entry_b.role` 必须接受攻方、防方和博弈策略三种已登记角色,解析器对未知枚举、缺失字段或类型错误直接拒绝,不得猜测默认值。

> 提交参战物 → 进对战池 → 撮合调度器异步起对局(M2)→ 判定 → 按标准 ELO 公式更新天梯 → 通知。ELO 初始分与 K 因子统一来自 `CONTEST_BATTLE_ELO_INITIAL_SCORE`、`CONTEST_BATTLE_ELO_K_FACTOR`,不得在代码或 SQL 中写死。参战物提交必须同时提供 `artifact_ref` 和内容 SHA256 `code_hash`;M8 持久化该 hash 用于复现、审计和查重。对局判题先通过 M2 `RestoreSandboxArchive(expected_hash=code_hash)` 校验并恢复双方参战物到对局沙箱 `battle/entry_a`、`battle/entry_b`,再用 M3 reuse 模式读取现场沙箱和 M5 锁定判题配置;M8 不把参战物伪装成 M3 代码提交字段。

---

## 6. 排行榜(实时)

- 经 M10 统一通道:M8 判题/对局后调 M10 `POST /notify/push(tenant_id, topic=tenant:{tenant_id}:contest:{id}:leaderboard)`;客户端订阅 M10 `WS /api/ws` 的该 topic。
- 封榜后停止推送实时变化；`GET /contests/{id}/ladder` 固定读取进入封榜时的服务端快照，不能通过刷新页面或直接请求 URL 窥探后续判定。

---

## 6.5 个人竞赛战绩档案 `[学生/教师]`

> B11 修复:竞赛成绩不计 GPA,但需有落点供学生查看。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/my/contest-records` `[学生]` | 我的参赛历史(竞赛/名次/得分/天梯ELO/徽章) |
| GET | `/contests/{id}/result-snapshot` | 竞赛结束生成的成绩快照(最终榜单存档) |
| GET | `/students/{id}/contest-achievements` `[内部]` | 供 M11 独立聚合"竞赛成就"(与 GPA 分离,只读) |

> 竞赛结束(已结束→已归档)时生成 `result-snapshot` 成绩快照存档,避免战绩只活在 ladder_rank/battle_match 流水里。`final_ranking` 每项固定为 `{ team_id, score, solved_count, last_solve_at, rank, updated_at }`,其中 `team_id` 是雪花 ID 字符串,不得退化为无类型对象数组。学生竞赛战绩独立展示,不混入学业 GPA。

---

## 7. 防作弊 `[教师/管理员]`

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/contests/{id}/cheat-suspects` | 可疑提交(行为异常 + M3 查重结果) |
| POST | `/contests/{id}/cheat-records` | 判定并处理(警告/扣分/取消资格) |
| GET | `/contests/{id}/cheat-records?page=&size=` | 违规处理记录分页列表,响应 data 为 `{list,total,page,size}` |

---

## 8. 真实漏洞源

### 8.1 租户能力 `[出题教师/学校管理员]`

漏洞源同步与漏洞题转化是**租户内**的出题工作流,产物落本租户题库:

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/vuln-sources` | 本租户漏洞源配置(SWC/情报/CVE) |
| POST | `/vuln-sources/{id}/sync` | 同步拉取漏洞案例,按配置映射生成漏洞题草稿 |
| GET | `/vuln-problems?source_id=&status=&page=&size=` | 漏洞题草稿分页列表,响应 data 为 `{list,total,page,size}` |
| POST | `/vuln-problems` | 录入一条漏洞题草稿(手工录入,或按来源案例分级后落草稿) |
| POST | `/vuln-problems/{id}/prevalidate` | 预验证(正向+反向) |
| POST | `/vuln-problems/{id}/finalize` | 导入即固化 → 调 M5 `system-import` 存入 M5(自包含) |

> 草稿录入只有 `POST /vuln-problems` 一条路径。原先另有 `POST /vuln-sources/import`,
> 两者绑同一个 handler、同一个请求体(`ImportVulnProblemRequest` 带可选 `source_id`),
> 是同一件事的两条路由;`source_id` 本身已经表达了「这条草稿来自哪个来源」,
> 不需要再用路径区分手工录入与来源导入。故收敛到 `/vuln-problems`,不保留同义路径。

### 8.2 平台能力 `[平台管理员]`

平台只维护**全局漏洞源目录**(`tenant_id=0`),不代租户同步、不生成租户草稿:

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/platform/vuln-sources` | 全局漏洞源列表,只返回 `tenant_id=0` 的记录 |
| POST | `/platform/vuln-sources` | 创建或更新全局漏洞源,不产生租户漏洞题草稿 |

⇒ 前端平台管理端「漏洞题源」页只做全局源的查看与新增/更新;同步、预验证、固化只出现在教师端。

---

## 8.5 内部聚合 `[内部]`(B2 修复)

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/internal/stats?tenant_id=` | 供 M9 看板聚合:竞赛数、参赛人数、活跃竞赛等只读统计 |
| GET | `/students/{id}/contest-achievements` `[内部]` | 供 M11 聚合竞赛成就(与 GPA 分离) |

> 漏洞题最终落 M5;运行时/答题零外部依赖。
> 异步判题事件订阅失败、事件 payload 无效、source_ref 未匹配分别使用独立错误码,便于定位是订阅链路、事件格式还是提交绑定问题。
> 内部聚合只保留 `/students/{id}/contest-achievements` 这一条稳定路径,不得新增旧式别名或旁路路由。

---

## 9. 典型时序

### 解题赛提交
```
学生 → 起环境(M2) → 提交 → M8 调 M3 判(取 M5 配置, judge 沙箱)
M3 → 回写结果 → M8 更新 ladder_rank → WS 推排行榜
```

### 对抗赛对局
```
学生 → 提交参战物 → 进对战池
撮合调度器 → 选对手 → 起对局沙箱(M2) → 跑对局 → M3 判定
→ M3 持久化 M2 链能力结果 → M8 写报告桶回放归档并记 battle_match.replay_ref → 更新 ELO → WS 推天梯
学生 → 看对局回放(交易序列重放)
```
