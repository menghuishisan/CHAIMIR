# 认证与公共页 (Auth & Public Pages) 全局页面设计规范

严格遵循无 Emoji、全令牌化、“青电×琥珀”双色规范。
**说明**：这是《前端后端功能对齐清单》中指定的“公共登录前页面”，所有认证相关页面均不包含普通的顶栏和侧边栏，而是采用全屏沉浸式的**左右分栏布局**（左侧品牌宣传/右侧操作表单）或**全屏居中卡片布局**。

**实现基准**：
1. 本文是 UI/UX 视觉与信息架构草图，不是后端契约来源。实现时以 `docs/前端后端功能对齐清单.md`、后端公开 API/DTO/枚举和 `frontend/packages/api-client` 为准。
2. 草图中的示例字段、状态、文案和布局可在实现时优化；凡与后端已实现能力冲突的，以后端实现为准。
3. 不得用模拟数据替代已有后端功能；没有后端契约的能力不得实现为可操作功能。
4. 最终界面不得使用 emoji，草图中的状态必须落地为 Lucide 图标 + `@chaimir/ui` 状态组件 + 文本文案。

## 全局布局模板 (Split 布局)
```text
======================================================================================================
[左侧: 品牌视觉展示区 (占宽 50% ~ 60%, 仅桌面端显示)]     | [右侧: 表单操作区 (占宽 40% ~ 50%, bg-white)]
[背景: 深色系青电色网格渐变与 3D 几何图形]              |
                                                        | [Logo] Chaimir 区块链教学平台
[Slogan H1] 虚实相生，铸造安全之盾                      |
[Slogan P] 下一代区块链与网络安全实训靶场               | (各页面的表单区见下文)
                                                        |
======================================================================================================
```

---
### 1. 学校用户登录 (`#login`)
```text
[右侧表单操作区]
------------------------------------------------------------------------------------------------------
[H1] 欢迎回来，请登录
[ Tabs: 账号密码登录 (Active) | 手机验证码登录 | 统一身份认证(SSO) ]

[ 账号输入框: 学号/工号/手机号 ]
[ 密码输入框 ]

[ 记住我 Checkbox ]                              [ 忘记密码？(链接) ]
[ 登录 (青电主按钮, w-full) ]

[ 尚未入驻？ -> [申请入驻平台(链接)] ]
[ 首次使用？ -> [账号激活(链接)] ]
```

### 2. 租户选择 (登录后的多校区拦截态)
```text
[右侧表单操作区]
------------------------------------------------------------------------------------------------------
[H1] 选择您的学校
[说明: 您的手机号绑定了多个学校，请选择本次要进入的学校；此处只选择学校，不让用户选择角色。]

[列表/卡片]
┌───────────────────────────────────────┐
│ [Icon:Building] 东南大学 (SEU)          │
│ 后端返回的账号身份: 学生                 │
└───────────────────────────────────────┘
┌───────────────────────────────────────┐
│ [Icon:Building] 南京理工大学 (NJUST)    │
│ 后端返回的账号身份: 教师                 │
└───────────────────────────────────────┘

[ 返回重新登录 ]
```

### 3. 找回密码 (`#forgot`)
```text
[右侧表单操作区]
------------------------------------------------------------------------------------------------------
[H1] 找回密码
[步骤条: 1. 验证身份 -> 2. 设置新密码 ]

[ 学校编号/短码 输入框 ]
[ 绑定的手机号 输入框 ]
[ 验证码 输入框 ] [获取验证码(按钮)]

[ 下一步 (青电按钮) ]
[ 返回登录 ]
```

### 4. 账号激活 (`#activate`)
```text
[右侧表单操作区]
------------------------------------------------------------------------------------------------------
[H1] 激活账号
[说明: 首次登录请使用管理员下发的激活码完成账号激活]

[ 激活码 输入框 (支持 6 位或 8 位卡密格式) ]
[ 设置新密码 输入框 ]
[ 确认新密码 输入框 ]

[ 立即激活 (青电按钮) ]
[ 返回登录 ]
```

### 5. 学校入驻申请 (`#apply`)
```text
[全屏居中沉浸式布局 (bg-slate-50)]
------------------------------------------------------------------------------------------------------
[ 居中白底宽卡片 max-w-3xl ]
[H1] 申请入驻 Chaimir 平台
[说明: 提交您的学校信息，我们将在 1-3 个工作日内完成审核]

[ 多步骤向导表单 ]
[ Step 1: 机构信息 ]
  机构名称: [输入框]     机构统一信用代码: [输入框]
[ Step 2: 联系人信息 ]
  联系人姓名: [输入框]   联系电话: [输入框]
[ Step 3: 资质文件上传 ]
  上传证明函件加盖公章扫描件: [拖拽上传组件区]

[ 提交申请 (青电主按钮) ] [ 已经提交？查询审核进度 ]
```

### 6. 统一认证中间页 (`#sso`)
```text
[全屏居中遮罩 (透明度毛玻璃)]
------------------------------------------------------------------------------------------------------
[ 居中卡片 ]
[ Loading 动画: 青电色双环旋转 ]
[ 文本: 正在通过您所在学校的 CAS/LDAP 进行身份核验，请稍候... ]
[ 状态: CAS 由后端返回跳转 URL 驱动；LDAP 使用学校短码和统一认证账号密码提交；失败展示用户向 message + trace_id。]
```

### 7. 平台管理员登录 (`#platform-login`)
```text
[专属暗黑极客风格分栏布局]
======================================================================================================
[左侧: K8s 节点拓扑图与代码流动态背景]                | [右侧: 表单操作区 (bg-slate-950)]
                                                    | [Logo] Chaimir (Platform Admin)
                                                    |
                                                    | [H1, 白字] 平台超级管理员通道
                                                    | [ 账号输入框 (暗色边框) ]
                                                    | [ 密码输入框 (暗色边框) ]
                                                    | [ 登录 (琥珀色警告按钮, 彰显特权与危险) ]
                                                    | [说明: 当前以后端 `POST /auth/login/platform` 字段为准；后端未提供额外二次校验契约前不得展示额外校验输入框。]
======================================================================================================
```

### 8. 强制改密提示 (`#change-pwd`)
```text
[右侧表单操作区]
------------------------------------------------------------------------------------------------------
[H1] 出于安全要求，请修改密码
[说明: 后端登录响应 `must_change_pwd` 时展示本拦截态。改密必须走个人中心 `POST /me/password`，不得在前端自造独立改密接口或绕开后端会话规则。]

[ 前往个人中心修改密码 (青电按钮) ] [ 退出并重新登录 ]
[ 若会话失效: 展示用户向错误文案 + trace_id，并引导返回登录。]
```
