# M11 成绩中心 — 数据模型

> PostgreSQL,全部租户级(tenant_id + RLS)。M11 只存聚合/审核/预警元数据,单课程成绩在 M6。
> 建模遵循:消灭数据冗余、表合并有度。
> 最后更新:2026-05-29

---

## 1. 表清单总览

| 表名 | 用途 |
| --- | --- |
| `grade_level_config` | 等级映射配置(分数段→等级→绩点) |
| `semester` | 学期 |
| `grade_review` | 成绩审核记录 |
| `student_semester_grade` | 学生学期成绩聚合(GPA) |
| `grade_appeal` | 成绩申诉 |
| `academic_warning` | 学业预警 |
| `transcript_record` | 成绩单记录 |

> 消灭冗余/合并说明:
> - **单课程成绩不复制**:`student_semester_grade` 存聚合结果(学期 GPA/学分),明细课程成绩从 M6 实时读,不复制 M6 成绩到 M11。
> - **等级映射按学校配置**:`grade_level_config` 一校一套(或按学期),不写死。
> - **审核记录与聚合分表**:审核是流程流水,聚合是结果,分开。

---

## 2. 配置与学期

### 2.1 grade_level_config — 等级映射

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| name | VARCHAR(64) | 方案名 |
| mapping | JSONB | 分数段→等级→绩点,如 [{min:90,grade:"A",gpa:4.0},…] |
| warning_rules | JSONB | 学业预警规则,如 {"fail_count":1,"min_gpa":2.0} |
| is_default | BOOLEAN | |

### 2.2 semester — 学期

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| name | VARCHAR(64) | 如 2025-2026-1 |
| start_date / end_date | DATE | |
| is_current | BOOLEAN | |

---

## 3. 审核与聚合

### 3.1 grade_review — 成绩审核

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| course_id | BIGINT | 关联 M6 课程 |
| semester_id | BIGINT NULL | 审核通过时归属学期,供后续 M6 成绩变更事件重算 GPA |
| submitter_id | BIGINT | 提交教师 |
| reviewer_id | BIGINT NULL | 审核学校管理员 |
| status | SMALLINT | 1待审/2通过/3驳回 |
| is_locked | BOOLEAN | 审核状态是否已进入锁定态,供申诉/重审流程使用 |
| comment | VARCHAR(500) NULL | |
| submitted_at / reviewed_at | TIMESTAMPTZ | |

### 3.2 student_semester_grade — 学期成绩聚合

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| student_id | BIGINT | |
| semester_id | BIGINT | |
| total_credits | NUMERIC(5,1) | 学期学分 |
| gpa | NUMERIC(4,3) | 学期 GPA |
| cumulative_gpa | NUMERIC(4,3) | 累计 GPA |
| computed_at | TIMESTAMPTZ | |

唯一约束:`(student_id, semester_id)`。

> 明细课程成绩从 M6 读;本表只存聚合结果,改分/申诉后重算。

---

## 4. 申诉与预警

### 4.1 grade_appeal — 申诉

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| student_id | BIGINT | |
| course_id | BIGINT | |
| reason | TEXT | 申诉理由 |
| status | SMALLINT | 1待处理/2受理改分中/3已完成/4驳回 |
| handler_id | BIGINT NULL | |
| result_comment | TEXT NULL | |
| created_at / handled_at | TIMESTAMPTZ | |

> 受理改分:发布事件驱动 M6 解除本模块 `course_grade.is_locked` 写保护投影 → 教师在 M6 改单课程成绩 → 触发 M11 GPA 重算。

### 4.2 academic_warning — 学业预警

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| student_id | BIGINT | |
| semester_id | BIGINT | |
| type | SMALLINT | 1挂科/2低GPA |
| detail | JSONB | 预警详情 |
| status | SMALLINT | 1待处理/2已知悉/3通知失败 |
| created_at | TIMESTAMPTZ | |

---

## 5. 成绩单

### 5.1 transcript_record — 成绩单

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | |
| tenant_id | BIGINT | |
| student_id | BIGINT | |
| scope | SMALLINT | 1学期/2全部 |
| semester_id | BIGINT NULL | |
| pdf_ref | VARCHAR(255) | 成绩单 PDF(MinIO) |
| generated_at | TIMESTAMPTZ | |

---

## 6. 多租户与索引

- 全部表 `tenant_id` + RLS(同 M1)。
- 关键索引:
  - `grade_review(tenant_id, course_id, status)`
  - `student_semester_grade(student_id, semester_id)` UNIQUE
  - `grade_appeal(tenant_id, student_id, status)`
  - `academic_warning(tenant_id, student_id, semester_id)`
