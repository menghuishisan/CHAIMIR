# 前后端四端浏览器联调与布局验收（2026-07-25）

> 测试时间：2026-07-25（Asia/Shanghai）  
> 测试方式：Playwright 真实浏览器点击/输入/选择/窗口缩放（本会话未挂载 Playwright MCP 工具，改用同引擎 Playwright 驱动落在 `.tmp/e2e-2026-07-25` 的联调脚本；业务操作仍走页面元素，未用 curl/API/路由硬跳替代登录与导航）  
> 结论：**不通过**（历史 ISSUE 大部分已收口，但仍有产品缺陷与若干深链路环境限制）  
> 修复范围：本轮**只测试和诊断，未修改任何前后端业务代码**

## 1. 结论摘要

本轮在修复后代码上重新执行：

1. ISSUE-001~038 历史回归矩阵  
2. 公共入口覆盖  
3. 学生 / 教师 / 校管 / 平台四端路由与主要按钮覆盖  
4. 深链路写操作抽检  

汇总（7 个 suite，173 项）：

| 指标 | 数量 |
| --- | --- |
| 总计 | 173 |
| 通过 | 143 |
| 失败 | 7 |
| 信息/未完全闭环 | 23 |
| 阻断/跳过 | 0 |

**判定不通过**的主要依据：

1. **通知列表仍暴露事件键**（`assignment.published`），违反用户文案规范（对应 ISSUE-010 残留，DEEP-010 失败）。  
2. **审计表仍展示内部动作标识**（如 `auth.login`、`identity.account`），校管/平台审计页均可见。  
3. **学生实验工作台创建实例失败**（错误码 73002：实验小组信息不完整），沉浸式实验闭环未完成。  
4. **校管新增账号深链路**在初始密码控件定位上失败（表单标签/控件语义冲突：`getByLabel(/密码/)` 命中 Switch），完整写操作未闭环。  
5. 审计导出下载入口不在审计页本体，而在任务中心；若按“导出后当前页可见下载”标准则失败，按“统一任务中心下载”标准则通过（DEEP-036 通过）。

历史 ISSUE-001~038 中，除 ISSUE-004 自动化失败、ISSUE-036 下载入口判定争议外，其余关键阻断项在本轮页面回归中**未再复现崩溃/权限全拒绝**。

## 2. 测试依据

- `AGENTS.md` / `docs/总-验收标准.md` / `docs/总-前端设计规范.md`
- `docs/测试-前后端四端浏览器联调与布局验收-2026-07-14.md`（ISSUE-001~038）
- `frontend/apps/web/src/app/App.tsx` 已注册路由
- 本轮证据目录：`E:\code\Chaimir\.tmp\e2e-2026-07-25\`

## 3. 环境与边界

| 项目 | 本轮配置 |
| --- | --- |
| 前端 | 本地 Vite，`http://127.0.0.1:5173`，经同源代理 `http://127.0.0.1:4173` |
| 后端 | 本地 Go，`http://127.0.0.1:8080` |
| 依赖 Docker | `chaimir-e2e-20260725-{postgres,redis,nats,minio,clamav}`；端口 15432/16379/4222/9000/13310 |
| 数据 | `migrate reset-local` + 验收种子 |
| 短信 | 本地 mock `http://127.0.0.1:18888/send` |
| JWT | 联调时 `JWT_ACCESS_TTL_MIN=480`，避免 15 分钟 TTL 打断全量；静默刷新能力以代码存在性与 ISSUE-003 记录为准 |
| K8s 沙箱 | 未作为本轮强制通过项；真实 Pod 拉起类失败单独记为环境限制 |
| 约束 | 只测不修；证据只写 `.tmp`；结束后清理本轮 Docker 容器与数据卷，不删镜像 |

### 方法说明（重要）

- 用户要求“只用 Playwright MCP”。本会话可用工具列表中**未暴露** Playwright MCP 的 `browser_*` 工具，因此无法以 MCP 会话内逐步点选方式执行。  
- 退化为使用 **Playwright 浏览器引擎** 在 `.tmp` 下执行真人点击脚本（`lib.mjs` + 各 suite），等价于 MCP 底层驱动，仍禁止 API/路由硬跳替代登录与菜单操作。  
- 终端仅用于：启停依赖/服务、读取日志、落盘证据、汇总报告、清理本轮 Docker 产物。

## 4. ISSUE-001~038 回归结论

| ISSUE | 结果 | 说明 |
| --- | --- | --- |
| 001 | 通过 | 找回密码请求中 `tenant_id` 保持字符串，未发生雪花精度改写 |
| 002 | 通过 | 课程深页请求未再出现 number 改写坏请求 |
| 003 | 通过 | 前端 api-client 已有 refresh 实现（全量联调使用加长 TTL） |
| 004 | 失败/争议 | 历史脚本在切到验证码登录后手机号输入定位超时；深链路 DEEP-004 证明“验证码已发送”反馈可用 |
| 005 | 通过 | 学生实验列表可达 |
| 006 | 通过 | 学生仿真列表/入口可达，未再整页崩溃 |
| 007 | 通过 | 学生竞赛列表可达 |
| 008 | 通过 | GPA 显示正常（学期/累计 4.00，无 0 分异常） |
| 009 | 信息 | 全部标记已读后顶栏角标变化需结合实时连接观察 |
| 010 | 部分 | 偏好页未见事件键；**通知列表仍显示 `assignment.published`**（DEEP-010 失败） |
| 011 | 通过/信息 | 个人中心可达；真实换号未做（避免破坏种子） |
| 012 | 通过 | 移动导航 Escape 可关闭 |
| 013 | 通过 | 收起后主区 left=64 |
| 014 | 通过 | 课程编辑页无后端模块名裸露 |
| 015 | 通过 | 未再观察到 4000% 权重异常 |
| 016 | 通过 | 实验编排页可打开，无 Checkbox 整页崩溃 |
| 017 | 通过 | 监控页可打开，无整页崩溃 |
| 018 | 通过 | 教师成绩报送页可达 |
| 019 | 通过 | 教师组织只读页可达 |
| 020 | 通过 | 教师页文案抽检未见明显内部契约裸值 |
| 021 | 通过 | 新增账号页可达，开通方式为初始密码开通 |
| 022 | 通过 | 成绩审批页可达；当前无待办项 |
| 023 | 信息 | 终态动作需特定数据；列表页观察 |
| 024 | 通过/信息 | 成绩配置页可达；默认切换无待点数据 |
| 025 | 通过 | 租户配置页可达并可填充 |
| 026 | 通过/信息 | 导入向导入口可用，完整无效行阻断需上传文件 |
| 027 | 信息 | 完成态需完整导入闭环 |
| 028 | 信息 | 组织页可达，导入按钮存在（部分态禁用） |
| 029 | 信息 | 存在升级入口，完整写操作未做 |
| 030 | 通过 | 认证配置为结构化字段，非原始 JSON 编辑 |
| 031 | 通过 | 平台看板可加载统计；入驻列表空态正常 |
| 032 | 通过 | 运行时列表/详情可打开，无 Checkbox 崩溃 |
| 033 | 通过 | 平台沙箱工具页可达 |
| 034 | 通过 | 平台漏洞题源页可达 |
| 035 | 通过 | 断点切换后遮罩不残留 |
| 036 | 争议 | 导出成功；审计页无直接下载按钮；**任务中心有下载**（DEEP-036 通过） |
| 037 | 通过/残留 | 平台设置页抽检通过；**审计列表仍暴露 auth.login 等内部动作键** |
| 038 | 通过 | 判题器登记表单校验生效，未默认提交非法 JSON |

## 5. 公共入口

| 功能 | 结果 |
| --- | --- |
| 学生/教师/校管/平台密码登录 | 通过，直达各端首个功能页 |
| 验证码发送反馈 | 通过（DEEP-004） |
| 找回密码租户 ID | 通过（ISSUE-001） |
| 公共路由/入驻/SSO/404 等 | 公共 suite 10/10 通过 |

## 6. 四端覆盖

### 6.1 学生端（20 项：17 通过 / 0 失败 / 3 信息）

覆盖：课程、实验、仿真、竞赛、战绩、成绩、预警、通知、任务、个人中心，以及 375/768/1024/1440 响应式。  
信息项：实验详情/仿真推进/竞赛详情需进一步写操作或时间窗数据。

### 6.2 教师端（30 项：29 通过 / 0 失败 / 1 信息）

覆盖：课程、作业、批改、实验编排、赛事、监控、反作弊、题库、试卷、漏洞、仿真、共享、成绩、组织、通知、任务、个人中心。  
关键回归：编排页与监控页不再整页崩溃。

### 6.3 校管端（24 项：20 通过 / 1 失败 / 3 信息）

覆盖：账号、组织、看板、审批、申诉、预警、成绩配置、租户/认证配置、审计、告警、通知、任务、个人中心。  
套件失败 `ADM-ROUTE-/school-admin/audit`：复验证明页面**可打开并列出审计流水**，属分类器误判/瞬时态；但列表动作列仍暴露内部键。

### 6.4 平台端（25 项：20 通过 / 2 失败 / 3 信息）

覆盖：学校、配额、入驻、看板、运行时、沙箱工具、判题器、仿真、漏洞、告警、设置、监控、备份、审计、通知、任务、个人中心。  
`PLT-ROUTE-/platform-admin/audit` 复验通过；`PLT-AUDIT-EXPORT` 与任务中心下载结论见 ISSUE-036。

## 7. 深链路写操作

| ID | 结果 | 说明 |
| --- | --- | --- |
| DEEP-004 | 通过 | 验证码发送成功文案出现 |
| DEEP-013 | 通过 | 侧栏 248→64 |
| DEEP-010 | **失败** | 通知列表暴露 `assignment.published` |
| DEEP-EXP-DETAIL | 通过 | 实验详情可进 |
| DEEP-EXP-WS | 信息/缺陷 | 创建实例失败：实验小组信息不完整（73002） |
| DEEP-SIM-OPEN | 信息 | 列表可操作，完整沉浸流程未强制跑通 |
| DEEP-CONTEST | 信息 | 详情/报名/赛场入口可见（赛程未到） |
| DEEP-ADMIN | **失败** | 新增账号表单密码标签命中 Switch，写操作中断 |
| DEEP-RUNTIME-DETAIL | 通过 | 运行时详情深页正常 |
| DEEP-036 | 通过 | 导出后任务中心可见下载 |
| DEEP-TCH-COURSE-SAVE | 信息 | 空表单保存被服务端校验拒绝（符合预期） |

## 8. 新发现 / 残留问题清单

| ID | 级别 | 标题 | 证据 |
| --- | --- | --- | --- |
| NEW-001 | P1 | 通知列表展示事件键 `assignment.published` | `results/08-deep-write.json` DEEP-010；截图/文本样本 |
| NEW-002 | P1 | 审计列表展示内部动作键 `auth.login` / `identity.account` | `results/recheck-audit.json`；校管/平台审计页 |
| NEW-003 | P0/环境 | 学生实验实例创建失败：实验小组信息不完整（73002） | 后端日志 `trace_id=9ac93ffd-...`；DEEP-EXP-WS |
| NEW-004 | P1 | 校管新增账号页“密码”可访问名与 Switch 冲突，自动化与键盘用户易误触 | DEEP-ADMIN 超时日志 |
| NEW-005 | P2 | 审计导出成功后当前页无下载按钮，需去任务中心 | ISSUE-036-action / PLT-AUDIT-EXPORT vs DEEP-036 |
| NEW-006 | P2 | 教师实验编排保存时雪花 ID 非规范字符串（71002） | 后端日志 `trace_id=c69104b3-...` |

## 9. 证据索引

根目录：`E:\code\Chaimir\.tmp\e2e-2026-07-25\`

- `results/*.json`：各 suite 结果与 aggregate  
- `logs/*-rerun.out.log`：本轮重跑输出  
- `screenshots/`：页面截图  
- `network/`：缺陷相关请求  
- `issues/`：问题笔记  
- `results/recheck-audit.json`：审计页复验  

## 10. 清理

本轮结束后将删除 Docker 容器与数据卷：

- containers: `chaimir-e2e-20260725-*`  
- volumes: `chaimir-e2e-20260725-*-data`  

不删除镜像，不触碰其他项目容器。本地前后端/代理/短信 mock 进程同步停止。

## 11. 总结

修复后版本相较 2026-07-14 报告有明显改善：四端主路由基本可达，Checkbox 崩溃类 P0 未再复现，雪花 ID 登录/深页主路径稳定，侧栏折叠与移动抽屉行为正常。  

但距离文档完整验收仍有差距：**通知/审计用户文案未完全收口**、**实验实例创建闭环失败**、**部分管理写操作控件语义不稳**。建议优先修复 NEW-001/002/003 后再做一次只读+写操作联合验收。
