# 后端完整功能测试记录 2026-06-19

本文记录 Chaimir 后端在本地工作区执行的真实功能测试。测试临时文件、脚本、日志和接口证据统一放在 `.tmp/backend-functional-test/`，该目录已加入 `.gitignore`，不进入仓库。测试期间未打印 `deploy/config/secret.env` 或 `backend/.env` 的真实密钥值。

## 1. 测试环境

| 项目 | 结果 |
| --- | --- |
| 仓库路径 | `E:\code\Chaimir` |
| commit | `55e33c40079a0940b00766f44db94f0cd8332ce5` |
| 后端启动目录 | `E:\code\Chaimir\backend` |
| 后端启动命令 | `.tmp\backend-functional-test\run-server.ps1 -BackendEnv .tmp\backend-functional-test\env\backend-test.env -SecretEnv deploy\config\secret.env`，内部执行 `go run ./cmd/server` |
| 后端地址 | `http://127.0.0.1:18081` |
| 依赖模式 | 前后端工作区不进 Docker；PostgreSQL、Redis、NATS、MinIO、ClamAV 在本地 K8s |
| K8s namespace | `chaimir-data` |
| 依赖 Pod | `clamav-0`、`minio-0`、`nats-0`、`postgres-0`、`redis-0` 均 Running |
| 本地端口 | `15432`、`16379`、`14222`、`19000`、`13310`、`18080`、`18081` 均 Listen |
| 数据库重置命令 | `scripts\db\reset-local.ps1`；脚本只加载 `backend/.env` 和 `deploy/config/secret.env`，并校验 `APP_ENV/DEPLOY_MODE` 与 `PG_HOST` 必须是本地 |
| 本地短信网关 | `127.0.0.1:18080`，日志 `.tmp/backend-functional-test/logs/sms-gateway.ndjson`；仅 `APP_ENV=dev` 且 `SMS_HTTP_ALLOW_LOOPBACK=true` 时允许 loopback 验收网关，生产仍走公网 SSRF 防护 |

## 2. 前置检查

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| `git status --short` | 通过；测试过程中有预期代码/文档改动和新增测试文档 | `.tmp/backend-functional-test/evidence/precheck-git-status-full-current.txt` |
| `git rev-parse HEAD` | `55e33c40079a0940b00766f44db94f0cd8332ce5` | `.tmp/backend-functional-test/evidence/precheck-commit-full-current.txt` |
| `git ls-files deploy/config/secret.env backend/.env` | 无输出，未跟踪 | `.tmp/backend-functional-test/evidence/precheck-secret-tracked-full-current.txt` |
| `git check-ignore -v deploy/config/secret.env backend/.env` | 两个文件均被 ignore；`.tmp` 也被 ignore | `.tmp/backend-functional-test/evidence/precheck-secret-ignore-full-current.txt` |
| `kubectl -n chaimir-data get pods` | 全部 Running | `.tmp/backend-functional-test/evidence/precheck-k8s-pods-full-current.txt` |
| `go test ./...` | 通过 | `.tmp/backend-functional-test/evidence/precheck-go-test-full-current.txt` |
| `go build ./cmd/server ./cmd/migrate` | 通过 | `.tmp/backend-functional-test/evidence/go-build-current.txt` |
| `kubectl kustomize` 五套 overlay | `local-dev`、`local-deps`、`staging`、`prod-school`、`prod-saas` 均渲染成功 | `.tmp/backend-functional-test/evidence/precheck-kustomize-summary-full-current.json` |

## 3. 路由覆盖对照

已从 `backend/api/openapi.yaml`、`backend/api/paths/*.yaml`、实际 Gin 路由和 `docs/总-API接口总览.md` 交叉生成清单。完整 338 行逐路由表在 `.tmp/backend-functional-test/evidence/routes-compare.csv`。

| 模块 | 路由数 | OpenAPI 存在 | 实际路由存在 | 非 `/api` 实际路由 |
| --- | ---: | --- | --- | --- |
| identity | 64 | 是 | 是 | 0 |
| transfer | 3 | 是 | 是 | 0 |
| sandbox | 37 | 是 | 是 | 0 |
| judge | 14 | 是 | 是 | 0 |
| sim | 21 | 是 | 是 | 0 |
| content | 26 | 是 | 是 | 0 |
| teaching | 54 | 是 | 是 | 0 |
| experiment | 23 | 是 | 是 | 0 |
| contest | 37 | 是 | 是 | 0 |
| notify | 13 | 是 | 是 | 0 |
| grade | 25 | 是 | 是 | 0 |
| admin | 19 | 是 | 是 | 0 |
| health | 2 | 是 | 是 | 0 |

汇总：OpenAPI 路由 338，实际路由 338，OpenAPI 有但实际没有 0，实际有但 OpenAPI 没有 0，实际非 `/api` 路由 0。`/api/healthz`、`/api/readyz` 均为 200 且响应 `{code,message,data,trace_id}`。未注册路径、错误 method 和历史 `/ws` 均返回统一 `{code,message,trace_id}`；其中 `NoRoute` 已从误用路径参数错误码修正为通用 `11005`，真实接口证据见 `.tmp/backend-functional-test/evidence/error-shape-after-fixes.json`。2026-06-20 新增命令工具路由 `POST /api/v1/sandbox/sandboxes/{id}/command-tools/{tool_code}/run` 后重新对照仍为 338/338，证据见 `.tmp/backend-functional-test/evidence/routes-summary.json`。

## 4. 已修正的测试前阻塞

| 编号 | 级别 | 问题 | 处理 | 验证 |
| --- | --- | --- | --- | --- |
| FIX-001 | P1 | `reset-local` 中 `ALTER ROLE ... PASSWORD $1` 不能作为 PostgreSQL utility statement 参数化执行 | 使用 PostgreSQL `quote_literal($1)` 生成密码字面量后执行，不打印密码 | `reset-local` 通过；`go test ./...` 通过 |
| FIX-002 | P1 | acceptance seed 的 `account_role` ID 派生碰撞 | 改为稳定唯一派生 | `reset-local` 通过 |
| FIX-003 | P2 | 验收 seed 缺少 `auth_session`、`sandbox`、`judge_task`、`judge_result`、`transfer_task` 等关键表数据 | 在现有 acceptance seed 入口补齐 | `db-seed-counts-latest.csv` |
| FIX-004 | P2 | 实际曾存在 `GET /ws`，不符合 `/api` 规范 | 统一为 `GET /api/ws`，同步 OpenAPI、文档和前端 client 默认路径 | 路由对照 337/337，无非 `/api` 实际路由 |
| FIX-005 | P2 | sandbox proxy 实际 `Any` 超出 OpenAPI 方法集 | 改为 GET/POST/PUT/PATCH/DELETE/HEAD/OPTIONS | 路由对照通过；TRACE 不再命中 proxy |
| FIX-006 | P2 | transfer seed 的 `artifact_ref` 是裸路径，下载授权无法覆盖成功路径 | 改为统一对象引用 `minio://.../{tenant}/transfer/export/{task}/...` | transfer download-grant 返回 `code=0` |
| FIX-007 | P1 | 内容附件声明 MIME 与内容魔数不一致仍可上传 | 在基础层 `internal/platform/upload` 增加通用附件类型校验，并由 M5 通过统一文件服务 `KindValidator` 接入；未知扩展名不走 MIME fallback | `attachment forged mime` 返回 `51012`，trace `433c9ec2-3762-4ac1-8330-853dff15cc92` |
| FIX-008 | P1 | 竞赛可在未来比赛窗口前手动结束 | 在 M8 状态机规则中叠加时间窗口校验，手动 start/freeze/end 必须满足时间约束 | `end already running/future window` 返回 `81003`，trace `e4a297a1-067e-4ca2-a50f-8dd0afea39f0` |
| FIX-009 | P1 | 沙箱启动超时后失败状态回写复用已超时 ctx | 失败回写使用保留 trace/tenant 属性的独立有界 context | 最新功能测试沙箱 `status=4, phase=1`，并记录 `stage=start` error，见 `sandbox-failure-status-full-current.txt` |
| FIX-010 | P2 | NoRoute 返回 Gin 默认空 404，且一度误用路径参数错误码 | 组合根注册 `response.NoRoute`，未知路径和 method mismatch 统一返回 `{code,message,trace_id}`；错误语义统一为已有 `ErrNotFound`/`11005` | `go test ./cmd/server` 通过；`/api/v1/unknown`、`/ws`、`POST /api/healthz` 均返回统一错误信封 |
| FIX-011 | P2 | 本地短信成功路径无法通过 loopback 模拟网关验收 | 在基础层 `netx` 增加 loopback-only HTTP 校验/客户端，并由统一配置 `SMS_HTTP_ALLOW_LOOPBACK` 限制在 local/dev/test；生产路径仍只用公网 client | `POST /api/v1/auth/sms/send` 返回 `code=0`，trace `f1340b4e-8014-4e4e-86a1-fd8a84f30651`；网关日志新增 `/sms` |
| FIX-012 | P1 | notify 批量站内信写入使用 sqlc `:copyfrom`，PostgreSQL RLS 表拒绝 `COPY FROM` | 保留 service 批量业务语义，数据层统一改为 sqlc 参数化 `CreateNotification :exec` 循环写入，不绕过 RLS、不加特权连接 | `run-notify-full.ps1` 26/26 通过；日志未再出现 `COPY FROM`/RLS 写入错误 |
| FIX-013 | P1 | NATS `FlushWithContext` 使用无 deadline context，发布/死信确认失败 | 在基础层 `internal/platform/eventbus` 增加统一配置 `NATS_FLUSH_TIMEOUT_MS` 和单一 `flush` 出口，发布与死信共用并及时 cancel timeout context | `run-notify-full.ps1`、`run-judge-supplement.ps1` 通过；日志未再出现 `context requires a deadline` |
| FIX-014 | P2 | WebSocket 非 upgrade 请求由 gorilla upgrader 直接写空 400，绕过统一错误信封 | 在基础层 `internal/platform/ws` 进入 upgrader 前校验 `websocket.IsWebSocketUpgrade`，由模块 API 继续走 `response.Fail` | `/api/ws`、judge progress、sim stream 非 upgrade 请求均返回统一错误信封 |
| FIX-015 | P1 | `judge.completed/failed` 是公共主题，teaching/experiment/contest 会把其他模块 `source_ref` 当坏事件投死信 | 各模块消费端先校验 `source_ref` 格式，合法但非本模块前缀直接忽略；非法 payload 仍返回模块错误，不吞真实坏消息 | `run-judge-supplement.ps1` 10/10 通过；日志未出现相关死信放大错误 |
| FIX-016 | P2 | acceptance seed 中 judge/sim/content 部分字段仍是旧契约或裸对象路径 | 通过既有 acceptance seed 入口修正为当前合同：MinIO 对象引用、`judge_config` 敏感字段、当前 sim interaction schema、关键 seed 行补齐 | `reset-local` 通过；关键种子计数已刷新 |
| FIX-017 | P2 | 部署配置仍残留 `WS_PATH=/ws` 和 Ingress `/ws` 入口 | 统一 `deploy/config/chaimir.env` 为 `/api/ws`，删除 Ingress 单独 `/ws` 路径，`/api` 前缀承载控制面和 WS | `kubectl kustomize` local-dev/local-deps/staging/prod-school/prod-saas 均通过 |
| FIX-018 | P2 | acceptance seed 中仿真包 bundle 对象引用仍使用旧目录前缀 | 通过既有 acceptance seed 入口改为统一文件服务目录 `sim/package-bundle/...`，不在测试脚本中伪造授权 | `reset-local` 后学生获取已发布 seed 包 bundle 授权返回 `code=0` |
| FIX-019 | P1 | M4 动态 validation-report 合并时会把上传阶段生成的静态安全字段覆盖为空，导致真实审批失败 | 在 `MergeValidationReport` 仓储边界只序列化动态字段 `determinism_check`、`worker_preview`、`details`；保留上传流程生成的 `bundle_hash`、`metadata_validation`、`static_scan` | `run-sim-full.ps1` 38/38 通过；审批、bundle 授权、归档、重新发布链路恢复 |
| FIX-020 | P2 | 启动失败且未产生代码哈希的分配阶段沙箱，后台回收不应再尝试保存不存在的工作区 | 回收决策统一在 `shouldPersistBeforeRecycle`，分配阶段且无 `code_hash` 时跳过工作区保存，继续销毁 K8s 资源并发布回收事件 | 真实 K8s 启动超时沙箱经后台周期后 `status=5`，仅有 `start` 错误和正常 `recycle` 事件 |
| FIX-021 | P2 | acceptance seed 伪造 `Ready/FullyReady` 沙箱，但本地 K8s 中没有对应 namespace/pod，后台回收会把假资源当活资源执行保存 | 将 seed 沙箱改为历史销毁态，保留详情/权限/审计查询能力；对象 key/hash 改成统一格式，不修改回收器的真实保存失败语义 | `reset-local` 后 seed 沙箱 `status=5, phase=4`；模块烟测 65/65 通过；等待一个回收周期后日志无 `sandbox scheduled recycle failed` |
| FIX-022 | P2 | `reset-local.ps1` 规定只加载 `backend/.env` 和 `deploy/config/secret.env`，但本地 `backend/.env` 缺少新增必填项 `NATS_FLUSH_TIMEOUT_MS` 和本地 HTTP 短信网关配置，导致 reset 阶段配置校验失败 | 在已忽略的本地 `backend/.env` 补齐非密钥配置：NATS flush 超时、本地短信网关 URL、loopback 开关和模板；真实 `SMS_HTTP_TOKEN` 仍只来自已忽略的 `secret.env` | `scripts\db\reset-local.ps1` 于 2026-06-19 22:25 成功完成，输出 `local database reset completed` |
| FIX-023 | P1 | M8 漏洞题 finalize 调 M5 `SystemImportContent` 时直接沿用普通用户 ctx，且导入版本为不符合 M5 semver 规则的 `v1`；已固化题重复 finalize 会再次尝试导入 | 进程内特权跨模块契约统一使用基础层 `auth.WithServiceIdentity`，M8 只在草稿态执行 M5 系统导入，已固化态幂等返回；导入版本统一为 `1.0.0` | `finalize seed prevalidated vuln` 返回 `code=0`；`run-contest-full.ps1` 71/71 通过；`go test ./...` 与 `go build ./cmd/server ./cmd/migrate` 通过 |
| FIX-024 | P2 | contest 完整脚本用 flag/明文对象测试 J1 `solidity-unit` 题型，导致假阴性；真实契约要求代码类判题提交 `minio://` 归档对象和 SHA256 | 测试脚本改为使用真实 MinIO 安全 tar 代码归档、`code_storage_key` 和 `code_hash`，不绕过 M3 统一归档安全层 | `lifecycle submit while running` 返回 `code=0`，DB 写入 `solve_submission` 和 `judge_task_ref`；contest 模块 71/71 通过 |
| FIX-025 | P1 | 并发 service-auth 创建沙箱后立即按 `source_ref` 回收时，异步启动协程仍可能在回收后写 `Failed/Ready/Running`，覆盖 `Destroyed` 终态；根因是启动路径直接 `UpdateSandboxPhaseStatus`，没有重读当前状态并判断回收链路是否已接管 | M2 启动路径统一经 `advanceStartupState` 从预期前置状态推进；`markStartFailed/markInitFailed` 在事务内重读当前状态，若已 `Recycling/Destroyed` 则不再写失败事件或广播；错误链用 `.WithCause(err)` 保留原始 cause | 复现接口：`POST /api/v1/sandbox/sandboxes` 并发 3 次后 `POST /api/v1/sandbox/sandboxes/recycle` 立即回收；期望 3 个沙箱均 `status=5` 且无 recycle 后 error；修复后 `run-concurrency-supplement.ps1` 10/10 通过，trace `concurrency-sandbox-f9890dd8-1411-4a19-9073-e81ed05ac692`/`concurrency-ftest-89c522a3-90ee-4b29-9e91-00ac18fe77f0`；DB/outbox/K8s 证据见 `sandbox-race-fixed-*.txt`；`go test ./...` 通过 |

## 5. 数据库与副作用

最新重置后的关键种子计数见 `.tmp/backend-functional-test/evidence/db-seed-counts-latest.csv`：tenant 1、account 6、account_role 7、auth_session 5、content_item/content_body 各 3、course 1、chapter 2、assignment 1、experiment/instance/group 各 1、runtime 1、sandbox/tool/event 各 1、judger 1、judge_task/result 各 1、sim_package/session 各 1、contest/problem 各 1、notification 1、course_grade 1、student_semester_grade 1、transfer_task 1、audit_log 5。

RLS 检查见 `.tmp/backend-functional-test/evidence/db-rls-check-latest.csv`：设置 `app.tenant_id=910000000000000001` 可见本租户数据；设置 `app.tenant_id=920000000000000001` 后 `content_item`、`sandbox`、`account` 计数均为 0，直接按 `tenant_id=910...` 查询也为 0。

MinIO 副作用见 `.tmp/backend-functional-test/evidence/minio-attachments-after-negative-full-current.json`。历史修复前曾写入的 `forged.jpg/eicar.txt` 仍是对象存储残留；本轮使用唯一文件名 `forged-1781869197.jpg`、`eicar-1781869197.txt` 复测，接口返回 `51012`，且 MinIO 中匹配数为 0，见 `.tmp/backend-functional-test/evidence/minio-negative-absence-full-current.json`。标准 EICAR 上传被 ClamAV 拦截，直连 ClamAV 也命中 `Eicar-Test-Signature`。

## 6. 功能测试结果

| 模块 | 成功路径 | 失败/权限路径 | 数据库/依赖副作用 | 结论 |
| --- | --- | --- | --- | --- |
| identity | 平台/学校/教师/学生登录、me、session、refresh、短信本地网关发送、教师账号启用/禁用/归档/恢复、强制改密 gate、组织挂靠校验 | 未登录、学生访问账号列表、logout 后 token、过期/篡改 token、pending 账号非法状态流转、教师挂班级/学生挂院系被拒 | auth_session 写入/吊销；审计可查；短信网关日志可查 | smoke 8/8，补测 19/19，通过 |
| content | 列表、教师题面、版本、分类、附件上传、下载授权、手动试卷创建/详情 | 学生禁止 full、学生直连 content item route 被拒、非法 object_ref、超大文件、伪造 MIME、EICAR、手动试卷 regenerate 被拒 | MinIO 对象、audit_log、ClamAV；题面响应本地扫描未含 `flag/judge_config/answer` | smoke 6/6，补测 8/8；学生直连 M5 题面入口仍列为 P2-004 |
| teaching | 课程、章节、作业、提交、教师提交列表、教师批改、成绩权重 | 学生批改被拒、非法发布状态、空提交、重复成绩来源、学生列出全量提交被拒 | submission、course_grade/outbox 相关副作用可见 | smoke 7/7，补测 7/7，通过 |
| experiment | 实验列表、实例详情、进度 | 非 owner pause、非法 stage | instance/group/report 种子可见 | 主路径通过 |
| sandbox | runtime/tool/detail/quota、service 创建、学生读本人 sandbox、service recycle、非 owner 拒绝、TRACE 不命中 proxy | 非 owner 拒绝、unsupported method、学生改 quota 被拒、销毁态文件保存/列表被拒、runtime selftest 真实失败有统一信封 | K8s 创建后超时可落失败态；后台周期可销毁失败沙箱并发布 recycle outbox | smoke 6/6，补测 9/9；Ready 成功路径仍列为 P2-005 |
| judge | judger、task 列表/详情 | 学生 manual score 拒绝、fingerprint 内部鉴权拒绝 | judge_task/result 种子可见 | 主路径通过 |
| sim | package、version、上传、审核、bundle、session、action、checkpoint、replay、share、stream、recycle | 学生 reviews 拒绝、非法动态报告拒绝、已归档包创建会话拒绝 | sim_session/share/checkpoint/review/package 状态副作用可见 | 38/38 通过 |
| contest | 列表、创建/更新/发布、报名、锁队、开始/结束/归档、题目、榜单、提交、提交详情、作弊记录、防作弊查重、漏洞源/漏洞题导入/预验证拒绝/finalize、对抗赛建赛/组队/参战入口、内部 stats/achievements | 未登录、学生管理拒绝、跨角色队伍读取拒绝、缺题发布拒绝、未来窗口 start/end 拒绝、非运行态提交/env/参战拒绝、重复提交限频、非法 code_hash、loopback 漏洞源拒绝、未预验证漏洞题 finalize 拒绝、内部接口无签名拒绝 | contest/problem/team/team_member/solve_submission/judge_task_ref/cheat_record/vuln_problem/result_snapshot/audit_log；生命周期提交使用真实 MinIO tar 代码对象并进入 M3 判题任务 | 完整脚本 71/71，通过；证据 `.tmp/backend-functional-test/evidence/contest-full-results-current.json` |
| notify | inbox、unread、announcement、mark read、批量站内信 | 权限专项部分覆盖 | notification read_at 更新；RLS 下批量写入通过 | 专项 26/26，通过 |
| grade | level config、semester、review submit/reject/approve/unlock、学生成绩/GPA、recompute、appeal、warning rules/list/scan、transcript/batch/download | 学生创建配置/学期/审核/重算/预警扫描被拒、学生读他人成绩/预警被拒、非法 review/appeal 状态机、非法 transcript scope、教师下载成绩单被拒 | grade_review、grade_appeal、academic_warning、student_semester_grade、transcript_record、MinIO PDF 授权 | smoke 5/5，补测 45/45，通过 |
| admin | platform/school dashboard、statistics、tenant/application、monitoring、backups、audit/query/export、config create/update/history/rollback、alert rule、alert events list | 学生 dashboard/audit/alert 被拒、学校访问平台 tenant/backups 被拒、平台租户 config/alert 被拒、统计非法日期、audit 反向时间、config stale version、已处理告警重复处理被拒 | audit export 创建 transfer task；system_config 脱敏；config_change_log；alert_rule；alert_event 状态机负向 | smoke 4/4，补测 35/35；pending 告警生成链路列为 P2-006 |
| transfer | task list/detail/download grant、审计导出 task 查询 | 学生读取学校管理员导出 task 被拒 | transfer_task 成功授权 | smoke 3/3，补测 4/4，通过 |
| health/readiness | `/api/healthz`、`/api/readyz` | unknown route 统一错误信封 | 依赖探活正常 | 通过 |

模块烟测最新结果见 `.tmp/backend-functional-test/evidence/module-smoke-v2-summary-full-current.json`：identity 8/8、content 6/6、teaching 7/7、experiment 5/5、sandbox 6/6、judge 5/5、sim 5/5、contest 6/6、notify 5/5、grade 5/5、admin 4/4、transfer 3/3、security 3/3。专项结果：notify 26/26、judge 10/10、sim 38/38，非实验/非竞赛补测 47/47，grade/admin 补测 80/80，experiment 48/48，contest 71/71。功能流专项见 `.tmp/backend-functional-test/evidence/functional-flows-results.json`，逐项 `ok=true`。通知专项脚本已在 `.tmp` 测试产物中补齐 token 刷新前置,避免 logout 安全流污染后续测试。最终后端验证见 `.tmp/backend-functional-test/evidence/final-go-test-2026-06-19.txt` 和 `.tmp/backend-functional-test/evidence/final-go-build-2026-06-19.txt`。

## 7. 问题清单

### 已关闭问题

| 编号 | 原级别 | 关闭证据 |
| --- | --- | --- |
| P1-001 内容附件上传缺少通用类型/魔数校验 | P1 | `attachment forged mime` 返回 `51012`；标准 EICAR 返回 `51012`；唯一负向文件未写入 MinIO；`go test ./...` 通过 |
| P1-002 竞赛可在未来比赛窗口前被提前结束 | P1 | `POST /api/v1/contest/contests/910000000000008001/end` 返回 `81003`；数据库不再被提前改为 ended |
| P1-003 沙箱真实 K8s 启动超时后数据库状态未正确落失败态 | P1 | 最新功能测试沙箱 `status=4, phase=1`，并记录 `stage=start` error |
| P1-004 沙箱异步启动与来源级联回收竞态覆盖终态 | P1 | 最新并发补测 `run-concurrency-supplement.ps1` 10/10 通过；3 个并发沙箱 `193761777094234112/193761777366863872/193761777878568960` 均为 `status=5`，事件仅有 `create/recycle`，回收 outbox `status=3`，K8s namespace 无残留 |
| P2-001 未匹配路由不返回统一错误结构 | P2 | `/api/v1/unknown`、`/ws`、`POST /api/healthz` 均返回 `{code,message,trace_id}` 且 code 为 `11005` |
| P2-002 本地模拟短信网关无法完成成功路径 | P2 | `POST /api/v1/auth/sms/send` 返回 `code=0`；本地网关日志新增 `/sms` |
| P2-003 沙箱后台回收链路仍有失败事件 | P2 | 真实 K8s 启动超时沙箱：后台回收周期后 `status=5`，事件为 `start` error + `recycle`，outbox `status=3`；seed 假 Ready 沙箱已改历史销毁态，一个回收周期后日志无 `sandbox scheduled recycle failed` |

### 仍需确认/修复

| 编号 | 级别 | 复现步骤 | 请求 | 期望结果 | 实际结果 | trace_id/证据 | 是否阻塞 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P2-004 | P2 | 学生 token 直接读取 M5 公开题面接口 | `GET /api/v1/content/items/lab-reentrancy-foundry/1.0.0` | 若 M5 文档/总验收要求“学生取题面接口”由该路由承担，应返回已脱敏题面；或文档明确学生只能经 M6/M7/M8 业务入口间接取题面 | 当前路由分组只允许教师/学校管理员；教师读取题面已验证不含 `flag/judge_config/answer`，学生直连返回无权限 | `.tmp/backend-functional-test/evidence/non-exp-contest-supplement-results-current.json` | 不阻塞联调，但阻塞“学生直连 M5 题面”验收结论 |
| P2-006 | P2 | 查找 M9 告警事件生成链路并尝试经公开/受控入口生成 pending `alert_event` | N/A；HTTP 仅有 `GET /alert-events` 与 `POST /alert-events/{id}/handle` | 文档所述“业务指标周期检测 -> 生成 alert_event -> M10 通知管理员”应有后台任务或受控入口可产生 pending 事件，供处理成功路径验收 | 代码中仅发现 repo 层 `CreateAlertEvent`，未发现服务/后台任务调用；acceptance seed 事件为已处理态，只能覆盖查询和重复处理拒绝 | `rg "CreateAlertEvent|alert_event" backend/internal backend/cmd -n`；`.tmp/backend-functional-test/evidence/grade-admin-supplement-results-current.json` | 不阻塞普通联调；阻塞 M9 告警成功链路验收 |

## 8. 未完全覆盖项

| 范围 | 未测原因 |
| --- | --- |
| 337 个路由的逐接口完整状态矩阵 | 已完成路由存在性对照和模块级主流程/权限/安全专项；尚未对每个路由逐一覆盖成功、参数错误、权限不足、跨租户、状态机非法流转 |
| SMS 厂商真实发送 | 当前只验证本地 loopback 模拟网关，未接真实短信厂商 |
| WebSocket 工具代理生产拓扑 | 终端 PTY 已通过；本地 Windows 后端仍不能解析集群内 `*.svc.cluster.local`，用户侧工具代理需在集群内 backend Pod 拓扑复测 |
| 并发压力矩阵 | 已补测并发报名、提交、service-auth 沙箱创建后立即回收；尚未做大规模压力、跨租户并发和长时间稳定性矩阵 |
| NATS/outbox 完整消费链路 | 已验证相关表存在和模块查询；未完整追踪每类事件从发布到消费 |
| 全量工具供应链与组合编排 | 13 个工具已做本地镜像层自测，但 11 个工具缺 Harbor digest/Cosign/Trivy 证明，不能进入 acceptance seed；Fabric Explorer 仍需 Fabric + PostgreSQL 组合编排联测 |
| M9 业务告警 pending 生成成功路径 | HTTP 层没有手工创建入口；当前未发现后台检测任务调用 `CreateAlertEvent`，seed 事件为已处理态，只能验证列表和重复处理拒绝 |

## 9. 结论

可以进入受控前后端联调：identity、content、teaching、experiment、judge、sim、contest、notify、grade、admin、transfer、health/readiness 的 smoke、模块补测与关键安全专项当前通过；短信本地成功路径已具备，但仅限 local/dev/test 显式开关。

不建议进入生产镜像部署测试。当前主要剩余风险是 337 个路由尚未完成逐接口状态矩阵、长时间并发幂等矩阵、工具 API 代理集群内拓扑复测、全部工具的 digest/Cosign/Trivy 供应链证明和组合编排验证。

后续顺序建议：先补集群内 backend Pod 拓扑的工具代理验收和 11 个工具镜像证明，再补 337 路由逐接口矩阵、长时间并发、NATS/outbox 全链路追踪；实验、竞赛继续按真实用户路径做完整矩阵。

## 10. 2026-06-20 续测：实验/竞赛/沙箱真实 K8s 闭环

本轮在 `E:\code\Chaimir` 接续上一轮中断测试，重点核验实验、竞赛、作业/判题、沙箱和仿真链路。证据仍只写入 `.tmp/backend-functional-test/`；未提交 `.tmp`、`backend/.env`、`deploy/config/secret.env`，未打印密钥值。

### 10.1 测试环境

| 项目 | 结果 |
| --- | --- |
| 当前 commit | `20e459f65c219916d8eb4f844417147706dd3322` |
| 后端启动命令 | `.tmp\backend-functional-test\run-server.ps1 -BackendEnv .tmp\backend-functional-test\env\backend-test.env -SecretEnv .tmp\backend-functional-test\env\secret-runtime-final.env`，内部执行 `go run ./cmd/server` |
| 后端地址 | `http://127.0.0.1:18081` |
| 数据库重置/种子命令 | `go run ./cmd/migrate reset-local`，加载 `backend-test.env` 和 `.tmp/backend-functional-test/env/secret-runtime-final.env`，证据 `reset-local-continue-after-sa-2026-06-20.txt` |
| 健康检查 | `/api/healthz`、`/api/readyz` 均 200，响应统一 `{code,message,data,trace_id}`，证据 `health-readiness-continue-after-sa-2026-06-20.json` |
| K8s 控制面 | `kube-apiserver`、`kube-controller-manager`、`kube-scheduler` 均 Running；曾因旧 `sbx-*` 残留和控制面 leader election 超时短暂阻塞，已记录并清理 |
| 数据依赖 Pod | `clamav-0`、`minio-0`、`nats-0`、`postgres-0`、`redis-0` 均 Running |
| 本地端口 | `15432`、`16379`、`14222`、`19000`、`13310` 均 Listen；`18081` 为后端服务 |
| 沙箱启动阈值 | `SANDBOX_READY_TIMEOUT_SECONDS=30`；未放宽为 300，避免掩盖文档要求的启动性能问题 |

### 10.2 本轮修复

| 编号 | 级别 | 问题 | 根因 | 修复 | 验证 |
| --- | --- | --- | --- | --- | --- |
| FIX-026 | P1 | acceptance seed 写入 `:2026.06` tag 镜像，沙箱镜像证明规则要求 `@sha256` digest | seed 与 M2 `imageAttested` 规则不一致，tag 永远无法满足不可变镜像证明 | `backend/cmd/migrate/acceptance_seed_rows.go` 改为从 `SANDBOX_IMAGE_ATTESTATIONS_JSON` 选择已 cosign/trivy 通过的 digest 地址，不保留 tag fallback | `runtime_image` 写入 digest；预拉取接口返回 5/5；证据 `runtime-image-prepull-db-continue-after-sa-2026-06-20.txt` |
| FIX-027 | P1 | 预拉取 DaemonSet 目标命名空间 `chaimir-prepull` 缺失 | 部署静态命名空间只声明 system/data/monitoring，与 M2 预拉取设计不一致 | `deploy/base/namespaces/namespaces.yaml` 增加 `chaimir-prepull`，PodSecurity 为 restricted | 五套 overlay 均渲染成功；真实 DaemonSet `chaimir-prepull-910000000000001002` 5/5 Ready |
| FIX-028 | P1 | 动态沙箱命名空间创建后立即建 Pod，曾出现 `serviceaccount "default" not found` 竞态 | 依赖 Kubernetes 默认 SA 异步创建，不符合生产可控安全边界 | M2 K8s 编排统一创建显式 `sandbox-workload` ServiceAccount，`automountServiceAccountToken=false`，无 RoleBinding；运行时 Pod 和工具 Pod 均使用该 SA | 30 秒真实沙箱诊断通过；Pod YAML 显示 `serviceAccountName: sandbox-workload` 和 `automountServiceAccountToken=false`；ServiceAccount 证据 `real-sandbox-30s-serviceaccount-after-recycle-current.yaml` |
| FIX-029 | P1 | 用户侧工具代理按旧规则访问 `tool-{toolCode}`，真实 K8s Service 是声明式 `resource_spec.routes[].service` | 代理入口没有复用工具 registry 的声明式 route/service/port，产生旧命名假设 | `backend/internal/modules/sandbox/api.go` 统一由 `resource_spec.routes` 解析 Service 和端口，不保留旧命名兼容 | 本地单测覆盖 `code-server -> tool-code-server-web:8080`；K8s 直连证据显示 `code-server`、`blockscout` 对应 Service 均可 HTTP 200 |
| FIX-030 | P1 | 工具代理使用 `httputil.NewSingleHostReverseProxy` 后再设置 `Rewrite`，Go 运行时报 `ReverseProxy must have exactly one of Director or Rewrite set` | 新旧反向代理机制混用 | 改为显式构造只含 `Rewrite` 的 `httputil.ReverseProxy`，并继续清理 Authorization/Cookie/service-auth 等敏感头 | 本地单测确认 `Director == nil` 且 `Rewrite != nil`；本地后端不再出现 Director/Rewrite 混用错误 |
| FIX-031 | P1 | 预拉取接口只拉取 `runtime_image.image_url`，不能保证用户进入实验时工具/infra 镜像已在节点缓存 | `runtime_image.prepull_status=2` 的语义没有覆盖默认工作负载镜像集合，和 M2 秒进设计不一致 | `PrepullRuntimeImage` 统一收集 runtime 主镜像、声明式 infra 镜像和按生态匹配的工具镜像；每个镜像都必须命中 `SANDBOX_IMAGE_ATTESTATIONS_JSON`，K8s DaemonSet 为每个镜像创建一个 `IfNotPresent` 容器，`prepull_detail` 记录 `image_count/images` | 本地当前证明清单只含 code-server/blockscout，所以实测 `image_count=4`；DaemonSet 4 容器 5/5 Ready，DB `prepull_status=2`，证据 `sandbox-prepull-set-*.json/txt` |
| FIX-032 | P1 | acceptance seed 只硬编码 code-server/blockscout 两个工具，无法覆盖 `images/tool` 中完整工具集，也会保留工具专用分支 | seed 没有把镜像 manifest 作为工具定义单一来源，导致文档、镜像层、数据库三套口径 | 新增 `acceptance_seed_tools.go` 从 `images/tool/*/manifest.yaml` 严格生成 `tool` 表；工具种子先清空 dev/test 旧 `sandbox_tool/tool`，再按 manifest 重建；命令工具使用 `tool.kind=4 command-tool` 和 `components[].name=command`，不保留 `cli` 类型或组件名 | `go test ./cmd/migrate -run TestAcceptanceToolDefinitionsFromManifests -count=1` 通过，验证 13 个工具 manifest、3 个 command-tool、无 Service/route |
| FIX-033 | P1 | 命令工具会创建 Pod，但租户单沙箱资源估算只统计 web-embed 工具；同时后端已有命令工具路由但 OpenAPI 和前端 client 未同步 | 命令工具未纳入统一 WorkloadSpec 资源估算和契约发布，存在配额绕过和前后端两套路由风险 | M2 资源估算统计所有会创建 Pod 的工具；新增用户路由 `POST /sandboxes/{id}/command-tools/{tool_code}/run`，只允许 owner 调用 Ready 的 command-tool，通过基础层 `internal/platform/k8s.Exec` 执行白名单 argv；OpenAPI、总 API、M2 文档和 `frontend/packages/api-client` 同步 | `go test ./internal/modules/sandbox -count=1` 通过；路由对照重新生成 338/338，新增命令工具路由 OpenAPI/实际/总览均 match |
| FIX-034 | P1 | 多个工具的后台 readiness 串行等待，慢工具会拖长其他工具入口解灰时间 | `updateToolReadiness` 按工具顺序逐个等待 `ToolReady`，与阶段二后台并行初始化目标不一致 | 保持阶段一只等待运行时 Pod Ready，不假 Ready；阶段二工具 readiness 改为并行检查，每个工具仍真实探测并分别写 Ready/Failed | `go test ./internal/modules/sandbox -run TestRunToolReadinessChecksRunsToolsConcurrently -count=1` 和 `go test ./...` 通过；该修复只降低工具解灰等待，不等于 20.3s SLA 已关闭 |
| FIX-035 | P1 | 终端安全策略已有容器标签校验，但验收运行时没有 `student-shell` sidecar，也没有 `terminal` 工具行，用户实际拿不到合规终端 | seed 只挂 Web 工具；运行时主容器因含运行态数据正确标记为不可进入，但缺少独立学生入口容器 | acceptance runtime 新增 `student-shell` sidecar，复用 digest 锁定运行时镜像，只挂学生可访问卷；工具 seed 保留 13 个 manifest 镜像工具并额外写入无镜像 `kind=terminal` 平台工具；默认工具包含 `terminal` | `go test ./cmd/migrate -run "TestAcceptanceSeedToolDefinitionsIncludeTerminal|TestAcceptanceRuntimeAdapterSpecDeclaresStudentShell" -count=1`、`go test ./internal/modules/sandbox -run TestStudentShellTerminalDoesNotMountPrivateDomains -count=1` 和 `go test ./...` 通过；真实 WebSocket PTY 尚待 K8s 端到端验收 |
| FIX-036 | P1 | 本地 13 个工具镜像存在,但多项镜像不符合非 root/端口/工作目录运行契约: Remix 非 root nginx 写 `/var` 失败, noVNC 上游启动脚本依赖 root 和 80 端口, Fabric Explorer 被错误工作目录引导到 `/workspace` | 镜像薄封装没有把上游 root 假设和应用目录转换为平台统一 WorkloadSpec 契约 | 修复 `images/tool/remix` 非 root nginx 临时目录和 8080 入口;修复 `images/tool/novnc-desktop` supervisor/nginx pid/socket/log/temp 路径、非特权 8080、预渲染用户配置;修复 `images/tool/fabric-explorer` 工作目录为 `/opt/explorer` 并授权应用/缓存目录;不改后端兼容旧镜像 | 本地重建后的 13 个工具镜像已发布到 Harbor 命名空间并统一 `Config.User=1000:1000`; `wallet-sim` 签名、`contract-ui` ABI、`gas-profiler` 统计、`slither` 扫描、`mythril` 分析、`remix` 只读根文件系统 HTTP 200、`jupyter` XSRF 写 workspace、`theia/code-server/db-viewer/novnc` HTTP 200 均有 `.tmp/backend-functional-test/local-tool-images/*` 证据;Fabric Explorer 单容器因缺 Fabric/PostgreSQL 编排仍需 K8s 组合联测 |
| FIX-037 | P1 | 加入合规 `student-shell` 后，当前沙箱工作负载资源合计超过旧单沙箱 ResourceQuota，导致正确终端方案可能被配额误拒 | 旧 `SANDBOX_MAX_CPU=6`/`SANDBOX_MAX_MEMORY=8Gi` 只覆盖运行时加两个 Web 工具，没有把学生入口 sidecar 纳入同一 WorkloadSpec 预算 | 统一调整环境示例和部署配置为 `SANDBOX_MAX_CPU=7`、`SANDBOX_MAX_MEMORY=9Gi`，保持 K8s ResourceQuota 严格生效，不放宽租户总配额、不删工具、不假 Ready | 两阶段真实沙箱 ResourceQuota 显示 `limits.cpu 6250m/7`、`limits.memory 8448Mi/9Gi`；Pod YAML 证明 `student-shell` 只挂 `/workspace`，不挂 `/runtime-state`/Secret；证据 `sandbox-two-phase-k8s-current.txt`、`sandbox-two-phase-runtime-pod-current.yaml` |
| FIX-038 | P2 | 终端 WebSocket 已升级后，客户端正常断开或业务流结束仍把错误返回 Gin handler，可能对已 hijack 连接写 JSON 并误记为工具代理失败 | WebSocket 升级成功后 HTTP 响应权已交给 WebSocket，后续错误不能再按普通 HTTP 错误信封写回 | 在共享基础层 `internal/platform/ws.ServeInteractive` 统一处理：升级前错误仍返回给 handler；升级后清理连接并返回 nil，避免模块内兼容和重复处理 | 终端默认进入 `student-shell` 返回 101 并执行 `pwd`；显式访问主运行时容器仍被 `22023` 拒绝；修复后日志未再出现 `hijacked`/`response.Write on hijacked`，证据 `sandbox-two-phase-startup-2026-06-20.txt`、`backend-server-after-ws-fix.out.log` |
| FIX-039 | P1 | 用户侧 Web 工具代理在集群内拓扑下必须支持真实浏览器资源加载，不能只验证带 Authorization 的单次 HTTP 200 | iframe/code-server 静态资源和上游重定向不会自动携带 Authorization；旧本地后端 23003 是 Windows 进程无法解析 `*.svc.cluster.local` 的拓扑限制，不能在 M2 写本地兼容分支 | 后端临时部署到 `chaimir-system` 后按生产拓扑复测；认证统一收敛到 `internal/platform/auth.BrowserAccessMiddleware`，首次 `?token` 只用于平台代理鉴权并换成工具路径受限 HttpOnly Cookie；反向代理通用头清理、Location 重写和 Cookie 作用域收敛到现有 `internal/platform/httpx.NewPrefixReverseProxy`，sandbox 只保留 owner/租户校验和工具目标解析 | 集群内 `code-server` 代理返回真实 HTML、`blockscout /api/v2/stats` 返回链数据 JSON，二者 `cookie_count=1`；脚本已把平台错误 JSON 当失败处理，避免 HTTP 200 包 `11001` 的假阳性；证据 `sandbox-cluster-proxy-browser-auth-2026-06-20.txt`、`sandbox-cluster-proxy-summary-current.json` |
| FIX-040 | P1 | 集群内 backend 需要创建动态沙箱 namespace 内的显式 `sandbox-workload` ServiceAccount，RBAC 缺少 serviceaccounts 权限会阻断真实沙箱创建 | M2 已统一创建非自动挂载 token 的显式 SA，但部署层 ClusterRole 未覆盖该资源，属于部署契约遗漏 | `deploy/base/rbac/backend-sa.yaml` 的 `chaimir-backend-sandbox-workload` ClusterRole 增加 `serviceaccounts` create/delete/get/list/watch，不改业务逻辑、不回退到默认 SA | 集群内沙箱 Namespace 包含 `serviceaccount/sandbox-workload`，Pod 使用显式 SA 且 `automountServiceAccountToken=false`；证据 `sandbox-cluster-proxy-rbac-2026-06-20.txt`、`sandbox-cluster-proxy-k8s-current.txt`、`sandbox-cluster-proxy-runtime-pod-current.yaml` |
| FIX-041 | P1 | 启用快照的沙箱回收存在卷残留风险，且 RBAC 缺少快照能力检测/ready 校验所需只读权限 | M2 会读取 `StorageClass`、`VolumeSnapshotClass`、`VolumeSnapshotContent`，但部署 RBAC 未授予对应只读权限；普通回收发起 Namespace 删除后未等待删除完成；快照保留阶段只删 Pod，Service/NetworkPolicy/Quota/SA 等非数据资源会继续留在保留 Namespace 内 | 统一修 M2 K8s 编排语义：普通销毁删除并等待 Namespace 消失后才进入 destroyed；快照保留阶段只保留 PVC/VolumeSnapshot/Namespace，立即清理 Pod/Service/ConfigMap/Secret/NetworkPolicy/ResourceQuota/LimitRange/ServiceAccount；快照能力检测要求 `VolumeSnapshotClass.deletionPolicy=Delete`；RBAC 只补 `storageclasses`、`volumesnapshotclasses`、`volumesnapshotcontents` 只读权限，不给快照内容写权限 | `.tmp/backend-functional-test/evidence/assert-sandbox-k8s-recycle-contract.ps1` 先红后绿；`go test ./...`、`go build ./cmd/server ./cmd/migrate`、5 个 Kustomize overlay 均通过；`kubectl auth can-i` 对 storageclasses/volumesnapshotclasses/volumesnapshotcontents/volumesnapshots 返回 yes；当前集群无 `sbx-*`/`judge-*`/`battle-*` PVC 或 VolumeSnapshot 残留 |
| FIX-042 | P1 | 全量工具无法进入生产 K8s 验收时，根因不能靠本地 `:dev` 镜像或 pull 脚本 fallback 掩盖 | `images/pull-images.ps1` 已正确只按 manifest + `image-digests.lock` 拉取 digest，但 CI 只上传分散的 `image-digest-*` 片段，未产出完整锁文件 artifact，导致离线包/校内 Harbor 预热缺少统一可消费入口 | 在现有 `.github/workflows/images.yml` 增加 `merge-digest-lock` 作业：所有镜像完成构建、Trivy 阻断扫描、推送 Harbor、Cosign 签名并解析真实 digest 后，下载锁片段、去 CR/空行、排序、校验重复镜像不同 digest 和 `sha256:<64hex>` 格式，再上传完整 `image-digests-lock` artifact；同步 `images/README.md`，仍禁止 tag fallback 和本地/生产分叉 | `.tmp/backend-functional-test/evidence/image-lock-merge-workflow-test-ps/images/image-digests.lock` 验证 CRLF/空行归一化后仍严格输出 digest 锁；本机仍缺 Harbor 推送、Trivy、Cosign 环境，11 个工具的 K8s 全量真实验收继续保持未关闭 |
| FIX-043 | P1 | `tool/fabric-explorer` manifest 声称只允许工具缓存卷写入，但实际关闭只读根文件系统且未声明 emptyDir 写点 | 薄封装镜像已把工作目录固定为 `/opt/explorer`，但 manifest 仍保留上游服务写根文件系统的例外，和平台“学生工具不写镜像层、不挂工作区、不碰私有域”的统一契约不一致 | `images/tool/fabric-explorer/manifest.yaml` 改为 `read_only_root_filesystem=true`，只声明 `/tmp`、`/opt/explorer/logs`、`/opt/explorer/walletstore`、`/home/node/.npm` 四个工具私有 emptyDir；不添加固定 Fabric 网络、不写本地兼容 profile | 本地按只读根 + tmpfs 挂载复测，Explorer 进入官方启动流程，日志显示失败点为缺 PostgreSQL `127.0.0.1:5432` 和动态 Fabric profile；证据 `fabric-explorer-readonly-contract-current.txt`。Fabric + PostgreSQL + profile 组合编排仍未关闭 |
| FIX-044 | P1 | 一旦 `tool/fabric-explorer` 后续补齐 digest/Cosign/Trivy 证明，旧 seed 会把它按单组件工具误标为可调度 | Fabric Explorer 依赖 PostgreSQL、Fabric connection profile 和证书挂载，属于运行时组合配置工具；镜像证明只证明镜像可信,不能证明该工具在具体运行时已可用 | 在工具 manifest 增加通用字段 `tool.runtime_config_required=true`；`acceptance_seed_tools.go` 读取该字段并把工具状态写为停用 `status=2`，仍保留工具定义和资源规格，不按 code 特判、不删除工具；M2/镜像文档和 README 同步说明 | `go test ./cmd/migrate -run TestAcceptanceToolDefinitionsFromManifests -count=1`、`go test ./internal/modules/sandbox -run "Test.*Tool|Test.*Ephemeral|Test.*Workspace" -count=1` 当前仓库无测试文件但包编译通过；全量 `go test ./...` 待本轮最终验证 |
| FIX-045 | P1 | 本地全量镜像拉取不能为了测试绕过生产的 registry + digest lock 规则，但本机又缺正式 Harbor/Cosign/Trivy 闭环 | 生产脚本缺本地等价发布入口；同时 `service/migrate` 在 Docker Desktop Linux 构建中因 Go 编译并发压力出现 BuildKit EOF，导致本地构建类镜像无法补齐完整 lock | 在现有 `images/pull-images.ps1` 增加显式 `-PublishLocalBuilt` 模式：先校验所有 `platform-built`/`thin-wrapper`/`build-base` 同一 registry 命名下的源镜像存在，再推送到指定 registry，用 `docker buildx imagetools inspect` 读取 registry digest 写临时 lock，清理临时发布 tag 后复用原有 digest 拉取逻辑；Go 服务 Dockerfile 统一使用 BuildKit Go 缓存和 `GOMAXPROCS=2 go build -p=2`，不新增脚本、不写 tag fallback | 2026-06-21 已统一切换到本地 Harbor `harbor.chaimir:30080`，`.tmp/backend-functional-test/evidence/local-registry-image-digests.lock` 为 61 行；`pull-images.ps1 -Scope all -Registry harbor.chaimir:30080` 验证 78 个镜像引用全部可按 digest 拉取，证据 `.tmp/backend-functional-test/evidence/pull-scope-all-final-after-127-clean.log`；Docker 中已清理 `127.0.0.1:5000/*`、旧本地可变调试标签、`retry-clean`、`upload-probe` 和旧 Harbor 本地域名残留标签。该证据只证明真实实验测试镜像前置可用，不等同生产 Trivy/Cosign 准入，也不等同实验功能已通过 |
| FIX-046 | P1 | 本机缺 Trivy/Cosign/Helm 和正式 Harbor 入口，不能靠宿主机临时安装或 `latest` tag 继续实验镜像验收 | 原 Makefile 直接调用宿主机 `helm`，仓库同时存在旧 Harbor 域名，工具链安装、Harbor 域名和密钥注入路径存在本地/生产分叉风险 | 新增容器化供应链入口 `deploy/image-supply-chain.compose.yaml`，Trivy/Cosign/Helm 镜像按 digest 写入 `deploy/config/chaimir.env`；Harbor 官方 Helm values 放入 `deploy/charts/harbor/values.yaml`；`deploy/Makefile` 统一用 Docker Compose 运行工具和 Harbor 安装；供应链密钥改用 `deploy/config/supply-chain.secret.env`，不进入应用 `secret.env`，不注入后端 Pod；Harbor 域名统一为 `harbor.chaimir` | `.tmp/backend-functional-test/evidence/supply-chain-config-contract-test.ps1` 先红后绿；Docker 已按 digest 拉取 `aquasec/trivy@sha256:f5d0e...`、`ghcr.io/sigstore/cosign/cosign@sha256:6bbe...`、`alpine/helm@sha256:ee6f...`，并删除本地 `latest` tag；容器执行 `trivy --version`、`cosign version`、`helm version` 成功。2026-06-21 Harbor 已通过官方 Helm chart 在 `harbor` namespace 启动，入口统一经 ingress-nginx 转发到 `harbor.chaimir:30080`；`deploy/config/supply-chain.kubeconfig`、`deploy/config/supply-chain.secret.env` 和 `deploy/config/cosign/` 均为本地敏感配置，不得提交 |
| FIX-047 | P1 | `SANDBOX_IMAGE_ATTESTATIONS_JSON` 默认配置残留 `sha256:aaaa...` 且声明 `cosign_verified=true`/`trivy_status=passed`，容易把示例误当生产证明 | 示例配置伪造了签名和扫描结论；这会误导真实实验测试和生产镜像准入，违反镜像证明必须由 CI/Harbor 产出的约束 | 后端配置允许 `SANDBOX_IMAGE_ATTESTATIONS_JSON=[]` 作为安全空证明状态；`backend/.env.example` 和 `deploy/config/chaimir.env` 改为空数组，表示没有正式证明时不准入任何沙箱镜像，不用假 digest 兜底 | 该修复只清除假证明风险；本地已完成 78 个镜像 digest 拉取，不等同完成生产 Cosign 签名/Trivy 阻断扫描。正式测试前必须由供应链流程生成真实证明清单后再写入该变量 |
| TEST-001 | P3 | `run-sim-supplement.ps1` 生成的 `sim-package.json` 被后端判定 `manifest:invalid-json` | PowerShell `Set-Content -Encoding UTF8` 写入 BOM；后端严格 JSON 解码正确拒绝 | 仅修 `.tmp` 测试脚本为无 BOM UTF-8 写入；不放宽生产端 manifest 安全校验 | 仿真补充用例从 3/4 变为 9/9；证据 `sim-bundle-analyze-debug-2026-06-20.txt`、`sim-supplement-rerun-nobom-2026-06-20.txt` |

### 10.3 路由覆盖表

2026-06-20 本轮按要求删除 `backend/**/*_test.go` 后,未重跑已通过的非实验模块;仅启动后端并重跑实验专项 `run-experiment-full.ps1`。本轮实验专项结果 48/48 通过,证据为 `.tmp/backend-functional-test/evidence/experiment-full-only-2026-06-20-after-testfile-delete.txt`;数据库副作用证据为 `.tmp/backend-functional-test/evidence/experiment-full-only-db-side-effects-2026-06-20-after-testfile-delete.txt`。

本轮已重新生成 338 路由总矩阵，`routes-summary.json` 显示 OpenAPI 路由 338、实际 Gin 路由 338、OpenAPI-only 0、actual-only 0、非 `/api` 实际路由 0；本轮新增/重点复核的实际路由如下：

| 模块 | method/path | 覆盖点 | 结果 |
| --- | --- | --- | --- |
| health | `GET /api/healthz`、`GET /api/readyz` | 统一信封、依赖 readiness | 通过 |
| sandbox | `POST /api/v1/sandbox/runtimes/910000000000001001/images/910000000000001002/prepull` | digest 镜像预拉取、K8s DaemonSet、DB prepull 状态 | 通过 |
| sandbox | `POST /api/v1/sandbox/sandboxes/{id}/command-tools/{tool_code}/run` | 受控命令工具执行入口、owner 鉴权、白名单 argv、OpenAPI/前端契约同步 | 路由对照通过；真实端到端受限于本地缺少完整工具镜像证明 |
| sandbox | `POST /api/v1/sandbox/sandboxes` | service-auth 真实创建、K8s Namespace/SA/Pod/Service/NetworkPolicy/ResourceQuota | 通过 |
| sandbox | `POST /api/v1/sandbox/sandboxes/recycle` | 按 `source_ref` 回收、DB destroyed、K8s namespace 清空 | 通过 |
| experiment | `run-experiment-full.ps1` 覆盖的 M7 路由 | 创建、编排、发布、会话、成员、角色、环境、报告/成绩链路 | 48/48 |
| contest | `run-contest-full.ps1` 覆盖的 M8 路由 | 创建、发布、报名、提交、榜单、封榜、对抗赛、漏洞源/题 finalize | 71/71 |
| judge | `run-judge-supplement.ps1` 覆盖的 M3 路由 | J1/J3/J6 相关任务、重判、批量重判拒绝、隐藏结果脱敏/鉴权 | 10/10 |
| sim | `run-sim-full.ps1`、`run-sim-supplement.ps1` | package、validation-report、approve、session、action、replay/share、bundle 授权 | 38/38 + 9/9 |
| grade/admin | `run-grade-admin-supplement.ps1` | 成绩、预警、申诉、配置、审计、告警处理 | 80/80 |
| 横切 | `run-concurrency-supplement.ps1`、`run-non-exp-contest-supplement.ps1` | 并发报名/提交/沙箱创建回收、未登录/越权/跨模块安全专项 | 10/10 + 47/47 |

### 10.4 功能测试表

| 范围 | 结果 | 关键证据 |
| --- | --- | --- |
| 沙箱预拉取 | 通过；接口返回 `desired_nodes=5`、`ready_nodes=5` | `sandbox-prepull-continue-after-sa-2026-06-20.json`、`runtime-image-prepull-k8s-continue-after-sa-2026-06-20.txt` |
| 真实沙箱启动 | 按两阶段启动验收通过；最新集群内代理复测阶段一 9.323 秒进入 `phase=2/status=7`，用户可进入环境，`code-server` 已 Ready，`blockscout` 仍为初始化中；阶段二后台继续真实探测，8.230 秒后全部 Web 工具 Ready。未通过假 Ready、删工具或放宽超时实现 | `sandbox-cluster-proxy-browser-auth-2026-06-20.txt`、`sandbox-cluster-proxy-summary-current.json`、`sandbox-cluster-proxy-k8s-current.txt` |
| 实验工具镜像与 Service | `code-server`、`blockscout` 通过真实 K8s 调度和功能操作：code-server 工具容器实际写入 `/workspace`，后端文件 API 读回一致并保存到 MinIO；Blockscout 页面、`/api/v2/stats`、`/api/v2/blocks` 均返回链浏览数据 | `experiment-tool-deep-retest-2026-06-20.txt`、`experiment-tool-deep-summary-current.json`、`experiment-tool-deep-code-server-workspace-exec-current.txt`、`sandbox-tool-service-direct-experiment-retest-2026-06-20.txt` |
| 全部工具 manifest 与命令工具模型 | 部分通过；本地工具镜像已按统一 `USER 1000:1000` 契约重建并发布到同一 Harbor 命名空间，其中 12 个完成单镜像真实功能/HTTP/命令自测，Fabric Explorer 需 Fabric + PostgreSQL 组合编排联测。后端/K8s 调度仍未关闭：当前证明清单只有 `code-server`、`blockscout`、`runtime/evm-foundry`、`infra/postgres-graph` 具备 `harbor.chaimir` digest/Cosign/Trivy 证明，其余工具不能写入 acceptance seed，不能伪造通过 | `.tmp/backend-functional-test/local-tool-images/local-tool-image-inspect-after-rebuild.txt`、`web-platform-tools.txt`、`web-embed-tools.txt`、`web-fix-retest-2.txt`、`novnc-retest-6.txt`、`experiment-tool-attestation-coverage-current.txt/json`、`seed-acceptance-full-tools-blocked-2026-06-20.txt` |
| 用户侧工具 API 代理 | 通过集群内 backend Pod 生产拓扑复测；`code-server` 返回真实 IDE HTML，`blockscout /api/v2/stats` 返回链数据 JSON，首次 `?token` 被换成路径受限 Cookie，平台 token/内部服务签名不透传给工具。此前本地 Windows 后端 `23003` 仅保留为“本机进程无法解析集群 DNS”的拓扑限制，不作为生产方案兼容 | `sandbox-cluster-proxy-browser-auth-2026-06-20.txt`、`sandbox-cluster-proxy-summary-current.json`；旧本地限制证据 `experiment-tool-deep-summary-current.json` |
| 受控命令工具 API | 后端端到端仍未做真实执行；本地镜像层已用真实输入验证 `gas-profiler` 输出 gas 统计、`slither` 发现样例合约重入/低级调用、`mythril` 完成 Solidity 分析。因缺 `harbor.chaimir` digest/Cosign/Trivy 证明，acceptance seed 仍不允许调度这些命令工具 | `.tmp/backend-functional-test/local-tool-images/`；`experiment-tool-attestation-coverage-current.txt/json`、`seed-acceptance-full-tools-blocked-2026-06-20.txt` |
| 沙箱文件/链交互 | 通过；工具容器写入 workspace、后端文件 API 读取一致、保存到 MinIO、路径穿越拒绝、链 `chainId` 查询均按真实用户/服务路径执行 | `experiment-tool-deep-summary-current.json`、`experiment-tool-deep-db-before-recycle-current.txt` |
| 自动回收/主动回收 | 主动回收和自动回收均有闭环证据；深度工具沙箱回收后 DB 为 `status=5`、`sandbox_event` 含 `file_save/recycle`、`sandbox_recycle_outbox.status=3`、动态 namespace 删除；两阶段沙箱回收后 10 秒内 namespace 删除，outbox 已投递。最长生命周期、ready 超时、outbox 失败重试仍未覆盖 | `experiment-tool-deep-db-after-recycle-current.txt`、`experiment-tool-deep-summary-current.json`、`sandbox-two-phase-cleanup-wait-current.json`、`sandbox-two-phase-startup-2026-06-20.txt` |
| 终端工具 | 通过真实 WebSocket PTY 验收；默认终端进入 `student-shell` 并执行 `pwd`，显式访问主运行时容器被 `22023` 拒绝。`student-shell` 只挂 `/workspace`，不挂 `/runtime-state`/Secret，符合“主运行时不开放、学生入口 sidecar”方案 | `sandbox-two-phase-startup-2026-06-20.txt`、`sandbox-two-phase-runtime-pod-current.yaml`、`backend-server-after-ws-fix.out.log` |
| K8s 隔离/资源限制 | 通过本轮深度沙箱和两阶段沙箱验证；Namespace 内 ResourceQuota/LimitRange/deny-all NetworkPolicy/受控 allow policy/显式 `sandbox-workload` SA 均存在；`tool-code-server-web` Pod 和 `student-shell` 只挂允许目录，不挂 `/runtime-state`/`/judge-private`；Quota 为 `limits.cpu 6250m/7`、`limits.memory 8448Mi/9Gi` | `experiment-tool-deep-k8s-resources-current.txt`、`experiment-tool-deep-k8s-yaml-current.yaml`、`experiment-tool-deep-code-server-pod-yaml-current.txt`、`experiment-tool-deep-code-server-private-domain-current.txt`、`sandbox-two-phase-k8s-current.txt`、`sandbox-two-phase-runtime-pod-current.yaml` |
| 沙箱 SA 安全 | 通过；显式 `sandbox-workload`，`automountServiceAccountToken=false`，无 RoleBinding | `real-sandbox-30s-serviceaccount-after-recycle-current.yaml`、`real-sandbox-30s-k8s-pods-current.yaml` |
| 实验模块 | 48/48 通过；2026-06-20 删除后端 `_test.go` 后仅重跑本模块,仍为 48/48 | `experiment-full-continue-after-sa-2026-06-20.txt`；本轮补充 `experiment-full-only-2026-06-20-after-testfile-delete.txt`、`experiment-full-only-db-side-effects-2026-06-20-after-testfile-delete.txt` |
| 竞赛模块 | 71/71 通过 | `contest-full-continue-after-sa-2026-06-20.txt` |
| 并发专项 | 10/10 通过；8 并发报名仅 1 个成员关系，5 并发提交未重复插入，3 并发沙箱均 destroyed | `concurrency-supplement-continue-after-sa-2026-06-20.txt`、`concurrency-sandbox-db-continue-after-sa-2026-06-20.txt` |
| 判题补测 | 10/10 通过 | `judge-supplement-continue-after-sa-2026-06-20.txt` |
| 成绩/管理补测 | 80/80 通过 | `grade-admin-supplement-continue-after-sa-2026-06-20.txt` |
| 仿真主/补充 | 38/38 + 9/9 通过 | `sim-full-continue-after-sa-2026-06-20.txt`、`sim-supplement-rerun-nobom-2026-06-20.txt` |
| 非实验/竞赛横切补测 | 47/47 通过 | `non-exp-contest-supplement-continue-after-sa-2026-06-20.txt` |

### 10.5 数据库/K8s/MinIO/NATS/Redis/ClamAV 副作用证据

| 依赖 | 证据 |
| --- | --- |
| PostgreSQL | `reset-local-continue-after-sa-2026-06-20.txt`；`runtime-image-prepull-db-continue-after-sa-2026-06-20.txt`；`real-sandbox-30s-db-current.txt`；`concurrency-sandbox-db-continue-after-sa-2026-06-20.txt`；本轮实验专项 `experiment-full-only-db-side-effects-2026-06-20-after-testfile-delete.txt`、`experiment-tool-deep-db-before-recycle-current.txt`、`experiment-tool-deep-db-after-recycle-current.txt`、`sandbox-two-phase-startup-2026-06-20.txt`、`sandbox-cluster-proxy-browser-auth-2026-06-20.txt` |
| K8s | `runtime-image-prepull-k8s-continue-after-sa-2026-06-20.txt`；`real-sandbox-30s-k8s-resources-current.txt`；`real-sandbox-30s-k8s-pods-current.yaml`；本轮工具深度 `experiment-tool-deep-k8s-resources-current.txt`、`experiment-tool-deep-k8s-yaml-current.yaml`；两阶段终端/回收 `sandbox-two-phase-k8s-current.txt`、`sandbox-two-phase-runtime-pod-current.yaml`、`sandbox-two-phase-cleanup-wait-current.json`；集群内代理复测 `sandbox-cluster-proxy-k8s-current.txt`、`sandbox-cluster-proxy-runtime-pod-current.yaml`；最终动态 namespace 已删除 |
| MinIO | 本轮实验工具深度测试通过 `POST /files/save` 写入 `minio://chaimir-code/910000000000000001/sandbox/code/194004623281885184/workspace.tar`，hash `d1087147b6aea4b30747d42ce53a5784d676f51c918c4571439150ac3f2bf2f7`；竞赛提交、成绩单/transfer/bundle 授权仍沿用既有证据 |
| NATS/outbox | 实验成绩、竞赛提交、判题、成绩/通知链路在上述脚本中通过；未重新逐条追踪每个 NATS subject，仍保留为未完全覆盖项 |
| Redis | 依赖 Pod Running；登录/token/限频/并发用例通过，未单独导出 Redis key 级证据 |
| ClamAV | 依赖 Pod Running；非实验/竞赛横切补测沿用上传病毒检测路径，未在本轮新增 EICAR 直连证据 |

### 10.6 问题状态

| 编号 | 状态 | 说明 |
| --- | --- | --- |
| P2-005 K8s 沙箱 Ready 成功路径未覆盖 | 已关闭 | 本轮真实 K8s 沙箱在 30 秒配置下进入 Ready，Pod/Service/NetworkPolicy/Quota/SA 均有证据，随后回收清空命名空间 |
| P1-006 用户侧工具启动 SLA | 按两阶段验收已关闭 | 最新集群内复测阶段一 9.323 秒进入环境，满足复杂多节点 ≤15 秒；阶段二后台继续初始化，8.230 秒后 `code-server` 与 `blockscout` 均真实 Ready。阶段一响应仍明确标记 `blockscout` 初始化中，未假 Ready |
| P1-007 工具 API 代理生产拓扑 | 已关闭 | 已在集群内 backend Pod 拓扑重跑；`code-server` 和 `blockscout` 代理均通过真实浏览器入口 token/cookie 流程。本地 Windows 后端 `23003` 保留为开发拓扑限制，不写兼容分支 |
| P2-008 实验终端用户可用性 | 已关闭 | 默认终端 WebSocket 升级到 101，进入 `student-shell` 并执行 `pwd`；显式访问主运行时容器被 `22023` 拒绝，且修复后未再出现已 hijack 连接写 JSON 的错误日志 |
| P1-009 全部工具真实运行验收 | 未关闭 | 13 个工具中本轮只有 `code-server`、`blockscout` 具备受控证明并完成真实功能操作；其余 11 个缺 digest/Cosign/Trivy 证明，acceptance seed 正确失败，不能声称全部工具已真实可运行。CI 完整 `image-digests.lock` artifact 生成闭环已修，仍需实际 Harbor 推送、扫描、签名和预拉取后重跑全量工具 |
| P1-010 快照/卷回收闭环 | 代码与部署契约已修，真实快照端到端待测 | 已补齐 RBAC 和 M2 回收语义，普通回收会等待 Namespace 删除完成，快照保留阶段只留 PVC/VolumeSnapshot/Namespace 并要求 SnapshotClass 删除策略为 `Delete`；当前本地集群未配置生产级 CSI 快照类，仍需在具备真实 CSI 的环境覆盖 `snapshot_enabled=true` 创建、到期清理和底层快照删除 |
| K8s 控制面短暂 CrashLoop | 环境阻塞已解除 | 现象为 scheduler/controller-manager leader election 超时，旧 `sbx-*` 残留加剧回收等待；清理动态测试命名空间后控制面恢复 Running。未改业务逻辑绕过 |
| 仿真补充包 BOM | 测试脚本问题，已修 | 后端严格拒绝 BOM manifest 是正确安全行为；仅修 `.tmp` 生成脚本，不改生产端 |

### 10.7 结论

可以进入受控前后端联调，但实验工具链需带限制进入：实验流程 48/48 通过；两阶段启动最新集群内复测阶段一 9.323 秒可进入，阶段二后台真实初始化；`code-server`、`blockscout` 在真实 K8s 中完成了工具容器写 workspace、后端读回保存、Blockscout 链数据读取、集群内用户侧代理、K8s 隔离与回收闭环验证；终端已按 `student-shell` sidecar 方案通过，主运行时容器仍保持不可进入；预拉取语义已统一为运行时工作负载镜像集合。

仍不建议直接进入生产镜像部署测试：镜像锁文件发布闭环已修，但全部 13 工具真实运行还缺少 11 个工具的 Harbor digest/Cosign/Trivy 实际证明和全量镜像预拉取证据；Fabric Explorer 仍需 Fabric + PostgreSQL 组合编排联测；最长生命周期、ready 超时、snapshot 到期、outbox 失败重试、NATS 每类 subject、Redis key 级副作用和长时间并发压力矩阵仍未完全覆盖。

## 11. 2026-06-28 续测：容器化后端、生产镜像准入和实验专项收口

本轮按“后端也跑在 Docker/K8s 里面”的口径执行，不再使用宿主机 `go run` 作为功能验收入口。测试只暴露真实问题，不为测试增加兼容分支；旧 Docker/K8s/DB 数据干扰按证据清理。证据全部写入 `.tmp/backend-functional-test/evidence/`，未提交 `.tmp`、`backend/.env`、`deploy/config/secret.env`、`deploy/config/supply-chain.secret.env`、`deploy/config/supply-chain.kubeconfig`、`deploy/config/cosign/cosign.key`。

### 11.1 测试环境

| 项目 | 结果 |
| --- | --- |
| 当前 commit | `89bc446507f0b5d75dc74bb430c50289e9436787` |
| 工作区 | 仅修改 `backend/internal/modules/sandbox/rules.go`、`backend/internal/modules/sandbox/service.go`；无后端 `_test.go` |
| 后端启动命令 | K8s Deployment `chaimir-system/chaimir-backend`，镜像 `harbor.chaimir:30080/service/backend@sha256:cbaeacbba4b9c7f00ae2fe88f8c4a0614df6a399f4e50d4fdc4ae3e6cf0a0153`；Service `chaimir-backend:80 -> 8080`；本机只用 `kubectl port-forward --address 127.0.0.1 svc/chaimir-backend 18081:80` 访问 |
| 数据库迁移/种子 | K8s Job `chaimir-migrate-20260628-130705` 使用 `service/migrate@sha256:b01428ef7131ec4c680ba7632dee133a81239b895e2f5179c1619ea01f24e574` 执行完成；`seed-acceptance` Job 失败，见 P1-052 |
| 基线命令 | `git status`/`rev-parse`/敏感文件 ignore、Harbor/data Pod、端口、`cd backend; go test ./...`、5 套 Kustomize、`frontend npm run build` 均执行；证据 `baseline-20260628-135334-round-after-user-correction.log`、`kustomize-overlays-20260628-135359-supplement.log` |
| Harbor | `harbor` namespace 7 个 Pod Running；入口 `harbor.chaimir:30080`；证据 `baseline-20260628-135334-round-after-user-correction.log` |
| 数据依赖 | `chaimir-data` 下 `postgres-0`、`redis-0`、`nats-0`、`minio-0`、`clamav-0` Running；本机数据端口未转发，后端通过 K8s 内网访问 |

### 11.2 镜像签名/扫描/attestation 证据

| 项目 | 结果 |
| --- | --- |
| 全量拉取 | `.tmp/backend-functional-test/evidence/pull-scope-all-final-before-push.log` 显示 `Pulled 78 image references successfully. scope=all registry=harbor.chaimir:30080` |
| Trivy/Cosign/attestation | `.tmp/backend-functional-test/evidence/image-fix-2026-06-26/attest-final-20260628/image-attestations-summary.txt` 显示 `attested_count=63`、`blocked_count=0`、registry 为 `harbor.chaimir:30080` |
| 后端配置 | `deploy/config/chaimir.env` 已写入真实 `SANDBOX_IMAGE_ATTESTATIONS_JSON`，不是空数组，也没有手写伪造证明 |
| 旧 registry 检查 | Pod 镜像检查未发现 `127.0.0.1:5000`、`chaimir.local`、`local-dev`、`retry-clean`、`upload-probe`、`harbor.chaimir.local`、`chaimir/backend`；证据 `no-old-host-pod-image-check-20260628-134652.log`、`side-effects-final-20260628-140329.log` |
| 当前限制 | 本轮代码修复尚未重建、Trivy、Cosign、重新生成 attestation 并部署，因此当前运行中的 backend digest 不包含 P0-048 修复 |

### 11.3 路由覆盖表

| 范围 | method/path | 结果 | 证据 |
| --- | --- | --- | --- |
| health | `GET /api/healthz`、`GET /api/readyz` | 容器化后端 200 | `backend-portforward-fixed-20260628-131338.log` |
| experiment | `run-experiment-full.ps1` 覆盖实验创建、编排、草稿、发布、实例、成员/角色、阶段、报告、成绩、权限 | 48/48 通过 | `run-experiment-full-after-cleanup-20260628-134849.log` |
| sandbox create | `POST /api/v1/sandbox/sandboxes`，body 使用 `runtime_code=evm-foundry`、`runtime_image_version=2026.06`、`tools=[code-server,blockscout]` | 阶段一 7.185s，可进入；复杂多节点满足 ≤15s | `run-sandbox-k8s-resource-scan-no-snapshot-20260628-140158.log` |
| sandbox status | `GET /api/v1/sandbox/sandboxes/{id}` | 返回 `phase=2/status=7`，工具分阶段状态可见，未假 Ready | `sandbox-k8s-resource-196867709701459968-summary.json` |
| tool proxy | `GET /api/v1/sandbox/sandboxes/{id}/tools/code-server/`、`.../blockscout/api/v2/stats` | code-server 代理 200；blockscout stats 200 | `run-sandbox-cluster-proxy-no-old-host-20260628-134708.log` |
| terminal | `GET /api/v1/sandbox/sandboxes/{id}/terminal` WebSocket | 集群代理脚本一次通过；两阶段脚本一次出现 Close/Text 错误但脚本未正确失败，记为 P2-055 | `run-sandbox-cluster-proxy-no-old-host-20260628-134708.log`、`run-sandbox-two-phase-no-old-host-20260628-134522.log` |
| recycle | `POST /api/v1/sandbox/sandboxes/recycle` | 回收 `code=0`，namespace 删除等待成功，DB destroyed | `sandbox-k8s-resource-196867709701459968-summary.json`、`side-effects-final-schema-corrected-20260628-140416.log` |

### 11.4 工具真实功能覆盖表

| 工具 | 本轮结果 |
| --- | --- |
| code-server | 通过容器/代理/工作区链路：工具容器写入 workspace，后端文件 API 读回一致，保存到 MinIO key，回收后 DB 保留 code hash；证据 `run-tool-deep-after-cleanup-20260628-134910.log` |
| blockscout | 通过页面、`/api/v2/stats`、`/api/v2/blocks`，读取真实链数据；证据 `run-tool-deep-after-cleanup-20260628-134910.log`、`run-sandbox-cluster-proxy-no-old-host-20260628-134708.log` |
| terminal | 默认终端进入 `student-shell` 在集群代理脚本通过，主 runtime 容器访问被拒绝；但两阶段脚本存在一次 WebSocket 断开失败且脚本断言不严，终端结论保留为“部分通过，需复测” |
| theia | 当前统一 IDE 入口已收敛到 `code-server`；`images/tool/theia` 不再作为当前准入工具，后端不补兼容工具定义 |
| jupyter/remix/contract-ui/slither/gas-profiler/mythril/novnc-desktop/db-viewer/wallet-sim | 未完成真实用户操作矩阵；虽然证明清单含这些 tool 镜像，但本轮 acceptance 运行时未编排对应工具组合，不能仅凭 HTTP/镜像证明宣称通过 |
| fabric-explorer | 未测；`fabric-explorer` 不在当前 digest lock/attestation 中，且必须在 Fabric + PostgreSQL/CouchDB + connection profile 组合编排下测试，不能用单容器或 HTTP 200 代替 |

### 11.5 实验功能测试表

| 范围 | 结果 |
| --- | --- |
| 实验创建/编排/草稿持久化/发布/实例/成员/角色/阶段/报告/成绩/权限 | 48/48 通过，证据 `run-experiment-full-after-cleanup-20260628-134849.log` |
| 会话创建、成员加入、角色分配、环境创建、状态查询、刷新后状态不丢 | 已由实验脚本和沙箱状态轮询覆盖；DB 统计 `experiment=13`、`experiment_instance=13`、`experiment_group=7`、`experiment_report=7`、`experiment_score_outbox=10`，证据 `experiment-table-counts-20260628-140441.log` |
| K8s 真实资源 | Namespace、Pod、Service、NetworkPolicy、ResourceQuota、LimitRange、ServiceAccount、workspace PVC 均有实物证据；证据前缀 `sandbox-k8s-resource-196867709701459968-*` |
| VolumeSnapshot | 当前集群无可用 CSI 快照能力；`snapshot_enabled=true` 被后端以 `22026` 明确拒绝，trace_id `sandbox-k8s-resource-c2342384-6e99-449e-809e-39ce40833004`，符合“无真实快照能力不得启用” |
| SLA | 复杂多节点阶段一 7.185s/8.208s/6.325s 多次满足 ≤15s；简单实验 ≤5s 未关闭，因为当前 acceptance 只有 `evm-foundry + 默认工具` 可用组合 |

### 11.6 数据库/K8s/MinIO/NATS/Redis/ClamAV 副作用证据

| 依赖 | 证据 |
| --- | --- |
| PostgreSQL | sandbox 最近 7 条均 `status=5`、`phase=2`、`code_storage_key/code_hash` 存在；`sandbox_event` 有 `create/phase_change/file_save/recycle/error`；`sandbox_recycle_outbox.status=3`、`retry_count=1`、`last_error` 空；证据 `side-effects-final-20260628-140329.log`、`side-effects-final-schema-corrected-20260628-140416.log` |
| 审计 | `audit_log` 对最近 7 个 sandbox 有 `sandbox.create` 和 `sandbox.recycle`，证据 `side-effects-final-schema-corrected-20260628-140416.log` |
| 成绩/通知/outbox | `course_grade=1`、`student_semester_grade=1`、`experiment_score_outbox=10`、`grade_lock_outbox=3`、`notification=2`，证据 `side-effects-final-schema-corrected-20260628-140416.log` |
| K8s | 无活动 `sbx-*` namespace；后端 Pod Running；旧失败 seed Job 已删除；证据 `cleanup-failed-seed-job-20260628-135359.log`、`side-effects-final-20260628-140329.log` |
| MinIO | code-server workspace 通过后端保存到 `minio://chaimir-code/.../workspace.tar`，hash 存在；MinIO 容器无 shell，未直接 `find /data`，以 API + DB key 作为本轮证据 |
| NATS | NATS Pod Running，监控 `/jsz` 可读，JetStream 当前 `streams=0/messages=0`；业务 outbox 表有待/已投递记录。未逐 subject 抓包，仍为未完全覆盖项 |
| Redis | 认证后 `DBSIZE=2`，证据 `side-effects-final-schema-corrected-20260628-140416.log` |
| ClamAV | Pod Running，`ClamAV 1.4.4/28044`，证据 `side-effects-final-20260628-140329.log` |

### 11.7 问题清单

| 编号 | 级别 | 复现步骤 | 期望 | 实际 | trace_id/证据 | 阻塞 | 修复方式/状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P0-048 | P0 | 在 DB `runtime_image`/`tool.resource_spec`/`runtime.adapter_spec` 残留 `harbor.chaimir.local` 时调用 `POST /api/v1/sandbox/sandboxes` | 创建沙箱前 runtime、infra、tool 镜像都必须命中当前 `SANDBOX_IMAGE_ATTESTATIONS_JSON` | 运行中的旧 backend digest 只校验了部分入口，旧 DB 镜像可进入 K8s 编排并导致拉取旧 host | `old-registry-db-metadata-fix-20260628-133840.log`、`runtime-adapter-spec-oldref-fix-20260628-134508.log` | 阻塞生产部署 | 已在源码增加 `validatePlanImagesAttested`，覆盖创建和已有 plan；尚需重建 service/backend、Trivy、Cosign、attestation、重新部署 |
| P1-049 | P1 | acceptance DB 元数据含 `harbor.chaimir.local` | 种子/运行数据只能引用 `harbor.chaimir:30080@sha256` | runtime/tool/adapter_spec 多处旧 registry 干扰真实测试 | 同上 | 阻塞真实实验测试 | 本地 DB 已清理到当前 digest；种子源需继续排查，不能兼容旧 host |
| P1-050 | P1 | K8s 后端 Pod 首次启动 | ConfigMap/Secret 应包含后端生产必需配置 | 缺少 SMS endpoint/templates 导致容器启动失败 | `container-backend-config-sms-fix-20260628-130546.log` | 阻塞容器化后端启动 | 已补非敏感 ConfigMap 配置；需同步部署配置源 |
| P1-051 | P1 | K8s 拉取 `service/backend@sha256` | 节点应可按 Harbor digest 拉取 | Docker Desktop registry mirror 对 Harbor digest HEAD 返回 500 | `container-backend-image-import-tagged-20260628-125515.log`、`containerd-digest-alias-allnodes-20260628-130124.log` | 阻塞本地 K8s 后端启动 | 本轮导入并打 digest alias 到所有节点；生产环境需修 registry mirror/节点拉取路径 |
| P1-052 | P1 | 运行 `chaimir-seed-acceptance` K8s Job | 生产 migrate/seed 镜像应可自包含执行 acceptance seed | Job 失败 `未找到 images 目录` | `k8s-seed-acceptance-20260628-130731.log` | 阻塞容器内 acceptance 重置 | 已保留失败证据并删除失败 Job；需修 seed 镜像/挂载输入，不用本地仓库目录兼容 |
| P1-053 | P1 | 创建 `snapshot_enabled=true` 沙箱 | 无 CSI 快照能力时明确拒绝 | 返回 `22026 实验环境快照能力暂不可用` | trace_id `sandbox-k8s-resource-c2342384-6e99-449e-809e-39ce40833004` | 不阻塞普通实验，阻塞快照验收 | 符合安全预期；需在有 VolumeSnapshotClass/CSI 的集群做端到端补测 |
| P2-054 | P2 | 创建 `snapshot_enabled=true` 但不传 `snapshot_retention_minutes` | 返回统一错误信封 | 返回 `24007` | trace_id `sandbox-k8s-resource-c2e225d2-bd42-4cb0-a7b2-6f14280a33a8` | 不阻塞 | 测试输入问题，脚本已改为显式传保留时间后复测能力校验 |
| P2-055 | P2 | 运行两阶段终端脚本 | 终端失败应导致脚本失败 | 一次 WebSocket Close/Text 错误被脚本 exit 0 掩盖 | `run-sandbox-two-phase-no-old-host-20260628-134522.log` | 阻塞终端“完全通过”结论 | 需修 `.tmp` 验收脚本断言并重跑，不改后端兼容 |
| P2-056 | P2 | 工具深测脚本在容器化后端拓扑下收尾查 DB | 应通过 K8s DB 或跳过本地端口假设 | 仍访问 `127.0.0.1:15432`，连接拒绝 | `run-tool-deep-after-cleanup-20260628-134910.log` | 不阻塞功能，但污染证据 | 需清理 `.tmp` 脚本旧本地端口假设 |
| P2-057 | P2 | 全工具真实功能测试 | 当前准入工具按真实用户操作覆盖 | fabric-explorer 因安全风险不准入；Theia 已从当前统一 IDE 范围移除；其余多工具需按准入镜像和真实编排继续回归 | attestation JSON、工具深测证据 | 阻塞“全工具通过”结论 | 继续补当前准入工具编排、镜像证明和真实操作测试；不补 Theia/Fabric Explorer 兼容路径 |

### 11.8 结论

可以进入受控前后端联调：容器化后端 Running，实验专项 48/48，通过真实 K8s 创建/状态/代理/回收闭环；复杂多节点两阶段启动满足 ≤15s；普通回收会保存代码、写 DB 状态、写 `sandbox_event`/`audit_log`/`sandbox_recycle_outbox` 并等待 namespace 删除。

不建议进入生产镜像部署测试：虽然当前已有 63 条真实 Trivy/Cosign attestation 且本轮功能跑通，但 P0-048 源码修复尚未重建、签名、扫描、重新生成证明并部署；`seed-acceptance` 容器 Job 仍失败；全工具真实用户操作矩阵、简单实验 ≤5s、VolumeSnapshot 端到端、NATS subject 级投递失败重试、长时间并发压力仍未关闭。

## 12. 2026-06-29 续测：真实实验、工具镜像和多镜像编排收口

本轮按最新要求执行“先完整测试、后统一修复”。除 `.tmp` 下测试脚本错误外，未继续修改后端或镜像实现；发现的后端/K8s/镜像问题先记录。`tool/fabric-explorer` 因安全风险不准入，本轮明确跳过。

### 12.1 基线与环境

| 项目 | 结果 |
| --- | --- |
| 当前 commit | `89bc446507f0b5d75dc74bb430c50289e9436787` |
| 后端 | K8s `chaimir-system/chaimir-backend` Running，本机通过 `kubectl -n chaimir-system port-forward svc/chaimir-backend 18081:80` 访问 |
| Harbor | `harbor` namespace 7 Pod Running；`0.0.0.0:30080` 由 PID `18940` 转发 |
| 数据依赖 | `chaimir-data` 下 `postgres/minio/nats/redis/clamav` Running；本机未暴露 15432/16379/14222/19000/13310 |
| 基线命令 | 本轮重新执行 `go test ./...`、5 套 kustomize、`frontend npm run build`，均通过 |
| 敏感文件 | `backend/.env`、`.tmp`、secret env、kubeconfig、cosign key 均未被 Git 跟踪且被 `.gitignore` 命中 |

### 12.2 镜像准入与自检

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| Attestation | `SANDBOX_IMAGE_ATTESTATIONS_JSON` 为真实 63 条，含 `image_url/digest/cosign_verified=true/trivy_status=passed` | `.tmp/backend-functional-test/evidence/attest-walletreadiness-20260629-155004/` |
| 镜像预拉取 | runtime + enabled tools 10 个镜像预拉取成功 | `runtime-image-prepull-current.json` |
| 镜像自检 | 大多数镜像版本/布局自检通过；原自检脚本存在 Windows 参数转义问题，已只修 `.tmp` 脚本并定向复现 | `image-selftest-current-summary.json`、`image-selftest-failure-repro-current.json` |
| 已确认测试脚本问题 | `base/judge-min`、`tool/gas-profiler`、`init/sandbox-seed reject-non-workspace-target` 复现通过；`init/sandbox-seed reject-symlink-source` 原测试目标不在 `/workspace`，修正目标后返回 65 | `sandbox-seed-symlink-valid-target-current.log` |
| 镜像真实问题 | `judger/testcase-evm` 缺 `forge`；`runtime/evm-hardhat` 缺 `anvil`；`runtime/cosmos` manifest 声明的 entrypoint 未入镜像且默认启动缺最小 gas 配置；`runtime/edu-chain` 在只读根文件系统下 `py_compile` 写 `/app/__pycache__` 被拒绝 | `image-selftest-failure-repro-20260629-184045/`、`runtime-cosmos-default-start-current.log` |

### 12.3 实验与沙箱功能

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| 实验 API | 创建、编排、草稿、发布、实例、成员、角色、阶段、报告、成绩、内部统计 48/48 通过 | `experiment-full-summary-current.json` |
| K8s 资源 | Pod、Service、NetworkPolicy、ResourceQuota、LimitRange、ServiceAccount、PVC 均有真实创建证据；无动态 namespace 残留 | `full-functional-20260629-163807/`、`dynamic-namespaces-after-tests-current.txt` |
| 简单 SLA | terminal-only 沙箱阶段一 `6.318s`，超过简单实验 ≤5s 要求 | `sandbox-simple-startup-sla-current.json` |
| 复杂 SLA | 多工具组合 `6.139s/6.184s/8.249s`，满足复杂多节点 ≤15s | `full-functional-current-summary.json`、`experiment-tool-deep-*` |
| 两阶段启动 | 阶段一可进入，阶段二工具初始化异步继续；未假 Ready | `sandbox-two-phase-startup-summary-current.json` |
| 回收闭环 | 保存 workspace、写 `code_storage_key`、删除 namespace、写 `sandbox_event`、outbox 可重试后成功 | `browser-tool-recycle-summary-current.json`、`novnc-real-recycle-current.json` |

### 12.4 工具真实功能覆盖

| 工具 | 结果 |
| --- | --- |
| code-server | 真实创建/编辑/保存 Node 项目，`node --check`、运行、`npm test` 通过；网络隔离下 npm registry 超时符合边界；无 Python/Go/Solidity 工具链 |
| jupyter | Python notebook/nbconvert 执行通过，workspace 与 code-server 共享 PVC |
| remix | 页面可打开，Solidity 插件入口可见；浏览器 console 有 13 条错误，且 Solidity 编译受 runtime 缺 solc 阻塞，不能判定 Remix 完整通过 |
| contract-ui | 页面可打开，`setNumber(uint256)` calldata 生成通过 |
| wallet-sim | `/accounts`、`/sign` API 通过，签名可恢复地址；根路径 `/` 返回错误页，若要求 UI 则不通过 |
| gas-profiler | 后端 command-tool 深测通过；原镜像自检失败为测试脚本引号问题 |
| mythril | 后端 command-tool 深测通过 |
| slither | 容器能输出有效 JSON，但工具退出码 255 时后端丢弃 stdout 并返回 `22011`，视为后端工具执行契约问题 |
| blockscout | 独立多镜像组合 `evm-foundry + postgres-graph + blockscout` 读真实链 blocks/tx/address/stats 通过；后端 DB 当前 `blockscout.status=2`，不能由后端调度 |
| db-viewer | 临时 PostgreSQL + db-viewer 组合通过，能看到 `balances` 表；对平台 DB 负向检查通过；后端 DB 当前 `db-viewer.status=2`，不能由后端调度 |
| novnc-desktop | noVNC 页面和 autoconnect 真实桌面截图通过；容器内 workspace 文件访问和平台 DB 网络隔离通过 |
| terminal | 默认 WebSocket 进入 `student-shell`；显式主 runtime 容器终端被拒绝 |
| theia | 已从当前统一 IDE 准入范围移除；当前 IDE 统一使用 `code-server`，不补 Theia 兼容工具 |
| fabric-explorer | 安全风险不准入，按要求跳过 |

### 12.5 新增/更新问题清单

| 编号 | 级别 | 复现步骤 | 期望 | 实际 | trace_id/证据 | 阻塞 | 修复方式 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P0-058 | P0 | 通过后端访问 `/api/v1/sandbox/sandboxes/{id}/tools/code-server/` | 后端可代理到动态 sandbox 工具 Service | `chaimir-system` 默认拒绝 egress，后端到 `sbx-*` Service 超时，返回 `23003` | trace_id `b3704169-8a39-413b-b7a9-d970e6a19237` | 阻塞前端真实工具入口 | 统一补 `chaimir-system` 到受管 sandbox namespace 的最小 egress，不做旧路径兼容 |
| P1-059 | P1 | terminal-only 简单沙箱启动 | 用户 ≤5s 可进入 | 阶段一 `6.318s` | `sandbox-simple-startup-sla-current.json` | 阻塞简单 SLA | 优化调度/预拉取/阶段一资源路径，不能放宽 SLA |
| P1-060 | P1 | 在 `runtime/evm-foundry` 内 `forge build` Solidity | 离线隔离下可编译/部署 | 缺预装 solc，forge 尝试下载后失败 | `browser-tool-functional/summary.json` | 阻塞 Solidity/Remix 完整验收 | 镜像内预置项目要求版本的 solc 或统一离线编译链 |
| P1-061 | P1 | 调用后端 slither command-tool | 返回结构化 stdout/stderr/exit_code | slither stdout JSON 有效但退出码 255，后端返回 `22011` 且丢弃输出 | trace_id `096da11c-5c09-40ca-8fc8-8b2f0f24cb15` | 阻塞安全分析工具 | 统一 command-tool 结果契约，保留 stdout/stderr，不按裸退出码吞结果 |
| P1-062 | P1 | `runtime/cosmos` 自检/默认启动 | manifest 与镜像一致，可初始化教学链 | scratch 镜像无 `sh`/无 `chaimir-cosmos-entrypoint`，默认启动缺 min gas 配置 | `runtime-cosmos-default-start-current.log` | 阻塞 Cosmos runtime | 统一 Dockerfile/manifest/entrypoint，补初始化配置 |
| P1-063 | P1 | `judger/testcase-evm` 自检 | Hardhat + Foundry 判题工具齐全 | `forge: not found` | `judger_testcase-evm-tool-versions.log` | 阻塞 EVM testcase 判题 | 镜像补 Foundry 工具链 |
| P1-064 | P1 | `runtime/evm-hardhat` 自检 | Hardhat runtime 带 EVM RPC/Foundry 辅助 | `anvil: not found` | `runtime_evm-hardhat-hardhat-and-anvil.log` | 阻塞 Hardhat runtime | 镜像补 anvil 或修 manifest 能力声明 |
| P1-065 | P1 | `runtime/edu-chain` 只读根文件系统语法/启动 | 非 root + 只读根可运行 | Python 写 `/app/__pycache__` 被拒绝 | `runtime_edu-chain-syntax.log` | 阻塞 edu-chain runtime | 预编译 pyc、设置不写 bytecode 或调整可写缓存挂载 |
| P1-066 | P1 | K8s `chaimir-frontend` Pod | 前端镜像可拉取运行 | `ImagePullBackOff`，`service/frontend` 未准入 | `kubectl -n chaimir-system get pods` | 阻塞前后端联调 UI | 前端镜像纳入 Trivy/Cosign/attestation 后部署 |
| P1-067 | P1 | 后端调度 blockscout/db-viewer | 已准入镜像可由实验编排使用 | DB 中两者 `status=2`，后端不调度 | `tool-db-query-current`、`db-viewer-integration-current.json` | 阻塞全工具编排 | 统一 seed/准入状态，不能手改 DB 冒充通过 |
| P2-068 | P2 | 实验流程后查 notification | 实验成绩/通知链路有通知记录 | `experiment.%` notification 记录为 0 | `experiment-notify-current.txt` | 不阻塞实验主流程 | 对照 notify 文档确认应发事件并修链路 |
| P2-069 | P2 | wallet-sim 根路径 | 工具有可操作页面 | `/` 返回错误页，API 正常 | `wallet-sim-accounts.png`、`wallet-sim-http-api.json` | 取决于产品定义 | 若要求 UI，补页面；否则 manifest 标注 API-only |
| P2-070 | P2 | Remix 页面 | 页面无明显前端错误并能完成 Solidity 操作 | 页面可打开但 console 13 errors，编译受 solc 缺失阻塞 | `browser-tool-pages/remix.png` | 阻塞 Remix 完整通过 | 先修 runtime solc，再复测 Remix UI 编译/部署 |

### 12.6 结论

当前不建议进入生产镜像部署测试：供应链 attestation 已真实生成，但后端工具代理 egress、简单 SLA、Solidity 离线编译、多个 runtime/judger 镜像能力缺失、前端镜像未准入仍阻塞。

可以继续后端问题修复阶段：实验 API 主流程已通过，K8s 资源和回收闭环有真实证据；修复应集中在网络策略、runtime/tool 镜像能力、工具执行契约、seed 准入状态和前端镜像准入，不为测试写兼容分支。

## 13. 2026-06-30 续测：按 11 模块重新补齐实验相关链路

本轮没有以旧脚本通过为准，而是重新按 `docs/01-身份与租户` 到 `docs/11-成绩中心` 梳理实验依赖链。测试证据仍放在 `.tmp/backend-functional-test/evidence/`，并把旧脚本路径错误、当前真实产品问题分开记录。当前结论：实验功能尚未完整通过，不能进入修复完成声明；需要先修 P1 阻塞后再回归。

### 13.1 新增覆盖

| 模块 | 覆盖点 | 结果 | 证据 |
| --- | --- | --- | --- |
| M1 身份与租户 | 未登录、过期 token、logout 后 token、畸形 token、学生读审计拒绝、学校管理员审计 | 通过 | `experiment-cross-module-supplement-results-current.json` |
| M2 沙箱引擎 | Pod/Service/NetworkPolicy/ResourceQuota/LimitRange/ServiceAccount/PVC、两阶段、工具、回收、MinIO 持久化 | 主链通过；VolumeSnapshot CRD 缺失；简单 SLA 失败 | `sandbox-k8s-resource-*`、`sandbox-two-phase-startup-summary-current.json`、`sandbox-simple-startup-sla-current.json` |
| M3 评测引擎 | 判题任务权限、重判、人工评分边界、M7 调 M3 入口 | M3 补测 9/10；M7 环境实验无法创建，checkpoint 未能真实闭环 | `judge-supplement-results-current.json`、trace_id `00070cfc-d90c-463d-be10-f5240be2fd84` |
| M4 仿真 | 内部服务创建 session、动作幂等/冲突、checkpoint、replay、share、wrong source 回收拒绝、destroy 后动作拒绝 | 通过 | `sim-internal-current-summary.json` |
| M5 题库/模板 | 教师题面无敏感字段、学生 full 拒绝、学生直接题面接口拒绝、内部 full 可取、版本列表 | 通过；学生题面只能经业务入口，不是 content 直出 | `experiment-cross-module-supplement-results-current.json` |
| M6 教学 | 实验模板课时、课程 outline、学校管理员非课程成员读课时拒绝、学生读成员列表拒绝 | 通过 | `experiment-cross-module-supplement-results-current.json` |
| M7 实验 | 主流程 48/48、报告 key 负向、分组角色、内部 score；环境型实验新增补测失败 | 部分通过 | `experiment-full-results-current.json`、`experiment-cross-module-supplement-results-current.json` |
| M8 竞赛 | 不作为实验主链；仅确认共享 M2/M3/M5 底座未跳过 | 已纳入模块 smoke 回归 | `module-smoke-v2-results.json` |
| M9 管理后台 | school dashboard、audit、platform dashboard、告警/配置边界 | 通过 | `grade-admin-supplement-results-current.json` |
| M10 通知实时 | `/api/ws` topic 订阅、内部 push、跨租户 topic 拒绝、内部 send、学生 inbox | 通过 | `experiment-cross-module-supplement-results-current.json` |
| M11 成绩中心 | 学生成绩接口边界、M7 内部 score 只经 experiment internal 读取 | 通过；实验成绩事件链仍需修复后复测 | `grade-admin-supplement-results-current.json`、`experiment-cross-module-supplement-results-current.json` |

### 13.2 新增/更新问题清单

| 编号 | 级别 | 复现步骤 | 期望 | 实际 | trace_id/证据 | 阻塞 | 修复方式 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P1-071 | P1 | `POST /api/v1/experiment/experiments/910000000000006001/instances` body `{"group_id":910000000000006011}` | 基于 seed 环境实验创建真实 K8s 环境实例 | 返回 `72005`，日志根因 `[22017] 实验环境创建信息不完整`；复现同 seed sandbox create 入参会因 `keep_alive_minutes=60` 但 `keep_alive=false` 被 M2 拒绝；补齐 `keep_alive=true` 和 `snapshot_retention_minutes=60` 后又因本地集群无 VolumeSnapshot 能力返回 `22026` | trace_id `00070cfc-d90c-463d-be10-f5240be2fd84`、`sandbox-create-contract-probe-current.json`、`experiment-cross-module-supplement-results-current.json` | 阻塞环境型实验、checkpoint 判分闭环 | 先修 seed/实验编排 SSOT，使 `keep_alive`/`snapshot_enabled` 与分钟字段成对；本地无真实快照能力时不能创建开启快照的实验，应提供普通环境实验和快照专项环境两套明确用例 |
| P1-072 | P1 | `kubectl api-resources` 和 `kubectl get volumesnapshotclasses.snapshot.storage.k8s.io` | K8s 提供 VolumeSnapshot CRD/Class，支持按需快照与清理 | 集群无 VolumeSnapshot 资源类型 | `kubectl` 输出：`the server doesn't have a resource type "volumesnapshotclasses"` | 阻塞快照验收 | 部署 CSI snapshot CRD/Class，或文档明确当前环境不支持并阻断相关能力 |
| P1-073 | P1 | 对比 `chaimir-backend` 当前部署镜像 digest 与 attestation/lock | 每次后端重建后必须重新生成 `images/image-digests.lock` 和 Trivy/Cosign attestation，不能用旧 lock 作通过标准 | 当前部署 `service/backend@sha256:e1ee...9965` 是修复后新部署，旧 lock/attestation 未刷新，因此仅说明供应链证据过期，不代表旧 lock 是正确标准 | `backend-deployed-attestation-gap-current.json` | 阻塞生产镜像部署测试 | 所有后端修复完成后统一重建镜像、扫描、签名、更新 lock/attestation，再部署回归 |
| P1-074 | P1 | 后端平台 K8s 创建 `evm-foundry + blockscout` | blockscout 可随 runtime 编排读取真实链数据 | 后端返回 `23002`；根因是 `tool/blockscout` 的 `network_rules` 要访问 `sandbox:rpc` 和 `sandbox:postgres`，但当前 `runtime.evm-foundry.adapter_spec` 只声明 `foundry` 的 `rpc` 端口和 `student-shell`，没有 `postgres` 端口/`infra/postgres-graph` 拓扑；独立 Docker 多镜像验证通过是因为脚本显式补了 `postgres-graph` | trace_id `sandbox-two-phase-2220eef8-0e72-46de-8ba0-15aeb020ba19`、`runtime-evm-foundry-adapter-spec-investigation.csv`、`tool-blockscout-network-rules-investigation.csv`、`blockscout-integration-current.json` | 阻塞 blockscout 平台编排 | 在 runtime/tool 编排 SSOT 中补 `infra/postgres-graph` 和命名端口，不绕过 NetworkPolicy 校验 |
| P1-075 | P1 | 检查 tool 清单和 DB seed | 当前 IDE 统一使用 `code-server`；`fabric-explorer` 仅在安全准入后才允许进入 | `images/tool/theia` 不存在且不再作为当前准入工具；`fabric-explorer` manifest `deployable:false` 且按安全风险明确不加入本轮修复范围 | `tool` 表查询、`images/tool/fabric-explorer/manifest.yaml` | 不阻塞当前实验工具全集；fabric-explorer 不作为当前可修项 | 不补 Theia 兼容路径；fabric-explorer 维持禁用，除非未来有安全替代或可验证重建产物 |
| P1-076 | P1 | terminal-only 简单实验启动 | 用户 ≤5s 可进入 | 当前 `6.056s`，超过 SLA | `sandbox-simple-startup-sla-current.json` | 阻塞启动 SLA | 优化 PVC/调度/阶段一资源路径；不能放宽 SLA 或假 Ready |
| P2-077 | P2 | 运行旧 `run-sim-full.ps1`、`run-notify-full.ps1`、`run-sandbox-user-tool-real.ps1` | 测试脚本使用当前 OpenAPI 路径 | 多处仍调用旧 `/sim/sessions`、`/notify/send`、公有 sandbox create | 对应 current-run 日志 | 不代表产品失败，但阻碍回归 | 清理旧测试脚本，统一到 OpenAPI 当前路径 |

## 14. 2026-07-01 续测：实验全链路修复后回归

本轮按文档要求从实际后端、镜像、K8s 编排和安全边界回归，不以旧脚本通过作为实现标准。证据仍只写入 `.tmp/backend-functional-test/evidence/`；未提交 `.tmp`、`backend/.env`、`deploy/config/secret.env`、`deploy/config/supply-chain.secret.env`、`deploy/config/supply-chain.kubeconfig` 或 Cosign 私钥。

### 14.1 环境与镜像证明

| 项 | 结果 | 证据 |
| --- | --- | --- |
| 后端部署 | `chaimir-backend` 运行 `harbor.chaimir:30080/service/backend@sha256:bccb6647ca7b989ae8822f1e6d0844ecf0a245250943db53bd86605b38d82388` | `kubectl -n chaimir-system get pods -o wide --show-labels`、本轮命令输出 |
| migrate/seed | `chaimir-migrate` 完成；acceptance seed 需单独运行 `seed-acceptance` Job，不能假设 `migrate-and-seed` 覆盖验收数据 | `seed-acceptance` Job 输出、DB judger/resource_spec 查询 |
| 供应链 | 重新生成 `images/image-digests.lock` 和 `SANDBOX_IMAGE_ATTESTATIONS_JSON`，`attested_count=63`、`blocked_count=0` | `.tmp/backend-functional-test/evidence/attest-expfix-20260701-012/` |
| runtime 预拉取 | `runtime_image.prepull_status=2`，5 个节点 ready，`image_count=13` | runtime prepull API 响应、DB 查询 |

### 14.2 已修复并回归通过

| 编号 | 级别 | 根因 | 修复方式 | 回归结果 |
| --- | --- | --- | --- | --- |
| P1-071 | P1 | 实验环境编排和 judge seed 使用旧 runtime 命令模型，环境型 checkpoint 无法进入真实判题闭环 | M3 judger 统一建模为 M2 private sidecar + `exec_target`；M7 创建环境时走同一 M2 contract；acceptance seed 使用 `judger/testcase-evm` digest 和完整 sidecar spec | `run-experiment-cross-module-supplement.ps1` 44/44，通过实验创建、成员、角色、环境、checkpoint 判题、报告、成绩、通知、审计和回收 |
| P1-073 | P1 | 后端重建后旧 lock/attestation 不能作为准入标准 | 重新构建 `service/backend`，刷新 `images/image-digests.lock`、attestation JSON、local-dev digest 并重新部署 | `attested_count=63`、`blocked_count=0`；部署 digest 与 lock 一致 |
| P1-074/P1-067 | P1 | Blockscout/db-viewer 编排未纳入当前 tool manifest/seed 口径 | 以 manifest 驱动 tool seed，并按声明式组件/网络规则创建 PostgreSQL + Blockscout/db-viewer 组合 | `run-blockscout-dbviewer-k8s.ps1` 通过：Blockscout `stats/blocks/transactions/address` 读真实链数据，db-viewer 只访问声明数据源 |
| P1-061 | P1 | command-tool 旧契约按裸退出码吞掉 stdout/stderr | 命令工具统一返回结构化 stdout/stderr/exit_code，并由工具镜像保证真实分析命令可执行 | `run-experiment-tool-deep-retest.ps1` 中 `slither`、`mythril`、`gas-profiler` 通过 |
| P2-068 | P2 | 旧证据未覆盖实验成绩通知链 | 实验完成后统一走 score/outbox/notify/audit 链路 | `db-side-effects-current.txt` 显示 `experiment_score_outbox`、`notification`、`audit_log` 记录 |
| P2-077 | P2 | 部分 `.tmp` 脚本仍读旧 token/旧端口 | 只修 `.tmp` 证据脚本，统一每轮刷新 token 和当前 18081 后端入口 | 跨模块回归从 38/41 变为 44/44；不作为产品代码变更 |

### 14.3 实验和工具覆盖表

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| 实验创建/编排/草稿/发布/会话/成员/角色/环境/状态/刷新/回收 | 通过；跨模块链路 44/44 | `experiment-cross-module-supplement-results-current.json`、`experiment-cross-module-supplement-summary-current.json` |
| IDE/业务工具真实操作 | `code-server`、`jupyter`、`remix`、`contract-ui`、`wallet-sim`、`slither`、`mythril`、`gas-profiler`、`novnc-desktop`、`terminal` 通过；Theia 已移出当前 IDE 范围；Fabric Explorer 安全风险不准入 | `experiment-tool-deep-summary-current.json` |
| Blockscout/db-viewer 多组件 | 通过；Blockscout 读真实链数据，db-viewer 只访问声明 PostgreSQL，NetworkPolicy 未放开平台 DB | `blockscout-dbviewer-summary-current.json`、`blockscout-dbviewer-k8s-resources.txt` |
| 代理/鉴权/隔离 | 工具浏览器代理通过；Cookie/token 边界通过；WebSocket 默认进入 `student-shell`，显式 runtime 容器被拒 | `sandbox-cluster-proxy-summary-current.json` |
| K8s 资源 | Pod、Service、NetworkPolicy、ResourceQuota、LimitRange、ServiceAccount、PVC 均真实创建；回收后无 `sbx-*` namespace 和 `chaimir-labs` 残留 | `sandbox-k8s-resource-*`、`k8s-sbx-namespaces-post-tests-current.txt`、`k8s-labs-post-tests-current.txt` |
| 回收闭环 | 失败会写 `sandbox_event:error` 并重试；成功后才写 destroyed/recycle/outbox/audit，namespace 已删除 | `db-side-effects-current.txt` |
| 中间件副作用 | MinIO ready 200，NATS health ok，Redis `PONG`，ClamAV `PONG` | `middleware-health-current.json`、`redis-ping-current.txt` |
| 路由暴露面 | 公开 API 对照可达；Ingress 将 `/api/readyz` 和 `/api/v1/*/internal` 黑洞到无 endpoint service | `ingress-public-deny-current.json` |

### 14.4 仍未关闭

| 编号 | 级别 | 复现步骤 | 期望 | 实际 | 证据 | 阻塞 | 处理结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P1-072 | P1 | 查询 VolumeSnapshot API/CRD | 本地 K8s 提供 CSI snapshot 能力 | 当前集群没有 VolumeSnapshot API resources | `k8s-volumesnapshot-capability-current.txt` | 阻塞快照专项，不阻塞普通实验链路 | 需安装 CSI snapshot CRD/Class 后再测；本轮不靠下载配置冒充通过 |
| P1-076 | P1 | terminal-only 简单实验启动两次 | 用户 ≤5s 可进入 | 连续两次 `5.44s`，功能可进入但超过 5 秒 | `sandbox-simple-startup-sla-current.json`、`sandbox-simple-startup-sla-failures-current.txt` | 阻塞严格简单 SLA | 本地 local-path/PVC/调度性能缺口，暂不为 0.44s 做假 Ready、砍工具或放宽阈值 |
| P1-066 | P1 | 前端 Pod | 前端镜像可拉取 | `chaimir-frontend` 仍为 `ImagePullBackOff` | `kubectl -n chaimir-system get pods -o wide --show-labels` | 阻塞前端 UI 联调，不阻塞后端实验回归 | 前端未开发完成，本轮不处理 |

### 14.5 结论

当前可进入后端实验功能的后续联调准备：除 VolumeSnapshot 专项和简单启动 5 秒 SLA 外，实验模块主流程、教师/学生全链路、准入工具、多组件编排、数据隔离、回收闭环和路由暴露面已在真实 Docker/K8s 环境复测通过。

生产镜像部署测试仍需谨慎：后端和实验相关镜像本轮已重新生成 digest/attestation，但前端镜像未准入，VolumeSnapshot 能力缺失，简单 SLA 未达标；这些不能用兼容分支、假 Ready 或旧 lock 规避。

## 15. 2026-07-01 续测：竞赛模块完整测试阶段发现

本轮按 `docs/08-竞赛`、OpenAPI、M2/M3/M5/M10/M11 依赖链重新测试竞赛相关后端，不以旧脚本 HTTP 200 为通过标准。证据目录：`.tmp/backend-functional-test/evidence/contest-20260701-214905/`。测试脚本问题已在 `.tmp` 内修正：真实提交物改为 MinIO tar + SHA256；管理统计、通知公告、内部 HMAC、WebSocket 路径均按当前 OpenAPI/DTO 重跑，`run-contest-admin-notify-internal-current.ps1` 结果 11/11，`run-contest-ws-topic-current.ps1` 证明同租户 topic 可订阅、跨租户 topic/未登录拒绝。

### 15.1 已覆盖但未判定全通过

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| 竞赛生命周期 | 创建、编辑草稿、发布、报名、团队、开始、结束/自动归档、结果快照均已触达 | `contest-full-summary-second-pass.json` 71/71；DB `contest_result_snapshot` |
| 多端视图 | 教师/学校管理员/平台管理员聚合/学生/内部服务入口已测；平台管理员直访 M8 拒绝，学校管理员 M8 列表允许 | `contest-admin-notify-internal-summary-current.json` 11/11 |
| 沙箱/工具链 | 竞赛环境真实创建 Pod/Service/NetworkPolicy/Quota/SA/PVC；工具代理鉴权、workspace、terminal 默认 `student-shell`、runtime 容器拒绝、回收闭环通过 | `contest-sandbox-deep-*`、DB `sandbox_event`/`sandbox_recycle_outbox` |
| 通知/WebSocket | 管理公告、内部 push、跨租户 topic 拒绝、`/api/ws?token=` topic 鉴权通过 | `contest-admin-notify-internal-results-current.json`、`contest-ws-topic-summary-current.json` |
| 镜像供应链 | 本轮竞赛实际调用 `runtime/evm-foundry`、`tool/code-server`、`judger/testcase-evm` 均使用 Harbor digest，且在最近 attestation/Trivy/Cosign 证据中 passed | `images/image-digests.lock`、`.tmp/.../attest-expfix-20260701-012/` |

### 15.2 新增/更新问题清单

| 编号 | 级别 | 复现步骤 | 期望 | 实际 | trace_id/证据 | 阻塞 | 根因与修复方式 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P0-078 | P0 | 运行中解题赛提交代码：`POST /api/v1/contest/contests/198074793751220224/problems/198074793856077824/submit` | M8 创建提交，M3 真实执行 `solidity-unit`，写 `judge_result`，M8 更新提交/排行榜/通知 | `judge_task=198074823761465344` 进入失败终态，`last_error=[21015] 判题私有域配置不正确`，无 `judge_result`，`solve_submission.passed=false score=0` | `contest-full-results-second-pass.json`、DB `judge_task`/`judge_result`/`solve_submission` | 阻塞解题赛真实判题、排行榜、通知、成绩链路 | 根因是验收 seed 中 `acceptanceEVMJudgerSidecar` 设置 `CHAIMIR_SUBMISSION_DIR=/judge-private`，但 sidecar 只挂载 `/judge` 和 `/tmp`，未声明判题私有域挂载。应改 migration/seed 源和判题器 resource_spec，使 M3/M2 私有域契约一致，然后重新 seed/回归 |
| P0-079 | P0 | 创建对抗赛 `198080117367377920`，添加 `ctf-reentrancy-vault:1.0.0`，提交攻/防参战物并等待 matchmaker | 对抗赛恢复双方参战物到 M2 对局沙箱，M3 reuse/onchain 判定，写 `battle_match.judge_task_ref`、结算 ELO/榜单/通知 | `battle_match=198080231523749888` 失败，`sandbox_ref/judge_task_ref` 为空；对局沙箱创建后因 `battle_judge_submit_failed` 回收；无对应 `judge_task` | `contest-battle-poll-6.txt`、DB `battle_match`/`sandbox_event`/`judge_task` | 阻塞对抗赛完整执行 | 根因是文档要求 M8 以 M3 reuse 模式读取现场沙箱且不得把参战物伪装成代码字段，但当前 DB 只有 `solidity-unit` J1 判题器，没有 battle 兼容的 `onchain-assert` J2 判题器/题库项；M8 也允许普通 testcase 题进入对抗赛直到执行时失败。应补 canonical battle judger/content seed，或在 M8 选题/发布阶段拒绝不兼容题 |
| P1-080 | P1 | 按 docs/08 与 M3/M5 文档梳理所有竞赛题型并查 DB seed | CTF/flag、编程/判题、智能合约漏洞利用/修复、链上交互、多阶段、团队协作、仿真/沙箱/判题依赖题均有可真实测试题/判题器/工具 | 当前 `content_item` 仅 1 个竞赛题、1 个实验模板、1 个理论题；`judger` 仅 `solidity-unit`；无 J2/J3/J5/J6、无对抗专用题、无多阶段/仿真竞赛题 | DB `content_item`/`content_body`/`judger` 查询 | 阻塞“所有题型/赛制”真实验收 | 不是脚本问题，是当前 seed/实现覆盖不足。应按 M5/M3/M8 文档补齐 canonical 题型、判题器和工具环境，再逐题型回归 |
| P1-081 | P1 | 新建解题赛和对抗赛后检查排行榜、通知、成绩/成就链 | fresh 判题完成后产生排行、M10 推送/通知、M8 internal achievements/M11 读侧证据 | 管理/通知/WS 权限链通过，但 fresh 解题判题失败、对抗赛失败；新建 result snapshot 为空，notification 无 contest/judge-completed 记录；只能证明 seed 预置榜单 | `contest-admin-notify-internal-results-current.json`、DB `ladder_rank`/`contest_result_snapshot`/`notification` | 阻塞成绩/榜单/通知真实闭环验收 | 下游阻塞来自 P0-078/P0-079。修复判题后必须完整回归，不允许只复测失败点 |
| P2-082 | P2 | `kubectl get volumesnapshotclasses.snapshot.storage.k8s.io` | 如本地 K8s 支持 CSI Snapshot，则测快照链路 | 当前集群没有该资源类型 | kubectl 输出：`the server doesn't have a resource type "volumesnapshotclasses"` | 阻塞快照专项，不阻塞普通竞赛链路 | 环境能力缺失，按要求记录，不下载配置冒充通过 |
| P2-083 | P2 | 从 Windows 宿主请求 Ingress IP/NodePort 的 `/api/v1/contest/internal/stats` | 公网入口实际请求被 `chaimir-public-deny` 阻断 | K8s Ingress 配置存在 deny 规则，但宿主访问 `172.18.0.4`/NodePort 超时，无法形成 HTTP 级实测证据 | `kubectl -n chaimir-system get ingress chaimir-public-deny -o yaml`、curl 超时记录 | 阻塞公网暴露面“实测”证据 | 本地网络路径问题。可后续从集群内 curl Pod 或修 host route 复测；当前不能把超时冒充通过 |

### 15.3 本轮明确不是代码问题的脚本误差

| 脚本问题 | 处理 |
| --- | --- |
| 旧竞赛脚本用占位 tar/hash 导致 M3 输入归档错误 | 已上传真实 MinIO tar，更新 `.tmp/.../run-contest-full-current.ps1` |
| 管理/通知补测使用旧统计路径、错误公告 DTO、非规范 topic、错误 HMAC/参数位置 | 已修 `.tmp/.../run-contest-admin-notify-internal-current.ps1`，复测 11/11 |
| WebSocket 初测使用 `/api/v1/notify/ws` 和 Authorization header | 已按 OpenAPI 改为 `/api/ws?token=<jwt>`，同租户/跨租户/未登录结果符合预期 |
| 沙箱深测 summary 保留旧终端事件计数失败 | DB 证据已证明 terminal 默认进入 `sandbox/student-shell`，显式 runtime/no-auth 被拒；脚本 summary 文案需清理，不列为代码问题 |

### 15.4 当前结论

竞赛模块尚未完成“所有功能均通过”的验收。已覆盖多端入口、生命周期、沙箱/工具、通知/WebSocket、镜像 digest 和数据副作用，但真实判题与对抗赛执行两条核心链路存在 P0 阻塞，导致排行榜/成绩/通知只能验证权限和预置数据，不能声明完整通过。下一步必须先统一修复 P0-078/P0-079，并补齐 P1-080 所需 canonical 题型/判题器/工具环境后，再执行完整回归。

### 15.5 2026-07-02 修复后完整回归

本轮按文档修复实现，不写旧路径兼容或模块内重复能力。修复后重新构建并推送 `service/backend`、`service/migrate`，刷新 `images/image-digests.lock`、`deploy/overlays/local-dev` digest 和 `SANDBOX_IMAGE_ATTESTATIONS_JSON`，重新部署 K8s 后回归。证据目录仍为 `.tmp/backend-functional-test/evidence/contest-20260701-214905/`。

| 编号 | 级别 | 根因 | 修复方式 | 复测证据 | 结果 |
| --- | --- | --- | --- | --- | --- |
| P0-078 | P0 | M3 J1 判题 sidecar 声明了 `/judge-private` 输入目录，但 M2 runtime adapter 没有 `judge-private` volume domain，导致私有判题域无法挂载 | 修改 acceptance seed 源，为 `evm-foundry` runtime adapter 增加 `judge-private` 私有卷域，学生侧容器不挂载；重新 seed/部署 | `contest-full-final-after-eventbus-fix-20260702.log` 73/73；`db-sandbox-recycle-final-20260702.txt` 显示 judge-private 只作为 M3 私有域写入 | 已修复 |
| P0-079 | P0 | 对抗赛缺少 canonical J2/reuse 判题器和对抗题；M8 允许 fresh/J1 题进入 battle，直到执行阶段才失败 | 补 `onchain-assert` J2 backend strategy judger、`battle-reentrancy-duel:1.0.0` M5 内容项；M8 添加题时通过 M3 `ValidateJudgeMode` 校验 battle 必须支持 reuse；battle 结算按攻防角色映射 M3 passed 结果 | `contest-admin-battle-notify-final-after-script-fix-20260702.log`：match `198332996732850176` status=3 result=2；`db-ladder-judge-outbox-notify-audit-final-20260702.txt` | 已修复 |
| P1-080 | P1 | 验收 seed 题型覆盖不足，无法按文档真实跑 CTF/编程/链上交互/对抗等类型 | 在本轮后端范围补齐 CTF/编程基础题、链上交互/对抗题及 J2 判题器；智能合约漏洞草稿/预验证接口、团队协作、沙箱依赖题均纳入脚本覆盖；仿真竞赛题受 M4/M8 题型扩展设计限制，未新增伪实现 | `contest-full-final-after-eventbus-fix-20260702.log`、`contest-admin-battle-notify-final-after-script-fix-20260702.log`、`contest-sandbox-deep-final-after-eventbus-fix-20260702.log` | 主要后端题型链路已补齐；仿真竞赛题仍需产品设计扩展 |
| P1-081 | P1 | P0 阻塞导致排行榜/成绩/通知只能看到预置数据，无法证明真实 judge 完成后的副作用 | 修复 P0 后完整回归；battle judge completed 写 `battle_match`、`ladder_rank`、`audit_log`、`sandbox_recycle_outbox`；M11 按文档只读 M8 `contest-achievements`，竞赛成就与 GPA 分离，不直接写 M11 GPA | `db-ladder-judge-outbox-notify-audit-final-20260702.txt`、`db-grade-ladder-counts-final-20260702.txt` | 已按文档闭环 |
| P0-084 | P0 | M3 `judge_event_outbox` 发布时把 JSON payload 解成 `any`，18 位租户 ID 经 `float64` 精度丢失后发布为 `910000000000000000`，M8 事件消费用错误租户查 M3 任务，battle 完成事件进死信 | eventbus metadata 解析改用 `json.Decoder.UseNumber`；M3 outbox 以 `json.RawMessage` 发布保留大整数；M8 matchmaker 增加 running battle match 补偿，只通过 M3 contract 读取终态并复用现有 handler 幂等结算 | `db-battle-compensated-after-eventbus-fix-20260702.txt` 显示旧 running match 结算；新 battle 回归 match status=3 | 已修复 |
| P2-083 | P2 | 初测从宿主机直连 Ingress 地址无响应，证据不足 | 通过 `ingress-nginx-controller` port-forward 带 Host 头实测 `/api/v1/contest/internal/stats` 不进入后端；Ingress deny 规则指向 `chaimir-public-deny` | `ingress-internal-route-deny-final-20260702.txt` status=503；`ingress-public-deny-describe-final-20260702.txt` | 已补证 |

| 回归项 | 结果 | 证据 |
| --- | --- | --- |
| 后端静态验证 | `go test ./...`、`go build ./cmd/server ./cmd/migrate` 通过 | 本轮命令输出 |
| 镜像供应链 | `service/backend@sha256:01bec1df5a9e87628e024d63a5522eb8d06d68be96b7b415bec5aa3a7d7b89a7`，`service/migrate@sha256:e686bce806c20391f51bc4288264051af8b5b6128179d190b061d5a3d714ab57`；`attested_count=63`、`blocked_count=0` | `service-images-build-push-20260702-144030.transcript.txt`、`attest-contestfix-20260702-144137/` |
| K8s 部署 | backend Pod 运行新 digest，migrate Job complete，dry-run/server apply 通过 | `backend-pod-image-after-eventbus-fix-20260702.txt`、`k8s-backend-rollout-after-eventbus-fix-20260702.txt` |
| 竞赛全链路 | 73/73 通过 | `contest-full-final-after-eventbus-fix-20260702.log` |
| 沙箱/工具/K8s 深测 | tool proxy、Cookie/token、terminal 默认 `student-shell`、runtime/no-auth 拒绝、卷边界、NetworkPolicy、回收通过 | `contest-sandbox-deep-final-after-eventbus-fix-20260702.log` |
| 管理/对抗/通知/内部 HMAC | 29/29 通过，真实 battle match 完成 | `contest-admin-battle-notify-final-after-script-fix-20260702.log` |
| WebSocket topic | 同租户订阅通过，跨租户/未登录拒绝 | `contest-ws-topic-final-after-eventbus-fix-20260702.log` |
| 数据副作用 | `judge_task`/`judge_result`/`judge_event_outbox`、`battle_match`、`ladder_rank`、`sandbox_event`、`sandbox_recycle_outbox`、`audit_log` 均有真实记录；竞赛成就供 M11 只读，不写 GPA | `db-ladder-judge-outbox-notify-audit-final-20260702.txt`、`db-sandbox-recycle-final-20260702.txt`、`db-grade-ladder-counts-final-20260702.txt` |

当前后端竞赛主链可进入后端联调；前端 UI 联调仍受 `chaimir-frontend` ImagePullBackOff 阻塞。生产镜像部署测试方面，后端与竞赛相关 runtime/tool/judger 镜像已按 Harbor digest、Trivy、Cosign、attestation 准入；但本地集群仍无 VolumeSnapshot CRD/Class，且前端镜像未准入，因此不能声明全平台生产部署测试已就绪。
