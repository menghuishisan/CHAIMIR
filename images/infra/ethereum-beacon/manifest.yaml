# 本文件声明 infra/ethereum-beacon 的 Ethereum Beacon API 能力和运行边界。
schema_version: 1
category: infra
name: ethereum-beacon
image: infra/ethereum-beacon
labels:
  app.kubernetes.io/name: ethereum-beacon
  app.kubernetes.io/component: infra
  app.kubernetes.io/part-of: chaimir
  chaimir.io/category: infra
  chaimir.io/image: infra/ethereum-beacon
  chaimir.io/data-driven: "true"
  chaimir.io/student-access: "false"
  chaimir.io/sensitivity: sandbox-infra
  chaimir.io/scope: sandbox
  chaimir.io/source: upstream-pinned
description: Sigma Prime Lighthouse 官方 Beacon Node 镜像。
source:
  type: upstream-pinned
upstream:
  registry: docker.io
  image: sigp/lighthouse
  version: 8.2.2
  digest: sha256:cdb2a6e5eb6197091896b4dac5e607ce696e09355614b56cbe5fb40b1a4dac29
  source_repository: https://github.com/sigp/lighthouse
  license: Apache-2.0
  reuse_policy: upstream-pinned
data_driven: true
capabilities:
  provides:
    - ethereum-beacon-api
    - beacon-node
    - consensus-state
  requires: []
  conflicts: []
  cardinality: one
  placement: sandbox
  config_schema:
    type: object
    additionalProperties: false
    required: [network, data_dir]
    properties:
      network:
        type: string
      data_dir:
        type: string
        x-chaimir-mount-domain: runtime-state
  student_access: private
infra:
  eco_tags: [evm]
  roles: [beacon, consensus-api-provider]
  command_schema:
    executable: lighthouse
    required_args: [beacon, --http, --http-address, --http-port]
    forbidden_args: [validator, account]
env_keys:
  config:
    - BEACON_NETWORK
    - BEACON_DATA_DIR
    - BEACON_HTTP_PORT
  secret: []
ports:
  - name: http
    container_port: 5052
    protocol: TCP
    expose: internal
    purpose: Ethereum Beacon REST API
security:
  run_as_non_root: true
  read_only_root_filesystem: false
  allow_privilege_escalation: false
  privileged: false
  host_network: false
  automount_service_account_token: false
  drop_capabilities: [ALL]
  network_policy: deny-all
security_exceptions:
  - field: read_only_root_filesystem
    reason: Beacon 节点需要写数据库、状态和缓存。
    writable_mounts: [runtime-state]
student_access:
  enabled: false
resources:
  cpu_request: 500m
  cpu_limit: "2"
  memory_request: 1Gi
  memory_limit: 4Gi
  ephemeral_storage_limit: 8Gi
selftest:
  commands:
    - name: lighthouse-version
      command: [lighthouse, --version]
  upstream_healthcheck:
    type: http
    path: /eth/v1/node/health
    port: 5052
supply_chain:
  sbom: true
  trivy_required: true
  cosign_required: true
  pull_by_digest: true
