// pagination constants 文件维护公开分页契约的稳定边界。

/** PAGINATION_DEFAULT_SIZE 与后端 pagex 默认页大小一致。 */
export const PAGINATION_DEFAULT_SIZE = 20

/** PAGINATION_MAX_SIZE 与后端 pagex 单次查询上限一致。 */
export const PAGINATION_MAX_SIZE = 100
